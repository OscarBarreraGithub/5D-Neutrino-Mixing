# C17 — Redundant .gitkeep cleanup

**Unit:** C17 (Tier 5, deterministic)
**Closes:** R09-I2 INFO
**Commit:** `63255fa` (inline by orchestrator)
**Status:** DONE

## Background

A previous Opus agent attempted C17 in a worktree branch (commit `4a92835` on `worktree-agent-a44592c93406fccc6`). Cherry-picking onto `main` hit a merge conflict on `.orchestration/ISSUES.md` (the worktree branch was based off pre-C16 state). To avoid the conflict cost, the orchestrator did C17 inline using the same logic.

## What was done

Listed all `.gitkeep` files via `git ls-files | grep '\.gitkeep$'` and counted tracked files per dir. Removed `.gitkeep` from any dir with ≥2 tracked files. Kept it in genuinely empty dirs.

### Removed (12)

| Dir | Tracked files (before) | Status |
|-----|------------------------|--------|
| flavor_catalog/references/ | 870 | removed |
| flavor_catalog/worklogs/pka/ | 102 | removed |
| flavor_catalog/worklogs/writer/ | 62 | removed |
| flavor_catalog/worklogs/checker/ | 62 | removed |
| flavor_catalog/processes/beauty/ | 46 | removed |
| flavor_catalog/processes/top_higgs_ew/ | 41 | removed |
| flavor_catalog/processes/kaon/ | 28 | removed |
| flavor_catalog/processes/charged_lepton/ | 24 | removed |
| flavor_catalog/processes/charm/ | 18 | removed |
| flavor_catalog/processes/edm_neutrino/ | 16 | removed |
| flavor_catalog/worklogs/discovery/ | 6 | removed |
| flavor_catalog/signoff/by_process/ | 4 | removed |

### Retained (1)

- `flavor_catalog/signoff/round_index/.gitkeep` — sole tracked file in its dir.

## Checks

1. **Issue resolved**: 12 redundant placeholders removed; one legitimate retained. ✓
2. **No regression**: no code/test/doc touched; pure deletion of redundant tracking markers. ✓
3. **No physics impact**: N/A (filesystem-only). ✓

## Bookkeeping

- ISSUES.md: R09-I2 moved to `### Closed by C17`.
- CLEANUP_QUEUE.md: C17 row → DONE with verdict.
- cleanup_progress.json: C17 → done.
