# C18 — MERGE_PLAN retroactive corrections

**Unit**: C18
**Tier**: 5
**Lane**: det
**Date**: 2026-05-26
**Issues closed**: R12-I1, R12-I2, R12-I3, R13-I1, R14-I1, R15-I1, R16-I3, R19-I1, R19-I2, R19-I5
**Bonus SHA typos fixed (uncatalogued)**: `ebdo66c` → `ebd066c` (R18 row); `cd3a8fe` → `cd8a3fe` (R19 row). Flagged in C15 report as MERGE_PLAN bookkeeping follow-up.

## Pre-flight

- `git status --porcelain` → only untracked `.orchestration/cleanup_reports/*.md`, `artifacts/collaborator_*`, `scripts/export_collaborator_*`, plus pre-existing review-r1/r2/cleanup-plan-r2 docs. No staged changes in MERGE_PLAN.md prior to this unit.
- Working tree on branch `flavor-catalog/2026q2`.
- All SHA fixes verified against `git log --all --format='%h %s'`.

## Per-fix evidence

### R15-I1 — `7500919` → `7500794`

- Pattern grep: `grep -n "7500919" .orchestration/MERGE_PLAN.md` → line 308 R15 commit list.
- SHA verification:
  - `git log --all --format='%h %s' | grep '^7500919'` → no match (typo confirmed).
  - `git log --all --format='%h %s' | grep '^7500794'` → `7500794 flavor-catalog(charged_lepton_top): CA batch ca_w6_charged_lepton_top_v2 — verify WA polish for L023 T020` (canonical).
- Before: `` `ee5c95b`, `67912c0`, `cd8816d`, `7500919`, `6d97db8` ``
- After:  `` `ee5c95b`, `67912c0`, `cd8816d`, `7500794`, `6d97db8` ``

### R19-I1 — `1cf8b57` → `1cd8b57`

- Pattern grep: `grep -n "1cf8b57" .orchestration/MERGE_PLAN.md` → line 312 R19 commit list.
- SHA verification:
  - `git log --all --format='%h %s' | grep '^1cf8b57'` → no match (typo confirmed).
  - `git log --all --format='%h %s' | grep '^1cd8b57'` → `1cd8b57 flavor-catalog(wave9): PKA-CR005 initial draft (collider_rs PRIMARY)` (canonical).
- Before: `` `42428d7`, `1cf8b57`, `a13edbb` ``
- After:  `` `42428d7`, `1cd8b57`, `a13edbb` ``

### R19-I2 — restore `82daa9b`; count 33 → 34

- Pattern grep: confirmed `82daa9b` was absent from MERGE_PLAN.md:312 R19 commit list (only mentioned at lines 12/46/47/65/68).
- Range verification: `git log --oneline 7ed9117^..864cd6d | wc -l` → 34.
- Chronological position (verified via `git log --oneline 7ed9117^..864cd6d --reverse`):
  - 29: 950ca36 CA-v2 vlq_4top cycle-2 verdict
  - 30: 82daa9b CA-v2 ew_tail cycle-2 verdict  ← restored
  - 31: 0c5dacc fact-check report collider_rs family
- Before: `` `e1aec33`, `950ca36`, `0c5dacc`, `cdd0238`, `2ad34b1`, `864cd6d` (33 commits ``
- After:  `` `e1aec33`, `950ca36`, `82daa9b`, `0c5dacc`, `cdd0238`, `2ad34b1`, `864cd6d` (34 commits ``
- Granularity-check line 317 updated: "R19 is now correctly 33 commits (down from 34 after the `82a96f0` removal)" → "R19 is now correctly 34 commits (R03 commit `82a96f0` is excluded by design; missing `82daa9b` restored per C18)". The "82a96f0 belongs to R03 ONLY" caveat preserved.

### R19-I5 — `claim_level` → `limit_type`

- Pattern grep: `grep -n "claim_level" .orchestration/MERGE_PLAN.md` → line 413 R19 grep-target list (the §C.1 entry) and line 114 (collaborator CSV provenance JSON, a different context — left untouched, since that one is the actual schema field used in the CSV).
- Schema verification: `grep -c "limit_type:" flavor_catalog/processes/collider_rs/*.yaml` → all 14 CR0XX yamls carry `limit_type` (under `pdg_or_equivalent.values[]`); `grep -l "claim_level" flavor_catalog/processes/collider_rs/*.yaml` → no match in the schema.
- Before: `` each `CR0XX` ID, the `claim_level` field, the `pdg_or_equivalent` field, the SHA-256 ``
- After:  `` each `CR0XX` ID, the `limit_type` field (under `pdg_or_equivalent.values[]`; the planner-era guess `claim_level` is not in the schema), the `pdg_or_equivalent` field, the SHA-256 ``

### R14-I1 — strike "K012, K018" from R14 description

- Pattern grep: `grep -n "T006 v2, B001, B016, K012, K018" .orchestration/MERGE_PLAN.md` → line 307 R14 description.
- Cross-reference verification: `git log --all --oneline -- flavor_catalog/processes/kaon/K012.yaml flavor_catalog/processes/kaon/K018.yaml` → `6a16bf4` (K012 PKA) and `98b203a` (K018 PKA); both are in the R15 commit list (line 308 lists `e9d13b7` etc., but K012/K018 PKAs land in R15 per their `git log --all -- ` provenance), not R14.
- Before: `T006 v2, B001, B016, K012, K018; codex quota pause + resumption plan.`
- After:  `T006 v2 + 8 other Wave-5 CA closures + B001 + B016 PKAs; codex quota pause + resumption plan. (K012, K018 belong to R15 per R14-I1.)`

### R12-I1, R12-I2, R12-I3, R13-I1, R16-I3 — retroactive-corrections block

Added a new "Retroactive corrections (C18, 2026-05-26)" annotation block between the granularity check (line 317) and the uniqueness invariant (now line ~325). The block records:
- **R12-I1**: original R12 dispatch prompt described "L008 (mu->e Au)" but L008 is actually `tau- -> e- gamma`; SINDRUM-II Au limit lives at L004. W4 = 20 PKAs, not "12+".
- **R12-I2**: commit `7d3da08` bundles EW001 + E004 PKAs (workflow-seam irregularity).
- **R12-I3**: commit `bcd8907` bundles L001 Opus signoff + W4 kaon_edm CA cycle-1 verdict (E004/E006/E008/K017 `CHECKER-DONE`) + `ca_w4_kaon_edm.md` worklog (three independent workstreams).
- **R13-I1**: R13 dispatch prompt mis-described K010/B023/E007 as anchors; canonical referents are K010 = `K_S → π⁰ e⁺e⁻`, B023 = `B → K*(892) ν ν̄`, E007 = `²²⁵Ra / ¹²⁹Xe atomic EDMs`. R13 reviewer cross-verified the actual catalog content.
- **R16-I3**: B012 disposition note — DA-4 deferred → Wave-7 ACTIVE on external-review grounds. Preferred future framing: "DA-4 converged at 75; Wave-7 amended active coverage to 80 (4 unallocated + 1 previously-deferred B012 promoted on external-review grounds)."

### Bonus (uncatalogued) — `ebdo66c` → `ebd066c` (R18) and `cd3a8fe` → `cd8a3fe` (R19)

- `git log --all --format='%h %s' | grep '^ebd066c'` → `ebd066c flavor-catalog(wave8): PKA-B013 initial draft (SECONDARY)`.
- `git log --all --format='%h %s' | grep '^ebdo66c'` → no match (typo: `o` for `0`).
- `git log --all --format='%h %s' | grep '^cd8a3fe'` → `cd8a3fe flavor-catalog(wave9): WA polish WA-w9-custodial (cycle 1)`.
- `git log --all --format='%h %s' | grep '^cd3a8fe'` → no match (typo: digit transposition).
- Both flagged by C15 as MERGE_PLAN bookkeeping follow-up (closes the C15 forward-pointer in one stroke).

## Three checks

1. **Each issue body in ISSUES.md cross-checked against post-edit MERGE_PLAN.md state**: confirmed above per-fix.
2. **`git log --all --oneline | grep <SHA>` resolves each new SHA citation**:
   - `7500794` → CA w6 charged_lepton_top v2 (canonical).
   - `1cd8b57` → PKA-CR005 initial draft (canonical).
   - `82daa9b` → CA-v2 ew_tail cycle-2 verdict (canonical).
   - `ebd066c` → PKA-B013 initial draft (canonical).
   - `cd8a3fe` → WA polish WA-w9-custodial cycle 1 (canonical).
3. **No code change → no test re-run**. Pure `.orchestration/` markdown edit.

## Files touched

- `.orchestration/MERGE_PLAN.md` — 7 surgical edits (5 SHA strings + 1 description + 1 grep-target + 1 granularity-check sentence + 1 new annotation block).
- `.orchestration/cleanup_reports/C18.md` (new — this file).
- `.orchestration/ISSUES.md` (will be updated below to move the 10 closed issues to `### Closed by C18`).
- `.orchestration/CLEANUP_QUEUE.md` + `.orchestration/cleanup_progress.json` (status PENDING → DONE).

## No-test-rerun rationale

No Python source, no YAML schema, no `.tex` content, no notebook, no website code touched. Only orchestration-layer Markdown. The `pytest_selection/C18.txt` exists from C15 as `# no test changes in this unit`, consistent with this unit's no-source-touch invariant.
