# C19 — Final docs sweep + methodology PDF rebuild

**Unit:** C19 (Tier 5)
**Closes:** R08-I3 INFO + R04-I4 substantive forwarding from C03
**Commit:** (inline by orchestrator)
**Status:** DONE

## Why inline

The Opus implementer agent ran in a worktree branch (`agent-aacf8354bbe295412`, commit `28acb98`). Cherry-picking onto main hit a merge conflict on `.orchestration/ISSUES.md` (the worktree branch was based off pre-C18 main state). The orchestrator extracted the agent's content patches via `git show 28acb98 -- <file>` and applied them to main directly, preserving the agent's actual work product.

## What was done

1. **Methodology PDF rebuilt** via 2× `pdflatex -interaction=nonstopmode quark_scan_methodology_note.tex`. Output: 20 pages, 627505 bytes (matches the worktree's rebuild byte-exactly).
2. **`docs/quark_scan_methodology_note.tex`** gained a one-line cross-reference sentence pointing at `docs/audits/wilson_rg_inventory.md` "Status as of 2026-05-25 post-cleanup" (the substantive R04-I4 forwarding from C03).
3. **`docs/audits/figure_prune_inventory.md`** gained a new "Status as of 2026-05-26 post-cleanup (C19)" block recording the accept-as-paper-archive-companion disposition for the historical-snapshot dirs (`quark_pre_audit_constants/` retained, `quark_baseline_800k/` already absent).
4. **ISSUES.md** updated:
   - R08-I3 moved from `## Open` to new `### Closed by C19` section
   - R04-I4 (already marked Closed by C03 with substantive forwarding to C19) updated in place; the C19 commit closes the forwarding loop
5. **CLEANUP_QUEUE.md + cleanup_progress.json** flipped C19 row → DONE.

## Checks

1. **Issue resolution**: R08-I3 closed via documented acceptance + provenance block. R04-I4 substantive piece closed by the methodology-note cross-reference. ✓
2. **No regression**: docs-only change; no test re-run; no code touched. PDF rebuild byte-deterministic (matches worktree agent's bytes). ✓
3. **No physics impact**: zero numerical content changes. ✓

## Bookkeeping

- ISSUES.md `## Open` reduced (R08-I3 closed)
- ISSUES.md `### Closed by C19` section added
- CLEANUP_QUEUE.md C19 row → DONE
- cleanup_progress.json C19 → done

## Note on worktree leftover

The agent's worktree (`.claude/worktrees/agent-aacf8354bbe295412`, branch `worktree-agent-aacf8354bbe295412`) is locked at commit `28acb98`. Same as the C07 and C17 worktrees — these will be pruned during final cleanup before pushing.
