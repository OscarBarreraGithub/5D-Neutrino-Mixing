# C10 — T010/T011 cross-reference + sha256 backfill + worklog-alias notes

**Tier:** 4 · **Lane:** opus · **Issues closed:** R10c-I1, R10c-I2, R10c-I3 (all INFO, tag:docs) · **Status:** DONE

## Scope

Three INFO-tag housekeeping issues from the R10c review of the six R10c PKAs
(T001, T002, T010, E001, C001, L001):

- **R10c-I1** — Plan-v1 row T011 (Z-pole asymmetries A_FB^b, A_b) was folded
  into T010 (R_b) during the Wave-1 PKA cycle; the merge is internally
  consistent but the plan-row -> file mapping was implicit (no T011.yaml).
- **R10c-I2** — Reference-snapshot dirs `T002/`, `T010/`, `E001/` lacked a
  top-level `sha256sums.txt` listing (T001, C001, L001 carry one).
- **R10c-I3** — Four R10c PKAs (T002, E001, C001, L001) were polished under
  sibling Wave-2/3 batches; a reader filtering worklogs by `wave1` would
  see WA/CA evidence for T001/T010 only.

C10 is a docs/provenance cleanup. No physics or numerical fields are touched.

## R10c-I1 — T010/T011 cross-reference

Per the brief, picked option (b): created a non-intrusive
`flavor_catalog/processes/top_higgs_ew/T011.yaml` **stub** with
`schema: flavor_catalog.process.v1`, `merged_into: T010`, a `canonical_home`
block, a one-entry `status_history` recording the merge closure, and a
`[CLOSED-as-merged-by-C10]` `open_issues` bullet. No `.tex` file is added
because `flavor_catalog/processes/top_higgs_ew/index.tex` uses
`\IfFileExists` and a `T011.tex` stub would render a hollow section in the
master compile; the merge is already documented in
`T010.tex:14-17` ("Plan v1 lists R_b as T010 and the asymmetries as T011;
this entry follows the batch assignment...") and in
`worklogs/discovery/round_004_addendum_deferred_scope.md:25` ("T011: Folded
into T010; promote if Zbb asymmetries need separate chiral ownership.").
The stub carries no physics content.

## R10c-I2 — sha256 backfill

Generated `sha256sums.txt` in each of the three referenced dirs using
`sha256sum *.txt source_manifest.yaml > sha256sums.txt`:

| Dir | Snapshot files | manifest | Total entries |
|---|---|---|---|
| `flavor_catalog/references/T002/` | 6 .txt | `source_manifest.yaml` | 7 |
| `flavor_catalog/references/T010/` | 6 .txt | `source_manifest.yaml` | 7 |
| `flavor_catalog/references/E001/` | 5 .txt | `source_manifest.yaml` | 6 |

The format matches the existing `sha256sums.txt` in T001/C001/L001/B011/
B012/..., so any future audit tool walking `references/*/sha256sums.txt`
sees a uniform six-of-six R10c coverage.

## R10c-I3 — worklog-alias notes (NOT addressed by C08)

Verified the C08 report explicitly: C08 closed R10a-I2, R10a-I3, and
R10b-I3 (open_issues thread closures in 7 Wave-1 yamls). It did **not**
touch R10c-I3. So C10 owns this issue.

Chose the field-level alias option (less intrusive than spawning four
empty `wa_wave1_*` worklog stubs). Each of the four affected yamls (T002,
E001, C001, L001) gets a new block inserted between `opus_signoff_id` and
`status_history`:

- A comment explaining that the PKA is `wave1` per plan v1 but was
  polished under the sibling w23 batch, plus an explicit list of the
  sibling worklog files.
- `wave_assignment: "wave1"` — top-level scalar.
- `worklog_alias:` block with `family_batch`, `writer_worklogs` (list of
  paths), and `checker_worklogs` (list of paths). L001 additionally carries
  `opus_arbitration_id: "opus_arbitration_L001"` because its status_history
  records an Opus arbitration on top of three CA cycles.

This satisfies the "wave-1-named worklog stub OR worklog_alias field"
option from the brief, picking the field-level form because the worklog
files themselves are correctly named for the batch they actually executed
(w23_*) — renaming or aliasing them would muddy *that* audit trail.

## Files touched (5 yamls + 3 sha256sums.txt)

| File | Change |
|------|--------|
| `flavor_catalog/processes/top_higgs_ew/T011.yaml` | NEW — stub, `merged_into: T010` |
| `flavor_catalog/processes/top_higgs_ew/T002.yaml` | `wave_assignment` + `worklog_alias` block |
| `flavor_catalog/processes/edm_neutrino/E001.yaml` | `wave_assignment` + `worklog_alias` block |
| `flavor_catalog/processes/charm/C001.yaml` | `wave_assignment` + `worklog_alias` block |
| `flavor_catalog/processes/charged_lepton/L001.yaml` | `wave_assignment` + `worklog_alias` block + opus_arbitration_id |
| `flavor_catalog/references/T002/sha256sums.txt` | NEW — 7 entries |
| `flavor_catalog/references/T010/sha256sums.txt` | NEW — 7 entries |
| `flavor_catalog/references/E001/sha256sums.txt` | NEW — 6 entries |

## Verification (three brief-mandated checks)

1. **Issues resolved.** R10c-I1 closed by `T011.yaml` stub + the existing
   `T010.tex:14-17` cross-reference. R10c-I2 closed by the three new
   `sha256sums.txt` files. R10c-I3 closed by the four new
   `worklog_alias` blocks.
2. **yaml parse.** `yaml.safe_load` succeeds on all five touched yamls.
   Schema string `flavor_catalog.process.v1` preserved on T011 stub plus
   the four edited yamls. Spot-check on T011: `process_id='T011'`,
   `merged_into='T010'`, `canonical_home.process_id='T010'`.
3. **sha256sums.txt validates.** `cd flavor_catalog/references/T002 &&
   sha256sum -c sha256sums.txt` returns 7/7 OK; same on T010 (7/7 OK) and
   E001 (6/6 OK).

## Out of scope

- No `T011.tex` stub (would render an empty section in the master
  compile; merge is already documented in T010.tex).
- No renaming of the sibling `wa_w23_*` / `ca_w23_*` worklog files (their
  filenames correctly reflect the batch they ran).
- No fact-check rerun (no `pdg_or_equivalent`, `supporting_values`, or
  numerical fields touched).
- No website rebuild — the website JSON content at
  `flavor_catalog/website/src/content/entries/T010.json` continues to use
  the existing T010.json (the website does not consume `worklog_alias`
  or T011.yaml directly).
