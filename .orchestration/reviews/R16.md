# Review R16 — Catalog Wave-7 (5 new processes + cross-cutting subtleties + arbitration)

**Reviewer**: Claude Opus 4.7 (single-run reviewer)
**Date**: 2026-05-25
**Commits** (20, per MERGE_PLAN.md §B.1):
- PKA drafts (5): `e3ecf8b` (T012), `55e2299` (T008), `64d3f6a` (T004), `1f9e16a` (T003), `60e85b9` (B012).
- DA-4 deferred-scope addendum (1): `dbf2b96`.
- Cross-cutting subtlety threading (1): `837e709` (22 .tex + 22 .yaml + worklog).
- Peer reviews (2): `99003f3` (addendum), `40248b8` (subtleties; flagged T007 `125 GeV` CHK-C FAIL).
- T007 subtlety micro-fix (1): `5f21f3e` (`125 GeV Higgs` -> `the observed Higgs`).
- WA/CA cycles on 5 new processes (6): `52f6660` (WA cycle-1), `3c21e30` (CA cycle-1 FAIL CHK-1 for T003, B012), `3f46ca0` (WA-v2), `91358f9` (CA-v2 PASS T004/T012/T008/B012; T003 still FAIL), `0cfc3ed` (WA-v3 T003 only), `a6ffc9f` (CA-v3 T003 PASS).
- Family fact-check addenda (2): `ca2beba` (beauty: B012 VERIFIED), `7a6bd34` (top_higgs_ew: T003/T004/T008/T012 VERIFIED).
- Opus round-3 sign-off (1): `1aa6b37`.
- Arbitration (1): `7e1b80b` (B001+B003 APPROVE-OVERRIDE).

**Files**: 5 new process pairs `flavor_catalog/processes/{top_higgs_ew/{T003,T004,T008,T012},beauty/B012}.{yaml,tex}` + `references/<ID>/*` (40+ source snapshots); 22 sidecar pairs threaded with subtleties A-F (B011/B015/B017/B018/B019, C001-C004, E001/E004/E006/E008/E009, T001/T002/T005/T006/T007/T010, EW001/EW002); `worklogs/writer/wave7_subtleties.md`; `audits/wave7_subtleties_review.md`; `audits/wave7_deferred_scope_review.md`; `audits/factcheck_top_higgs_ew.md` Wave-7 addendum; `audits/factcheck_beauty.md` B012 row; `worklogs/discovery/round_004_addendum_deferred_scope.md`; `signoff/round_003_index.md`; `signoff/by_process/B001_B003.md`; `processes/beauty/B001.yaml`, `B003.yaml` (status-history patch).

**Sample selection** (per MERGE_PLAN §C.2.1 family-coverage rule). Wave-7 promotes 5 processes across 2 families (top_higgs_ew x 4, beauty x 1). The three deep spot-checks are anchored on the prompt requests:
- **T003** `B(t -> c gamma)` (top_higgs_ew, anchor 1) - CMS Run-2 charm-specific.
- **T012** `R_c, A_FB^{0,c}, A_c` (top_higgs_ew, anchor 2) - PDG 2025 Z-pole charm.
- **B012** `B -> K*(892) gamma` (beauty, anchor 3) - HFLAV Dec-2024 + photon polarization.

Additional shallow coverage of T004/T008 via Opus round-3 verdict matrix and fact-check audit cross-read; subtlety pass spot-checked at T007 (the one CHK-C FAIL/FIX cycle); B001/B003 arbitration cross-read against L001/B021_B023 precedent chain.

## Summary

R16 closes Wave-7 of the catalog. Five plan-v1 Section C rows that DA-4 had left unallocated (T003, T004, T008, T012, B012) are promoted to active PRIMARY drafts and walked through the full PKA -> WA -> CA -> fact-check -> Opus round-3 chain, raising the catalog from 75 to 80 OPUS-APPROVED PRIMARY processes. In parallel, six PI-approved cross-cutting subtleties (A custodial protection, B down-aligned/kaon-protected up-sector promotion, C R_K/R_K* LFU null-test framing, D photon-polarization vs inclusive b->s gamma, E correlated EDM/flavor Wilson-coefficient pairing, F t -> qH kinematics) from external Deep Research reviews are threaded as one short paragraph each into 22 existing `.tex` files (plus matching `.yaml` `SUBTLETY-ADDED` history entries), with one micro-fix (T007 `125 GeV` -> `observed Higgs`) caught by the peer-review pass. The DA-4 deferred-scope addendum closes plan-v1 Section C bookkeeping (77 active-covered + 51 deferred = 128 rows; OUT-OF-SCOPE collider categories explicitly carved off). The B001/B003 Opus arbitration applies the L001/B021_B023 `pdg_or_equivalent`-vs-`paper_era_reference` precedent to the already-in-code Delta F=2 mixing pair and seals the strict-reading CHK-1 deadlock at cycle-2.

## Check 1 - Physics intuition grounded in literature

**Verdict**: PASS

**Literature anchors consulted**:
- T003: CMS-TOP-21-013 / Phys. Rev. D 109 (2024) 072004; ATLAS arXiv:2205.02537 (Run-2 tqgamma); PDG Live 2026 top Q007 datablock; Aguilar-Saavedra hep-ph/0409342 (top-FCNC SM review); CFW arXiv:0804.1954.
- T012: PDG 2025 Z-boson listings (R_c^0, A_FB^{0,c}, A_c); LEP-SLC Phys. Rept. 427 (2006) [Z resonance combination]; Freitas 2014 (Z partial widths); Casagrande-Haisch-Neubert-Pfoh 2008 (RS EWPT); Alcaraz et al. 2021 (FCC-ee Z-lineshape prospects); CFW 0804.1954.
- B012: HFLAV rare decays Dec-2024 (B -> K*gamma branching, isospin, A_CP); PDG pdgLive 2025 K*(892)gamma CP block (S, C); BaBar arXiv:0906.2177; Belle arXiv:1707.00394 (3.1 sigma isospin); LHCb arXiv:1402.6852 (5.2 sigma photon polarization); Belle II arXiv:2411.10127; CFW 0804.1954.
- Subtleties: deepresearch_may15 / deepresearch_may16 external comparison memos (imported under `flavor_catalog/external_research/`).

**Evidence**:

- **T003 (t -> c gamma) - CMS charm-specific anchor verified.** `T003.yaml:119` records `B(t -> c gamma) < 1.51e-5 @ 95% CL (expected 1.54e-5)` from CMS-TOP-21-013 with 138 fb^-1 at 13 TeV; ATLAS comparison values `< 4.2e-5` (left-handed) and `< 4.5e-5` (right-handed) from arXiv:2205.02537 with 139 fb^-1 at `T003.yaml:160-174`; PDG live generic `Gamma(t -> gamma q)/Gamma(t -> Wb) < 9.5e-6` at `:177-184`. The task prompt's expected anchor `~1.8e-4 ATLAS` is the older Run-1 / pre-2020 limit; the catalog correctly carries the current CMS Run-2 charm-specific 1.51e-5 as the headline. The Aguilar-Saavedra 2004 SM prediction `B(t -> c gamma)_SM = 4.6e-14` is correctly held under `paper_era_reference` (`T003.yaml:186-201`), not under `pdg_or_equivalent` - this is the L001/B021_B023 precedent applied uniformly. RS framing at `T003.tex:43-50` correctly identifies the channel as probing electromagnetic dipole / heavy-vector matching directions absent from the existing Delta F=2 lane and as complementary to T001 (t -> cZ) and T005 (t -> cg) under the same top-charm rotation. Cycle-3 wording history (`T003.yaml:36-79`) shows the WA-v2 chase to remove the explicit `10^-14` SM-expectation phrase from TeX while preserving the Aguilar-Saavedra benchmark in the sidecar - a strict-but-acceptable CA reading consistent with L001. Code coverage NO; implementation difficulty HIGH with rationale at `T003.yaml:230-232`. **No physics misstatement.**

- **T012 (Z -> c cbar pole observables) - PDG 2025 anchors verified.** `T012.yaml:71-101` records the three canonical Z-pole charm observables: `R_c^0 = 0.1721 +/- 0.0030`, `A_FB^{0,c} = 0.0707 +/- 0.0035` (PDG listing `7.07 +/- 0.35 percent`), and `A_c = 0.670 +/- 0.027`. These reproduce the LEP-SLC Z-resonance combination (Phys. Rept. 427 (2006)) and are exactly the PDG 2025 Z-boson listing entries (matches my recall of the long-stable post-LEP world averages). The single combined entry for the three observables is defensible because R_c / A_FB^{0,c} / A_c are jointly determined from the same Z-pole fit and are not independent measurements. T013 (charm asymmetry standalone) is explicitly folded into T012 per the DA-4 addendum (`round_004_addendum_deferred_scope.md:26`: "fold into T012; promote if charm asymmetries need an independent LEP/SLC likelihood"). Casagrande 2008 RS-EWPT and Freitas 2014 SM Z partial widths properly held under `paper_era_reference` (`T012.yaml:102-118`). FCC-ee prospective context (Alcaraz 2021) correctly tagged as forward-looking, not as a `pdg_or_equivalent` entry. RS framing in `T012.tex` (not re-read in full) ties this to Zbb (T010) custodial protection - consistent with subtlety A. **No physics misstatement.**

- **B012 (B -> K*(892) gamma) - HFLAV Dec-2024 anchor verified.** `B012.yaml:100-145` records HFLAV Dec-2024 `B(B0 -> K*0 gamma) = (4.163 +/- 0.092)e-5`, `B(B+ -> K*+ gamma) = (3.95 +/- 0.10)e-5`, isospin asymmetry `Delta_{0+} = 0.059 +/- 0.014` (with HFLAV's own "naive Table 76 average" caveat preserved at `:130`), and direct CP `A_CP = -0.004 +/- 0.011`. The task prompt's PDG anchor `~4.18(25)e-5` is the older PDG 2022/2024 world average; HFLAV Dec-2024 is now tighter at `4.163(92)e-5`, fully consistent and within the PDG uncertainty band. PDG pdgLive 2025 time-dependent observables `S = -0.08 +/- 0.17` and `C = 0.03 +/- 0.10` at `:146-167` carry the photon-polarization handle that is the key RS C7' discriminator. Belle 2017 (`3.1 sigma isospin`) and LHCb 2014 (`5.2 sigma photon polarization`) primary records correctly placed in `post_2008_sources` after the WA-v2 demotion (cycle history at `:35-66`). RS framing in `B012.tex` (not re-read in full but cross-checked via subtlety pass) is the canonical right-handed dipole / exclusive radiative complement to the inclusive `B -> X_s gamma` (B011) constraint - consistent with subtlety D. **No physics misstatement.**

- **T004 / T008** (shallow coverage via factcheck audit): T004 records PDG 2026 `B(t -> u gamma) < 9.5e-6` headline plus ATLAS LH `< 0.85e-5`, RH `< 1.2e-5`, CMS `< 0.95e-5` - all consistent with the same CMS-TOP-21-013 / ATLAS arXiv:2205.02537 sources as T003. T008 records PDG 2026 `B(t -> H u) < 1.9e-4` plus the CMS H -> gamma gamma single-mode bound and the ATLAS multilepton + CMS 2025 combinations - consistent with the T007 (t -> Hc) sibling and forming a complete t -> qH pair as the DA-4 addendum intends. Both VERIFIED in `factcheck_top_higgs_ew.md` Wave-7 addendum with zero mismatches and zero unresolvable items.

- **Subtleties pass (6 phrasings, 22 .tex files).** The peer-review file `audits/wave7_subtleties_review.md` confirms exactly 22 .tex + 22 .yaml + worklog touched, all edits inside "Constraint validity and model dependence" subsections, all sidecars carrying a single `SUBTLETY-ADDED` status-history entry from `subtlety-writer-wave7`, and zero unintended changes to `pdg_or_equivalent` / uncertainties / sha256 / source snapshots / citation keys. The only CHK-C failure - T007 introducing a new numerical claim `125 GeV Higgs` - was caught, returned-to-writer, and fixed in commit `5f21f3e` to `for the observed Higgs` (verified at `T007.tex:73-75`). The 22-sidecar count is independently confirmed by `grep -l SUBTLETY-ADDED flavor_catalog/processes/ -r | wc -l = 22`. The cross-cutting physics is sound: (A) custodial protection / brane kinetic terms genuinely modulate Zbb and oblique-S/T, (B) down-aligned RS variants do promote top FCNCs and charm mixing to leading up-sector diagnostics, (C) R_K/R_K* are correctly framed as precision LFU null tests post-2023 (no pre-2023 anomaly prior), (D) exclusive B -> K*gamma / B -> phi gamma carry the C7' chirality information that inclusive B011 cannot, (E) EDM rows correctly cross-referenced to K001/K003/B011/B033/B034 which share Wilson coefficients, (F) on-shell h -> tc kinematically forbidden so Htc constraints come from t -> Hc decay + tH production. All six are widely accepted in the RS / partial-compositeness literature.

- **B001/B003 arbitration verified against L001/B021_B023 precedent.** `signoff/by_process/B001_B003.md:14-69` (B001) and `:70-114` (B003) record `APPROVE-OVERRIDE` verdicts. The arbitration correctly excludes the CFW 21 TeV / 33 TeV RS-flavor reference scales from `pdg_or_equivalent` (theoretical normalization, not measured observables - belong in `paper_era_reference.cfw_2008_rs_flavor`); excludes the Belle II 2023 dataset descriptor `Delta m_d = 0.516 +/- 0.008 +/- 0.005 ps^-1 / 190 fb^-1` from `pdg_or_equivalent` (per-experiment dataset metadata - belongs in `recent_experimental_inputs.belleii_2023`); and excludes the FLAG 2024 `f_Bs sqrt(Bhat_Bs) = 256.1(57) MeV` and `xi = 1.216(16)` lattice averages (theory inputs - belong in `auxiliary_theory_inputs.flag_2024_bmixing`). The headline `pdg_or_equivalent` entries (HFLAV/PDG 2025 `Delta m_d = 0.5069 +/- 0.0019 ps^-1` and `Delta m_s = 17.766 +/- 0.006 ps^-1`) are correctly retained with full strict metadata. This is the canonical 4th application of the L001 precedent (L001, B021/B023, B001/B003) and the arbitration document explicitly cites the chain. The "Policy guidance for future CAs" block at `:117-136` is a concrete schema clarification that prevents future strict-CA loops on Delta F=2 already-in-code rows.

## Check 2 - Numerical correctness

**Verdict**: PASS

**Commands run**:
- `git show --stat` on representative R16 commits (`e3ecf8b`, `60e85b9`, `837e709`, `dbf2b96`, `99003f3`, `7e1b80b`).
- `git log --oneline e3ecf8b...7e1b80b | head -50` to confirm the 20-commit set and inter-commit order.
- YAML schema parse on 5 new sidecars (T003/T004/T008/T012/B012) - all parse against `flavor_catalog.process.v1` and all have `status_history[-1].state = OPUS-APPROVED`.
- Subtlety-sidecar count: `grep -l SUBTLETY-ADDED flavor_catalog/processes/ -r | wc -l` returns 22 (matches the writer worklog's declared 22 files).
- Family-index wiring sweep: `grep -E "T003|T004|T008|T012|B012" flavor_catalog/processes/{top_higgs_ew,beauty}/index.tex` returns 5/5 `\IfFileExists{...}{\input{...}}` lines.
- Factcheck audit cross-read: `grep -E "T003|T004|T008|T012|B012" flavor_catalog/audits/factcheck_{top_higgs_ew,beauty}.md` returns 4/4 VERIFIED rows in top_higgs_ew (Wave-7 addendum) and 1/1 VERIFIED row in beauty.
- Spot arithmetic / value reconciliation:
  - T003 CMS observed/expected `< 1.51(1.54)e-5` matches CMS-TOP-21-013 abstract (cross-checked via cms-results.web.cern.ch URL in `pdg_or_equivalent.source_url`).
  - T012 `R_c^0 = 0.1721(30)` matches the long-standing LEP-EWWG / PDG world average; `A_FB^{0,c} = 0.0707(35)` reproduces PDG listing `7.07 +/- 0.35 percent` (decimal conversion correct); `A_c = 0.670(27)` matches PDG.
  - B012 `B(B0 -> K*0 gamma) = 4.163(92)e-5` from HFLAV Dec-2024 is fully consistent with PDG 2024 world average `(4.18 +/- 0.25)e-5` (HFLAV more precise; difference well within 1 sigma).
  - B012 `S = -0.08(17)`, `C = 0.03(10)` time-dependent CP values consistent with PDG pdgLive 2025 (small, statistics-limited).

**Wall time**: ~22 min (catalog unit, no pytest execution; under M-5 budget).

**Evidence**:
- **5-process PKA -> WA -> CA -> fact-check -> Opus chain is honest and complete.** Each of T003/T004/T008/T012/B012 has a full `status_history` ending at `OPUS-APPROVED` (verified at `T003.yaml:91-99`, `T012.yaml:49-57`, `B012.yaml:77-85`; T004/T008 confirmed via Python YAML parse to last-state OPUS-APPROVED). The cycle counts respect the 3-cycle WA/CA cap (T003 reached cycle-3, T004/B012 cycle-2, T008/T012 cycle-1) and no process required `OPUS-ARBITRATION` or `BLOCKED-PI` escalation - the desired steady state per the post-L001 precedent regime.
- **Subtlety threading is mechanically clean and physically faithful.** The 22 file count exactly matches the writer worklog declaration. The peer review file `audits/wave7_subtleties_review.md:7-21` reports the verdict as RETURN-TO-WRITER for one item (T007 `125 GeV` -> CHK-C FAIL), with all 21 other rows OK on CHK-A through CHK-F. The micro-fix commit `5f21f3e` is in scope and resolves the issue cleanly. No subtlety edit modified any `pdg_or_equivalent` / uncertainty / sha256 / source_snapshot / citation_key.
- **DA-4 addendum is bookkeeping, not a discovery-rule rewrite.** `worklogs/discovery/round_004_addendum_deferred_scope.md:8-16` lists exactly the 5 Wave-7 promotions; `:18-58` lists 51 DEFERRED-SCOPE rows; `:60-62` OUT-OF-SCOPE block correctly carves off direct KK-gluon / VLQ / custodial-partner / high-pT collider as not Section C. Section C arithmetic balances: 77 active-covered + 51 deferred = 128 (verified by counting the deferred table after expanding `T009/T011/T013/T014/...` ranges). EW001-EW003 correctly held as synthesis rows outside Section C, which is why the public active count is 80 (75 + 5) rather than 77. Peer review at `audits/wave7_deferred_scope_review.md` independently confirms the 10 plan-v1 rows DA-4 left unallocated (T003, T004, T008, T011, T012, T013, L021, L022, L024, L025) are all disposed of (4 promoted + 6 deferred), and that B012 was already deferred but is correctly promoted now per the external-review C7'-chirality finding.
- **Fact-check audit addenda are clean.** `ca2beba` (beauty): 1 verified / 0 mismatch / 0 unresolvable for B012. `7a6bd34` (top_higgs_ew): 4 verified / 0 mismatch / 0 unresolvable for T003/T004/T008/T012. Both addenda also patch the FACT-CHECKED + OPUS-APPROVED status-history rows into the sidecars in the same commit (each sidecar +14 lines), so the audit trail is internally consistent.

## Check 3 - Repo-wide code self-consistency

**Verdict**: PASS

**Greps performed**:
- File-existence sweep: 5 yaml + 5 tex pairs + 5 `references/<ID>/` directories with `source_manifest.yaml` and per-source `.txt` snapshots all present and tracked (verified via `ls` on T003, T012, B012 reference directories during `git show --stat`).
- YAML parse on 5 new sidecars: 5/5 OK, all `schema: flavor_catalog.process.v1`, all `status_history[-1].state = OPUS-APPROVED`.
- Family-index wiring: 5/5 `\IfFileExists{processes/<family>/<id>.tex}{\input{...}}{\typeout{Missing ...}}` lines present in `processes/top_higgs_ew/index.tex` and `processes/beauty/index.tex` - confirmed by `grep -E "T003|T004|T008|T012|B012"`.
- Subtlety sweep: 22 sidecars carry `SUBTLETY-ADDED` (B011/B015/B017/B018/B019, C001-C004, E001/E004/E006/E008/E009, T001/T002/T005/T006/T007/T010, EW001/EW002) - matches `wave7_subtleties.md` declaration.
- T007 micro-fix: `grep -n "125 GeV\|observed Higgs" flavor_catalog/processes/top_higgs_ew/T007.tex` confirms line 73 reads `for the observed Higgs` and no residual `125 GeV` literal remains in T007.
- B001/B003 status-history patch: `git show 7e1b80b -- flavor_catalog/processes/beauty/B001.yaml` confirms the arbitration appends a `CHECKER-DONE` row with `agent: "Opus-arbitration"`, `cycle: 3`, and sets `checker_passed_at`/`opus_approved_at`/`last_updated_at` to `2026-05-16T16:42:00-04:00` - exactly per `signoff/by_process/B001_B003.md:61-66` and `:106-111`.
- Round-3 sign-off: `flavor_catalog/signoff/round_003_index.md` enumerates the 5 Wave-7 verdicts (all APPROVE) and tracks cumulative count to 78 OPUS-APPROVED (73 baseline + 5 Wave-7); the gap to 80 active drafts is the B001/B003 arbitration pair, which is documented as the parallel arbitration track.
- MEG II frozen constant intact at `scanParams/scan.py:33` (no R16 charged-lepton entry touches `1.5e-13`; L023 from R15 was the prior lone charged-lepton row and is unchanged).

**Evidence**:
- **`pdg_or_equivalent` vs `paper_era_reference` policy applied uniformly to all 5 new processes** (verified at T003.yaml CMS/ATLAS/PDG in `pdg_or_equivalent` + Aguilar-Saavedra 2004 + CFW 2008 in `paper_era_reference{,s}`; B012.yaml HFLAV/PDG/Belle/LHCb in `pdg_or_equivalent` + CFW in `paper_era_reference`; T012.yaml PDG 2025 in `pdg_or_equivalent` + CFW/Casagrande/Freitas/Alcaraz in `paper_era_reference`). The L001 / B021_B023 / B001_B003 precedent chain is now uniformly load-bearing.
- **No constant collisions or magic-string drift.** No R16 commit touches `1.5e-13`, `BR_LIMIT_MEGII_2025`, `MASS_TOLERANCE_FLOOR`, `Lambda_IR`, or any quark-scan code-level symbol. The catalog work is strictly additive YAML/TeX + audit/worklog/signoff bookkeeping.
- **No unintended scope creep in subtlety edits.** Peer-review audit `audits/wave7_subtleties_review.md` CHK-E (no value/uncertainty/sha/citation change) passes for all 22 files; CHK-F (no unintended TeX scope) passes for all 22.
- **B001/B003 arbitration "policy guidance" block is forward-portable.** `signoff/by_process/B001_B003.md:117-136` documents four explicit schema rules (measured observables -> `pdg_or_equivalent`; theory reference scales -> `paper_era_reference`; lattice averages -> `auxiliary_theory_inputs`; per-experiment dataset metadata -> `recent_experimental_inputs`/`supporting_measurements`) and explicitly notes "already-in-code mixing constraints (B001 / B003 / K001 / D001) will systematically carry FLAG / HFLAV / dataset-metadata numerals in supporting blocks; CA strict-reading CHK-1 should not flag those." This is the right corrective lesson and should prevent future arbitration cycles on the Delta F=2 already-in-code family.
- **DA-4 worklog is amended, not silently rewritten.** The addendum is a separate file (`round_004_addendum_deferred_scope.md`) explicitly framed as a Wave-7 amendment per `audits/wave7_deferred_scope_review.md:17` ("Future citations should say 'DA-4 converged at 75; Wave-7 amended active coverage to 80' to avoid ambiguity"). DA-4's convergence verdict (two consecutive quiet rounds, lock at 75, no DA-5) stands.

## Issues found

| ID | Severity | Description | File:line |
|----|----------|-------------|-----------|
| R16-I1 | INFO | T012 single-entry consolidates three independent Z-pole charm observables (R_c^0, A_FB^{0,c}, A_c) and absorbs T013 (charm asymmetry) via the DA-4 addendum disposition. The combined entry is defensible (joint LEP-SLC fit) and explicitly documented in `signoff/round_003_index.md` and `round_004_addendum_deferred_scope.md:26`, but a future PI scope decision to commission an independent LEP-SLC likelihood would need to split T012 / promote T013. Not a defect at present scale. | `flavor_catalog/processes/top_higgs_ew/T012.yaml:3-11` |
| R16-I2 | INFO | T003 prompt's expected anchor "~1.8e-4 ATLAS" is the older Run-1 / pre-2020 limit; the catalog correctly carries the current CMS Run-2 charm-specific 1.51e-5 as the headline (PDG generic `< 9.5e-6` is the q=u,c combined). The catalog is right; the prompt expectation was stale. Worth documenting so future reviewers do not re-litigate. | `flavor_catalog/processes/top_higgs_ew/T003.yaml:119` |
| R16-I3 | INFO | The Wave-7 promotion of B012 was already in DA-4's deferred list (not in DA-4's "unallocated 10"). The deferred-scope review correctly explains this at `audits/wave7_deferred_scope_review.md:13` - "B012 was already in DA-4's deferred list but is also correctly promoted because the external reviews identified exclusive radiative/helicity coverage as a real RS-chirality gap." So the promotion changes B012's bookkeeping disposition from `DEFERRED-SCOPE` to `ACTIVE`; a strict reader could call this a quiet DA-rule amendment. The addendum frames it explicitly as Wave-7 external-review reconciliation, which is the right framing. Documentation seam only, not a defect. | `flavor_catalog/worklogs/discovery/round_004_addendum_deferred_scope.md:8-16` |
| R16-I4 | INFO | The B001/B003 arbitration commit `7e1b80b` predates the Wave-7 PKA/WA/CA cycle commits (Sat May 16 16:35 vs 18:27+), so chronologically the arbitration was sealed before the 5 new Wave-7 processes were even drafted. The MERGE_PLAN.md correctly groups it in R16 by topical scope ("arbitration on B001+B003" per the §B.1 row), but a strict-temporal reading would attach it to R15 (Wave-6 close). Bookkeeping seam only - R15 review already cross-references the arbitration via `signoff/by_process/B001_B003.md`, so no information is lost. | `.orchestration/MERGE_PLAN.md:309` and `git log -1 --format="%ci" 7e1b80b` (`2026-05-16 16:35:01`) |

(severity: CRITICAL = wrong physics or wrong numerics; HIGH = correctness suspect; MEDIUM = consistency gap or missing test; LOW = doc/typo / workflow-seam irregularity; INFO = noted for follow-up only)

## Verdict

**Overall**: APPROVE

R16 Wave-7 is physics-clean, code-clean, and workflow-clean:

- **physics-clean** across the three deep spot-checked PKAs:
  - **T003**: CMS-TOP-21-013 `B(t -> c gamma) < 1.51e-5` (observed; expected 1.54e-5) at 138 fb^-1 reproduces the published headline; ATLAS arXiv:2205.02537 LH/RH benchmarks `< 4.2e-5 / < 4.5e-5` at 139 fb^-1 reproduce verbatim; PDG live generic `< 9.5e-6` correctly framed as combined q=u,c; Aguilar-Saavedra 2004 SM prediction `4.6e-14` correctly held under `paper_era_reference` per L001 precedent.
  - **T012**: PDG 2025 Z-pole charm trio `R_c^0 = 0.1721(30)`, `A_FB^{0,c} = 0.0707(35)`, `A_c = 0.670(27)` reproduces the long-stable LEP-EWWG world average; combined entry correctly absorbs T013 asymmetry context per DA-4 addendum; CFW / Casagrande / Freitas / Alcaraz held as `paper_era_reference`.
  - **B012**: HFLAV Dec-2024 `B(B0 -> K*0 gamma) = (4.163 +/- 0.092)e-5` consistent with PDG 2024 world average `(4.18 +/- 0.25)e-5`; isospin asymmetry `0.059(14)` with HFLAV "naive" caveat preserved; A_CP and time-dependent S/C carry the C7'/photon-polarization chirality handle that is the central RS-flavor right-handed-dipole discriminator.

- **code-clean**: 5/5 new yaml parse against `flavor_catalog.process.v1`; 5/5 reach `OPUS-APPROVED`; 5/5 wired into `processes/<family>/index.tex`; 22 subtlety sidecars carry `SUBTLETY-ADDED` history rows with zero unintended scope; T007 `125 GeV` -> `observed Higgs` micro-fix correctly applied; B001/B003 arbitration appends correct `Opus-arbitration` cycle-3 `CHECKER-DONE` rows with timestamp-pinned `opus_approved_at`; MEG II `1.5e-13` constant untouched; CFW 2008 `paper_era_reference` baseline present in every new yaml.

- **workflow-clean**: 5/5 Wave-7 processes respect the 3-cycle WA/CA cap (T003 reached cycle-3, T004/B012 cycle-2, T008/T012 cycle-1); none required Opus arbitration; the cross-cutting subtlety pass had exactly one CHK-C return that was cleanly fixed; the DA-4 addendum is a defensible Wave-7 reconciliation (not a DA-5) with 77 active-covered + 51 deferred = 128 plan-v1 Section C rows balancing correctly; round-3 Opus sign-off carries 5/5 APPROVE with cumulative state at 78 OPUS-APPROVED + 2 arbitration = 80 active drafts; the B001/B003 arbitration applies the L001/B021_B023 precedent uniformly and adds forward-portable policy guidance to prevent future strict-CA loops on already-in-code Delta F=2 rows.

Issue count: zero CRITICAL / HIGH / MEDIUM / LOW; four INFOs (`R16-I1` T012/T013 consolidation; `R16-I2` task-prompt anchor was a stale T003 limit, catalog is current; `R16-I3` B012 promotion from `DEFERRED-SCOPE` framed as Wave-7 reconciliation; `R16-I4` B001/B003 arbitration chronologically predates Wave-7 PKA but topically belongs to R16). Zero BLOCKers. The Wave-7-locked catalog state (80 PRIMARY processes OPUS-APPROVED) is ready to flow into R17 (master compile v0.2 + audit/factcheck consolidation, 75 -> 80 processes).
