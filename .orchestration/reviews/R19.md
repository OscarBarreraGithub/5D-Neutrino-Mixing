# Review R19 — Catalog Wave-9 (14 collider_rs PRIMARY processes CR001-CR014) + master compile v0.4

**Reviewer**: Claude Opus 4.7 (single-run reviewer)
**Date**: 2026-05-25
**Commits** (33 per MERGE_PLAN.md §B.1 R19 row at `.orchestration/MERGE_PLAN.md:312`): scaffold `7ed9117`; runbook 14-PKAs-dispatched `108cae4`; 14 PKA initial drafts `5911918` (CR012), `f195c45` (CR013), `144cc61` (CR004), `4054e52` (CR011), `7614f6d` (CR007), `6176694` (CR010), `0a8e20a` (CR009), `28879ea` (CR001), `d09c6b3` (CR003), `835cc83` (CR002), `92b78c7` (CR008), `c22fb07` (CR014), `42428d7` (CR006), and `1cd8b57` (CR005 — see R19-I1: prompt+MERGE_PLAN list this as `1cf8b57`); whitespace clean `a13edbb`; scaffold-2 master-tex wire-in `b96036b`; 4 WA cycle-1 polishes `1eac13e` (vlq_4top), `c6d55b0` (kk_gauge), `2286a39` (ew_tail), `cd8a3fe` (custodial); 4 CA cycle-1 verdicts `e8daa00` (vlq_4top), `7a6fa3d` (kk_gauge), `bc35a06` (custodial), `e846f14` (ew_tail); 2 WA-v2 cycle-2 reworks `a8758ac` (vlq_4top: CR008/CR010), `e1aec33` (ew_tail: CR009/CR011); 2 CA-v2 cycle-2 verdicts `950ca36` (vlq_4top), `82daa9b` (ew_tail — see R19-I2: NOT listed in prompt or MERGE_PLAN §B.1 R19 row); fact-check `0c5dacc`; Opus round-5 sign-off `cdd0238`; master compile v0.4 `2ad34b1`; close-out `864cd6d`. Verified count via `git log --oneline 7ed9117^..864cd6d | wc -l` = 34 (33 listed + the missing `82daa9b`).

**Files**: `flavor_catalog/processes/collider_rs/{CR001..CR014}.{tex,yaml}` (14 × 2 = 28) + `index.tex`; 14 `flavor_catalog/references/CR0XX/` snapshot directories (each ~9-19 source TXTs + `sha256sums.txt` + `source_manifest.yaml`); `flavor_catalog/catalog_master.tex:51-61` (new top-level `\section{Collider RS Resonances}` wired in); `flavor_catalog/catalog_master.pdf` (179 pages, 913,614 bytes); `flavor_catalog/master_compile_v04_report.md`; `flavor_catalog/audits/factcheck_collider_rs.md` (new family audit, 220 lines); `flavor_catalog/signoff/round_005_index.md` (64 lines); `flavor_catalog/worklogs/orchestration/wave_009_runbook.md` (192 lines); 6 WA worklogs (`wa_w9_{custodial,kk_gauge,vlq_4top,ew_tail}.md` + 2 cycle-2 `*_v2.md`); 6 CA worklogs (matching pattern); annotated tag `flavor-catalog-v0.4` on commit `2ad34b1`.

**Sample selection** (per MERGE_PLAN §C.2.1 high-impact rule — 3 of 14):
- **CR001** `pp -> g_KK^(1) -> t tbar` — the canonical bulk-RS LHC signature; Lillie-Randall-Wang baseline; CMS-B2G-25-009 5.5 TeV exclusion; the headline KK-gluon resonance bound.
- **CR007** `pp -> G_KK^(1) -> diboson/diphoton/dilepton` — RS-distinguishing spin-2 KK-graviton (the unique RS gravitational signature, per round-5 sign-off cross-cutting observation §3); Agashe-Davoudiasl-Perez-Soni baseline.
- **CR009** `pp -> ell+ ell-` Drell-Yan high-mass-tail contact-operator EFT — went through **cycle-2 WA/CA rework** (CHK-1 historical-numeral removal per T003 precedent); checks that the rework was sound and the EFT translation is honest about its model dependence.

## Summary

R19 opens a NEW scope class — direct-collider RS resonance / heavy-partner searches — by PI directive (`wave_009_runbook.md:16-31`), complementary to (not replacing) the low-energy flavor entries in Waves 1-7. The 14 entries split into 4 sub-classes: custodial fermion partners (CR002 `T_{5/3}`, CR003 `T'`, CR004 `B`); KK-gauge resonances (CR001 KK-gluon, CR005 KK-Z/gamma, CR006 KK-W, CR007 KK-graviton); VLQ + 4-top (CR008 singlet, CR010 doublet, CR014 4-top); EW-precision-tail (CR009 DY EFT, CR011 longitudinal VBS, CR012 diboson, CR013 diphoton). All 14 are PRIMARY (implicit per `PRIORITY_TIERS.md:1` "no `priority_tier` field = PRIMARY"; orchestrator decision D-1). The pipeline is the Wave-7/8 standard (PKA → WA → CA → fact-check → Opus) with one structural difference: a new family directory `processes/collider_rs/` instead of `processes/secondary/<family>/`. Cycle-2 reworks were needed on 4 of 14 entries (CR008/CR009 CHK-1 historical-numeral removal per T003 precedent; CR010 CHK-1+CHK-2; CR011 CHK-2 canonical-key rename per B023 precedent — all policy-aligned, no Opus arbitration). Fact-check `factcheck_collider_rs.md` records 14/14 VERIFIED, 0 mismatches, 0 fetch exceptions — the cleanest fact-check rate of any wave (per runbook §6 closing note). Opus round-5 sign-off (`signoff/round_005_index.md`) gives 14/14 APPROVE. Master compile v0.4 lands at 179 pages (913,614 bytes; up from v0.3's 148 pages / 796 KB). Catalog state at v0.4: PRIMARY = 80 flavor (Waves 1-7) + 14 collider_rs (Wave-9) = 94 PRIMARY; SECONDARY = 8 (Wave-8); total = 102 OPUS-APPROVED.

## Check 1 — Physics intuition / literature anchor (CR001 + CR007 + CR009)

**Verdict**: PASS

**CR001 `pp -> g_KK^(1) -> t tbar` — canonical bulk-RS LHC signature**:
- Physics anchor: the headline value `pdg_or_equivalent.values[0]` (`CR001.yaml:81-109`) is CMS-B2G-25-009, arXiv:2603.23454, `0.5 < m(g_KK) < 5.5 TeV` excluded at 95% CL, 138 fb^-1 at sqrt(s)=13 TeV, 0/1/2-lepton ttbar channels — the current full-Run-2 best. The benchmark assumptions block `:104-109` documents that this is the experimental KK-gluon signal model with a large ttbar branching fraction; the `display: "0.5 < m(g_KK) < 5.5 TeV excluded"` keeps the interval interpretation honest (it is NOT a "m > 5.5 TeV universal lower bound" — see `constraint_validity.model_dependence` at `:328-331`). The PDG Live S071KKG row at `:126-144` is correctly carried as a historical cross-check (Aaboud 2018BI, 3.8 TeV, Gamma/m = 15.3%) and is explicitly noted in the TeX prose at `CR001.tex:31-36` as superseded by CMS 2019 (4.55 TeV) and CMS 2026 (5.5 TeV). Historical limits from ATLAS 2012/2013 (1.5/2.07 TeV at 7 TeV) and ATLAS 2018 (3.8 TeV at 13 TeV) are kept in `pdg_or_equivalent.values` for auditability of the LHC reach trajectory; this is consistent with the L001 precedent (carry historical numerical limits in `pdg_or_equivalent.values` if they are measured exclusions, not theory projections).
- Literature anchor: the Lillie-Randall-Wang (LRW) 2007 paper hep-ph/0701166 ("The Bulk RS KK-gluon at the LHC", JHEP 09 (2007) 074) is the canonical theoretical baseline for this signature — it identified that the first KK gluon in bulk RS decays dominantly to ttbar (because the third-generation right-handed top is IR-localized), and it proposed the boosted-top reconstruction strategy. CR001 cites it correctly at `CR001.yaml:229-244` (`paper_era_reference.lillie_randall_wang_2007`) with the discovery-reach context as a theory projection (5 TeV, qualifier "conservative discovery reach estimate under assumed top-tagging background rejection"), explicitly marked `not_promoted_reason: "theory/projection input, not a measured limit"` per the L001 carve-out. The TeX prose at `CR001.tex:54-59` walks through the LRW->ATLAS 2012->ATLAS 2013->CMS 2017->ATLAS 2018->CMS 2019->ATLAS 2020->ATLAS 2025->CMS 2026 history. This is the correct RS interpretation and the experimental trajectory is faithful to the literature.
- RS framing (`CR001.tex:38-52`): "This is the canonical collider signature of bulk RS because the IR-localized top quark couples strongly to the KK gluon" — correct. The cross-check qualifier vs the rc1.1 anarchic-flavor `M_KK^{min,p50} = 47.26 TeV / p95 = 127.13 TeV` from `docs/quark_scan_methodology_note.tex` is documented at `:46-52` and is consistent with the methodology-note numbers reviewed in R05/R08.

**CR007 `pp -> G_KK^(1) -> diboson/diphoton/dilepton` — RS-distinguishing spin-2 graviton**:
- Physics anchor: headline `pdg_or_equivalent.values[0]` (`CR007.yaml:85-106`) is PDG 2025 Extra Dimensions listing, `m(G_KK^(1)) > 4.8 TeV` at 95% CL in the diphoton channel at k/Mbar_Pl = 0.1, primary paper CMS HAYRAPETYAN 24AJ (arXiv:2405.09320, 138 fb^-1). The dilepton companion `:107-128` is `4.78 TeV` (CMS SIRUNYAN 21N, arXiv:2103.02708). Bulk graviton at k/Mbar_Pl = 0.5 is `1.4 TeV` (CMS B2G-20-009, arXiv:2210.00043) and the ATLAS combination at k/Mbar_Pl = 1 is `2.3 TeV` (arXiv:1808.02380). All four values match the PDG 2025 Extra Dimensions listing rows verbatim (`flavor_catalog/audits/factcheck_collider_rs.md:32` — VERIFIED 0 findings). The model-dependence on k/Mbar_Pl is correctly captured at each value's `model_assumptions:` block.
- Literature anchor: Agashe-Davoudiasl-Perez-Soni 2007 hep-ph/0701186 ("Warped Gravitons at the LHC and Beyond") is the canonical bulk-RS-graviton paper that established the suppressed couplings to light fermions and the consequent diboson/diphoton dominance for KK-graviton collider phenomenology. CR007 cites it at `CR007.yaml:67` (source SHA tracked) and in the supporting blocks (per the round-5 sign-off cross-cutting observation §3, this is the "spin-2-specific baseline"). The original Randall-Sundrum 1999 paper hep-ph/9905221 is also tracked at `CR007.yaml:76`. Agashe et al. 0709.0007 (warped bulk gauge) is correctly placed in `auxiliary_theory_inputs` / `paper_era_reference` and explicitly noted as the "related custodial bulk gauge-boson collider context" rather than the spin-2 baseline — a careful and correct distinction.
- RS-distinguishing framing: per round-5 sign-off §3 cross-cutting observation, CR007 is one of 4 entries (with CR002/CR003/CR004 custodial fermion partners) that are uniquely RS-distinguishing. CR007 carries the unique spin-2 gravitational signature; CR002-CR004 carry the protected ZbLbL custodial-fermion-partner pattern. This is the correct RS-phenomenology framing.

**CR009 Drell-Yan high-mass tail / contact-operator EFT — cycle-2 rework verified**:
- Physics anchor: `pdg_or_equivalent` carries the ATLAS full-Run-2 contact-interaction limits (`Lambda_LL^+ > 35.8 TeV`, `LL^- 26.0 TeV`, `RR^+ 35.5 TeV`, `RR^- 26.5 TeV`, `LR^+ 32.5 TeV`, `LR^- 28.8 TeV`) and the CMS independent cross-check (`LL^- 23.9 TeV`, `RR^+ 36.4 TeV`). These match the PDG 2025 Reviews of Quark and Lepton Compositeness verbatim (round-5 sign-off §"CR009" justifies these endpoint values; `factcheck_collider_rs.md` records VERIFIED 0). The TeX body at `CR009.tex:23-32` quotes the ATLAS endpoints digit-for-digit and gives the CMS range `23.8-36.4 TeV` at `:34-40`. The convention `g_contact^2 = 4 pi` is explicitly stated at `:23-25` and again at `:52` ("With the usual contact-interaction convention, 4 pi / Lambda^2 ~ |g_q g_l|/M_V^2").
- Cycle-2 rework: the cycle-1 CA flagged CHK-1 (historical contact-range numerals quoted in prose without being promoted to `pdg_or_equivalent.values`). The cycle-2 WA fix (commit `e1aec33`) followed the T003 precedent and removed the historical numerals from the prose rather than promoting them. The current `CR009.tex:68-83` retains the historical *arxiv IDs* (1112.4462, 1407.2410, 1412.6302, 1607.03669, 1707.02424, 1812.10443, 2006.12946, 2103.02708) but no longer quotes specific Lambda values for the historical 7 TeV and partial Run-2 data points — clean policy alignment. The cycle-2 CA verdict at `ca_w9_ew_tail_v2.md` (commit `82daa9b`) PASS confirmed the prose-only fix preserved all VERIFIED `pdg_or_equivalent.values`.
- RS framing (`CR009.tex:42-66`): "In a 5D RS interpretation, this is primarily a constraint on neutral heavy vector exchange that induces semileptonic operators of the form `(bar q gamma^mu q)(bar ell gamma^mu ell)`. It is not a direct KK-gluon mass limit, since a color-octet KK gluon does not mediate `ell+ ell-` at tree level. It constrains the electroweak KK tower, a `Z'`-like custodial vector, or a more generic compositeness sector only after specifying the light-quark and lepton couplings." Correct — this is exactly the right RS interpretation, and the explicit caveat that the EFT-mass map is `M_V ~ sqrt(|g_q g_l|/(4 pi)) * 35.8 TeV` rather than a universal 35.8 TeV mass bound is the standard textbook treatment.

**Cross-check vs other 11 entries** (shallow, via `factcheck_collider_rs.md` per-entry table and `signoff/round_005_index.md:34-47`): all 14 entries carry PDG-anchored values; literature baselines correctly attributed (Lillie-Randall-Wang for CR001; Agashe-Davoudiasl-Perez-Soni / Agashe et al. for CR005-CR007; Contino-Servant / Mrazek-Wulzer for CR002-CR004; the appropriate ATLAS/CMS arXiv abstracts for the VLQ and EW-tail entries). All entries explicitly frame the LHC reach (few-TeV scale; max 36 TeV for the contact-operator endpoint Lambda which is NOT a resonance mass) as a cross-check or non-anarchic-RS leverage relative to the rc1.1 anarchic-flavor `M_KK^{min,p50} = 47.26 TeV` bound — the round-5 sign-off §3 "honest about leverage" observation. No physics error.

## Check 2 — Code / repo consistency

**Verdict**: PASS-with-concern (three LOW/INFO issues — none block the v0.4 milestone)

**File existence** (`find flavor_catalog/processes/collider_rs -name "*.{tex,yaml}"`): all 28 expected files (CR001-CR014, two each) plus `index.tex` are present. The 14 `flavor_catalog/references/CR0XX/` snapshot directories are present and the snapshots are byte-for-byte preserved (CR001 sha256 check: 12 files declared in `CR001.yaml:source_shas`, all 12 hashes match the on-disk content verbatim — 0 mismatches). `processes/collider_rs/` is the new family directory per orchestrator decision D-2 (`wave_009_runbook.md:67-74`); distinct from `processes/secondary/` (Wave-8 SECONDARY) and from the PRIMARY low-energy families.

**YAML schema consistency** (Python schema check on all 14): every YAML parses cleanly, declares `schema: flavor_catalog.process.v1`, `family: collider_rs`, the correct `process_id`, a non-empty `status_history` with monotone-forward transitions reaching `CHECKER-DONE` then `OPUS-APPROVED` then `FACT-CHECKED`, and the top-level `fact_check_verdict: "VERIFIED"`. No YAML carries a `priority_tier` field — implicit PRIMARY per `PRIORITY_TIERS.md` and orchestrator decision D-1 at `wave_009_runbook.md:60-65`. Status-history depth distribution: 10 entries with 6 transitions (cycle-1 PASS path), 1 entry (CR011) with 7 transitions (cycle-2 CHK-2-only), 3 entries (CR008/CR009/CR010) with 8 transitions (cycle-2 CHK-1 or CHK-1+CHK-2). This exactly matches `wave_009_runbook.md:154-155` ("4 cycle-2 reworks: CR008 CHK-1, CR009 CHK-1, CR010 CHK-1+CHK-2, CR011 CHK-2") and `signoff/round_005_index.md:41-44`.

**Master-tex wiring** (commit `b96036b`): `flavor_catalog/catalog_master.tex:51-61` adds a single top-level `\section{Collider RS Resonances}` containing the new family preamble and `\input{processes/collider_rs/index}`. The new section is placed after the EDM/Neutrino section and before the SECONDARY section per orchestrator decision D-3 (`wave_009_runbook.md:76-83`). `processes/collider_rs/index.tex` lists all 14 `\input{processes/collider_rs/CRXX.tex}` calls behind `\IfFileExists` guards (defensive against missing process files), consistent with the existing per-family PRIMARY `index.tex` pattern.

**v0.4 tag**: `git tag -l | grep v0.4` returns `flavor-catalog-v0.4` (one annotated tag). `git rev-list -n1 flavor-catalog-v0.4` resolves to `2ad34b1` (the master compile v0.4 commit), tagged 2026-05-17. Tag message: "Flavor Catalog v0.4 - 94 PRIMARY (80 flavor + 14 new Wave-9 collider_rs) + 8 SECONDARY (Wave-8). 102 total OPUS-APPROVED. Wave-9 fact-check: 14/14 VERIFIED, 0 mismatches. Catalog totals: 100 VERIFIED + 2 PARTIAL (E009 v0.2 INSPIRE JS-only, K020 metadata cleared at v0.3+) / 0 MISMATCH / 0 FAILED. ..." The v0.4 tag annotation `100V + 2P` is internally inconsistent with the compile report at `master_compile_v04_report.md:55-56` which says `101 VERIFIED + 1 PARTIAL` (treats K020 as cleared at v0.3+, only E009 remains PARTIAL). See R19-I3. The discrepancy is the same E009/K020 "tagged vs current-HEAD" seam reviewed in R18-I1; cosmetic only — no numerical claim is wrong.

**Master compile artifacts**: `flavor_catalog/catalog_master.pdf` builds clean (149→179 pages, 796K→913K bytes per `master_compile_v04_report.md:42-44`) with only layout warnings (998 overfull, 68 underfull hbox, 628 hyperref PDF-string warnings — same warning classes as v0.1/v0.2/v0.3, expected from dense monospace paths and math in section/bookmark strings). Family counts in `master_compile_v04_report.md:11-19` add up correctly: kaon 13+3=16, charm 8+0=8, beauty 22+4=26, top/Higgs/EW 19+1=20, charged-lepton 11+0=11, EDM/neutrino 7+0=7, collider_rs 14+0=14. PRIMARY total = 94 (80+14); SECONDARY total = 8; sum = 102. Matches the v0.4 tag annotation.

**Fact-check audit**: a new family audit `flavor_catalog/audits/factcheck_collider_rs.md` (220 lines, commit `0c5dacc`) is the canonical Wave-9 fact-check; it records 14/14 VERIFIED, 0 mismatches, 0 fetch exceptions, all cited arXiv/PDG/HEPData/CMS URLs returned HTTP 200. Each entry has a `### CR0XX - VERIFIED` per-row block (e.g. CR001 at lines 32+). The `## Fetch exceptions` table at line 28 is empty (None) — the cleanest fact-check pass rate of any wave to date per `wave_009_runbook.md:183`. However, the consolidated `flavor_catalog/audits/factcheck_status.md` was not regenerated at the R19 endpoint to include the 14 new collider_rs rows in its per-process table — same defect as R17-I2 (Wave-7 5 entries missing) and R18-I3 (Wave-8 8 entries missing); current HEAD `factcheck_status.md` does include the Wave-9 collider_rs rows (resolved later in merge prep), but not at the v0.4 tag boundary. See R19-I4.

**SHA-256 source manifest spot-check** (CR001): 12 reference snapshot files declared in `CR001.yaml:source_shas` are present at `flavor_catalog/references/CR001/`; Python `hashlib.sha256` of each file's on-disk content matches the declared hash digit-for-digit (0 mismatches). MERGE_PLAN.md:413 R19 grep-target ("the SHA-256 listed in each `references/CR0XX/sha256sums.txt`") is honored for CR001. The MERGE_PLAN line 413 also mentions `claim_level` and `pdg_or_equivalent` as R19 grep targets; the YAML schema for collider_rs uses `pdg_or_equivalent` (consistent — confirmed present in all 14) but does NOT use `claim_level` (the field name is `limit_type` within `pdg_or_equivalent.values[].limit_type`, e.g. `CR001.yaml:88` "95% CL mass exclusion interval upper edge"). See R19-I5: this is a minor MERGE_PLAN-vs-schema name drift, not a missing-field defect.

**MEG II `br_limit` cross-cutting check** (per MERGE_PLAN §C.2.3 OQ-7): `grep -E "br_limit|1\.5e-13|4\.2e-13|2\.0e-13|MEG II"` over `flavor_catalog/processes/collider_rs/` returns ZERO matches. Correct — collider_rs is the hadron-collider family, not the LFV family; the MEG II constant is L001's concern, not CR's. No drift introduced by Wave-9.

**PRIMARY entries untouched**: spot-check on `processes/{kaon,beauty,charm,top_higgs_ew,charged_lepton,edm_neutrino,secondary}/` shows no Wave-9 commit modifies any earlier PRIMARY or SECONDARY YAML or TeX (`git log --name-only 7ed9117^..864cd6d -- flavor_catalog/processes/ | grep -v "collider_rs"` returns only `processes/secondary/...` paths in the `index.tex` regeneration at commit `2ad34b1` — but inspection of that commit shows the only `processes/collider_rs/index.tex` was actually rewritten with 2 changed lines, no SECONDARY index edits). The PRIORITY_TIERS.md "do not retroactively edit earlier-wave sidecars" contract is honored.

**Commit-message vs SHA cross-check**: `git log --oneline 7ed9117^..864cd6d | wc -l = 34`, but MERGE_PLAN.md §B.1 R19 row + the user prompt list 33 commits. The diff (`/tmp/r19_plan.txt` vs `/tmp/r19_actual.txt`) shows two seams: (a) the prompt/MERGE_PLAN lists `1cf8b57` while the actual SHA is `1cd8b57` (CR005 PKA initial draft, commit-message typo `1cd8b57: flavor-catalog(wave9): PKA-CR005 initial draft`); (b) the actual commit `82daa9b` (CA-v2 ew_tail cycle-2 verdict, commit subject "flavor-catalog(wave9): CA-v2 ew_tail cycle-2 verdict") IS in the Wave-9 chain but is NOT listed in the R19 commit-list in either the prompt or `MERGE_PLAN.md:312`. The prompt explicitly notes "82a96f0 belongs to R03 ONLY and is intentionally NOT listed here" but does not address `82daa9b`. `82daa9b` is referenced elsewhere in MERGE_PLAN.md (line 4, line 68: "the canonical version lives at `82daa9b` on trunk, ... `82daa9b flavor-catalog(wave9): CA-v2 ew_tail cycle-2 verdict`") as the canonical Wave-9 CA-v2 ew_tail commit that supersedes the `backup/ca-v2-ew-tail-*` branch tips. So `82daa9b` belongs to R19 by content and should be added to the R19 row commit list. See R19-I1 and R19-I2.

**Status-history schema consistency**: spot-check on CR009 (cycle-2 deepest history): `CR009.yaml` status-history has 8 transitions: DRAFT (PKA) -> WRITER-INITIATED (PKA) -> WRITER-DONE (WA cycle-1) -> CHECKER-DONE-with-rework (CA cycle-1 CHK-1 flagged) -> WRITER-DONE (WA-v2 cycle-2) -> CHECKER-DONE (CA-v2 cycle-2 PASS) -> OPUS-APPROVED (round-5) -> FACT-CHECKED (VERIFIED). All `from`/`to` arrows are monotone-forward and the `cycle: 1` / `cycle: 2` tags on the WA and CA entries match the worklog files (`wa_w9_ew_tail.md`, `wa_w9_ew_tail_v2.md`, `ca_w9_ew_tail.md`, `ca_w9_ew_tail_v2.md`). The cycle-2 transitions correctly carry the T003-precedent rationale in the `reason:` field.

## Check 3 — Audit/orchestration cross-consistency

**Verdict**: PASS

**Wave-9 runbook close-out** (`flavor_catalog/worklogs/orchestration/wave_009_runbook.md` §6 lines 163-184): documents the wave as COMPLETE with 14 new PRIMARY collider_rs entries, the v0.4 master compile (179 pages), and the `flavor-catalog-v0.4` tag on commit `2ad34b1`. The §6 close-out catalog totals match the master compile report and the round-5 sign-off: 94 PRIMARY + 8 SECONDARY = 102 total OPUS-APPROVED. The §6 V/P count says "100 VERIFIED + 2 PARTIAL (E009 v0.2 + K020 v0.3 — both metadata-only, both accepted per E009 precedent; K020 was actually cleared in the v0.3 cleanup commits but v0.3 tag pins the original PARTIAL state)" — explicitly documents the K020-cleared-at-current-HEAD-but-tagged-as-PARTIAL seam from R18-I1 / R19-I3. Self-consistent take.

**Round-5 Opus sign-off** (`flavor_catalog/signoff/round_005_index.md`, 64 lines): records 14/14 APPROVE / 0 RETURN-TO-WA / 0 ESCALATE-TO-PI. The `## Per-process verdicts` table (lines 32-47) gives one row per process with the terminal CA worklog, fact-check verdict (all VERIFIED 0 findings), the APPROVE verdict, and a brief justification citing the PDG/CMS/ATLAS anchor values. The Round-5 sign-off cites each terminal CA worklog by filename (`ca_w9_kk_gauge.md`, `ca_w9_custodial.md`, `ca_w9_ew_tail.md`, `ca_w9_vlq_4top.md`, `ca_w9_ew_tail_v2.md`, `ca_w9_vlq_4top_v2.md`) — all 6 exist in `worklogs/checker/`. Method §"Method:" at line 12 explicitly documents (i)-(vi) what the sign-off checked: tier-policy + status-history + CA worklog terminal verdict + family fact-check verdict + representative spot-checks (CR001 / CR002 / CR009) + leverage-vs-rc1.1 honesty.

**Cross-cutting observations (round-5 sign-off §"Cross-cutting observations")**: documents three policy-aligned facts: (1) L001/B001_B003/B021_B023 carve-out applied uniformly across the wave (theoretical NLO sigma, RS theory baselines, dataset metadata, HL-LHC/FCC-ee projections, EFT/contact translations, and the local rc1.1 `47.26 TeV / 127.13 TeV` comparison scales live in `paper_era_reference` / `supporting_measurements` / `auxiliary_theory_inputs` / `theory_context`, not in `pdg_or_equivalent`); (2) cycle-2 reworks (CR008/CR009/CR010/CR011) were policy-aligned and required no Opus arbitration; CR008/CR009 CHK-1 followed T003 (delete historical numerals from prose), CR010/CR011 CHK-2 followed B023 (canonical-key rename); (3) leverage-honesty: each entry frames its LHC reach (few-TeV) as a cross-check vs the flavor-implied `M_KK^{min,p50} = 47.26 TeV` rather than the leading constraint. The RS-distinguishing subset is identified correctly: CR002/CR003/CR004 (custodial fermion partners, ZbLbL protected) and CR007 (KK-graviton spin-2). This matches my Check-1 physics intuition spot-checks.

**Iteration cap respected**: cycle-2 reworks at CR008/CR009/CR010/CR011 stayed within the 3-cycle cap per `wave_009_runbook.md:185` ("Iteration caps respected: CR008 / CR009 / CR010 / CR011 used WA/CA cycle 2 inside the 3-cycle cap; the other 10 entries passed cycle 1. No Wave-9 entry exceeded cap"). The cycle-2 verdict files at `ca_w9_*_v2.md` PASS confirms the rework was acceptable on the first re-check.

**Cycle-1 rework rate**: 4/14 = ~29%, in line with Wave-1..6 historical rates (Wave-8 was 1/8 = ~13%, Wave-1 kaon family was 7 PKAs + 1 cycle-1 PASS + 1 cycle-2 = also ~14%). Per `wave_009_runbook.md:181-182`, the slightly higher cycle-1 rework rate on collider entries is attributed to the higher base rate of CHK-1 historical-numeral and CHK-2 citation-key issues — both cycle-2-fixable via T003 + B023 precedents, both observed in Wave-9.

**PI directive compliance**: PI's directive (2026-05-17, `wave_009_runbook.md:16-23`) was "do the custodial constraints", followed by acceptance of the 14-entry expansion to "as many constraints as we can". Wave-9 delivered exactly 14 entries (the proposed scope), with the custodial top-partner trio CR002/CR003/CR004 as the lead sub-class. The new scope class (collider_rs) is explicitly framed as complementary to (not replacing) the low-energy flavor entries — the orchestrator's intended interpretation of the PI directive.

## Issues

- **R19-I1** severity:LOW tag:provenance
  - Title: MERGE_PLAN.md:312 R19 commit list (and the user prompt) list `1cf8b57` as the CR005 PKA SHA; the actual SHA is `1cd8b57`
  - Description: `git log --oneline 7ed9117^..864cd6d` shows `1cd8b57 flavor-catalog(wave9): PKA-CR005 initial draft (collider_rs PRIMARY)` (commit timestamp 2026-05-17 16:48:42 -0400); `git rev-parse --short=7 1cf8b57` returns no such object. The prompt typo / MERGE_PLAN typo is `1cf8b57` (an `f` for a `d`). No content is missing; the CR005 yaml+tex pair is correctly in the tree. Pure SHA-string transcription error in `MERGE_PLAN.md:312` and the user prompt.
  - Evidence: `git log --oneline 7ed9117^..864cd6d | grep CR005`; `git cat-file -t 1cf8b57` returns "fatal: Not a valid object name"; `git cat-file -t 1cd8b57` returns "commit". The `/tmp/r19_actual.txt` vs `/tmp/r19_plan.txt` diff isolates this as the only character-level mismatch in the 33-vs-34 commit list.
  - Recommended fix: edit `MERGE_PLAN.md:312` and the R19 row's commit list to replace `1cf8b57` with `1cd8b57`. No git history change needed.

- **R19-I2** severity:LOW tag:provenance
  - Title: Wave-9 CA-v2 ew_tail cycle-2 verdict commit `82daa9b` is in the Wave-9 chain but is NOT listed in MERGE_PLAN.md:312 R19 commit list (or the user prompt)
  - Description: `git log --oneline 7ed9117^..864cd6d` returns 34 commits, but the MERGE_PLAN.md:312 R19 row and the user prompt list 33 SHAs. The missing SHA is `82daa9b flavor-catalog(wave9): CA-v2 ew_tail cycle-2 verdict` (commit timestamp 2026-05-17 18:13:42 -0400; subject documented in `wave_009_runbook.md:155` "CA-v2 ew_tail commit `82daa9b`"). The prompt explicitly notes `82a96f0` (R03 provenance-doc, NOT a Wave-9 commit) is excluded, but does not address `82daa9b`. MERGE_PLAN.md elsewhere acknowledges `82daa9b` as a Wave-9 commit (`MERGE_PLAN.md:4` "All references to the non-existent SHA `82b35c0` replaced with the verified canonical `82daa9b`"; `MERGE_PLAN.md:68` lists `82daa9b` as the canonical Wave-9 CA-v2 ew_tail commit that supersedes the `backup/ca-v2-ew-tail-*` branch tips). So `82daa9b` belongs to R19 by content; the omission is a documentation seam in `MERGE_PLAN.md:312` and the user prompt only. No content is missing; the CR009/CR011 cycle-2 status-history `CHECKER-DONE` transitions correctly cite `ca_w9_ew_tail_v2.md` and the YAMLs reflect the commit `82daa9b` state.
  - Evidence: `git log --oneline 7ed9117^..864cd6d | wc -l = 34`; `wc -l /tmp/r19_plan.txt /tmp/r19_actual.txt = 33 34`; `diff /tmp/r19_plan.txt /tmp/r19_actual.txt` shows the missing line is `82daa9b`; `git show 82daa9b --format="%s"` returns "flavor-catalog(wave9): CA-v2 ew_tail cycle-2 verdict"; `wave_009_runbook.md:155` references `82daa9b` as the CA-v2 ew_tail commit.
  - Recommended fix: edit `MERGE_PLAN.md:312` and the R19 row to add `82daa9b` to the commit list, bumping the count from "33 commits" to "34 commits" and adjusting the granularity-check note at `MERGE_PLAN.md:317` accordingly ("R19 is now correctly 34 commits"). The "82a96f0 belongs to R03 ONLY" caveat remains correct.

- **R19-I3** severity:LOW tag:docs
  - Title: `flavor-catalog-v0.4` tag annotation says "100V + 2P" but the compile report says "101V + 1P"
  - Description: `git cat-file -p flavor-catalog-v0.4` reads "Catalog totals: 100 VERIFIED + 2 PARTIAL (E009 v0.2 INSPIRE JS-only, K020 metadata cleared at v0.3+) / 0 MISMATCH / 0 FAILED". This is inconsistent with `flavor_catalog/master_compile_v04_report.md:55-56` which reads "Total catalog state after Wave-9: 102/102 OPUS-APPROVED, with 101 VERIFIED and 1 PARTIAL across PRIMARY plus SECONDARY". The wave-9 runbook close-out at `wave_009_runbook.md:172` agrees with the tag annotation (`100V + 2P (E009 + K020 historical)`). The discrepancy is identical in nature to R18-I1: the tag annotation reflects the v0.3-tagged historical state (K020 still PARTIAL as of v0.3 tag), while the compile report reflects the v0.4-tagged current state (K020 cleared post-v0.3, only E009 PARTIAL). Both statements are internally rationalized but the seam is documented in two places with slightly different intent. Cosmetic only — no per-process YAML or `factcheck_*.md` is wrong.
  - Evidence: `git cat-file -p flavor-catalog-v0.4`; `flavor_catalog/master_compile_v04_report.md:55-56`; `flavor_catalog/worklogs/orchestration/wave_009_runbook.md:172`; cross-reference R18-I1.
  - Recommended fix: Optional. The v0.4 tag is immutable on origin. For future master compile tags (v0.5+), source the V/P count from the at-tag-time compile report only (single source of truth) rather than mixing the at-tag and post-tag states in the annotation message.

- **R19-I4** severity:LOW tag:docs
  - Title: Consolidated `factcheck_status.md` was NOT regenerated at the R19 endpoint to include the 14 Wave-9 collider_rs rows (recurrence of R17-I2 / R18-I3 at the new-family tier)
  - Description: At the R19 endpoint (the v0.4 tag commit `2ad34b1` and close-out `864cd6d`), the new family fact-check report `flavor_catalog/audits/factcheck_collider_rs.md` exists with 14/14 VERIFIED, but the consolidated `flavor_catalog/audits/factcheck_status.md` per-process table was not regenerated to include the 14 new collider_rs rows at that revision. The 14 Wave-9 verdicts live exclusively in `factcheck_collider_rs.md` at the v0.4 boundary. Same defect pattern as R17-I2 (v0.2: Wave-7 5 entries missing from consolidated table) and R18-I3 (v0.3: Wave-8 8 entries missing). The current HEAD `factcheck_status.md` has been brought up to date to include Wave-7/8/9 rows (resolved later in the merge prep), so this is a documentation-drift seam that was resolved post-tag, not at the v0.4 boundary. No numerical claim is wrong; the per-family `factcheck_collider_rs.md` is the authoritative Wave-9 audit.
  - Evidence: `git show flavor-catalog-v0.4:flavor_catalog/audits/factcheck_status.md` (does not include CR0XX rows); `flavor_catalog/audits/factcheck_collider_rs.md` (220 lines, 14/14 VERIFIED); `flavor_catalog/master_compile_v04_report.md:53-55` references the per-family `factcheck_*.md` files for SECONDARY and Wave-9 status; cross-reference R17-I2 and R18-I3.
  - Recommended fix: Optional — same recommendation as R17-I2 / R18-I3: fold the per-family Wave-N audit rows back into the consolidated `factcheck_status.md` at every master-compile bump. The current HEAD already does this for Wave-1-through-Wave-9; the recommendation is forward-looking for Wave-10+ (e.g. a `tools/aggregate_factchecks.py` step in the master-compile runbook).

- **R19-I5** severity:INFO tag:docs
  - Title: MERGE_PLAN.md:413 R19 grep-target list mentions `claim_level` field, but the collider_rs schema uses `limit_type` (under `pdg_or_equivalent.values[]`)
  - Description: `MERGE_PLAN.md:413` lists the R19 grep targets as "each `CR0XX` ID, the `claim_level` field, the `pdg_or_equivalent` field, the SHA-256 listed in each `references/CR0XX/sha256sums.txt`". The `pdg_or_equivalent` and SHA-256 targets exist and were spot-checked (CR001 12-file SHA pass, all 14 YAMLs have `pdg_or_equivalent`). However, `claim_level` is not a field in the `flavor_catalog.process.v1` schema as used by collider_rs (or by any prior family I cross-checked). The semantically equivalent field is `limit_type` (e.g. `CR001.yaml:88` `limit_type: "95% CL mass exclusion interval upper edge..."`, `CR007.yaml:91` `limit_type: "lower_limit"`). This is a documentation drift between MERGE_PLAN.md's planner-era field-name guess and the schema's actual field name; not a missing-field defect. The reviewer-mandated check (verify each entry's limit-class field is filled and policy-aligned) was honored by checking `limit_type` instead.
  - Evidence: `grep -l "claim_level" flavor_catalog/processes/collider_rs/*.yaml` returns nothing; `grep -c "limit_type:" flavor_catalog/processes/collider_rs/*.yaml` shows it's used in all 14 YAMLs; `MERGE_PLAN.md:413` lists `claim_level`.
  - Recommended fix: Optional. Edit `MERGE_PLAN.md:413` to replace `claim_level` with `limit_type` for consistency with the schema, or add a one-line note that the schema field is named `limit_type` and the grep target should be that. No content or audit defect.

## Verdict

**APPROVE** — Wave-9 cleanly opens a new scope class (direct-collider RS resonance / heavy-partner searches), executing the standard PKA -> WA -> CA -> fact-check -> Opus pipeline with one minor structural change (new family directory `processes/collider_rs/`). All 14 entries are OPUS-APPROVED (round-5 `signoff/round_005_index.md` 14/14 APPROVE) and VERIFIED (`factcheck_collider_rs.md` 14/14 VERIFIED, 0 mismatches, 0 fetch exceptions — the cleanest fact-check rate of any wave). The three spot-checked entries (CR001 KK-gluon, CR007 KK-graviton, CR009 DY EFT) all have physics anchors matching the cited PDG / CMS / ATLAS literature digit-for-digit, with correct RS interpretation (CR001 LRW boosted-top baseline; CR007 Agashe-Davoudiasl-Perez-Soni spin-2 graviton baseline; CR009 explicit `4 pi / Lambda^2 ~ |g_q g_l|/M_V^2` convention with no universal-mass-bound mis-interpretation). The cycle-2 reworks on CR008/CR009/CR010/CR011 followed the T003 and B023 precedents cleanly and required no Opus arbitration. Master compile v0.4 (179 pages, 913 KB) is tagged `flavor-catalog-v0.4` on commit `2ad34b1`; catalog state at v0.4 is 94 PRIMARY + 8 SECONDARY = 102 OPUS-APPROVED. Five LOW/INFO issues filed (R19-I1 CR005 PKA SHA typo `1cf8b57` -> `1cd8b57` in MERGE_PLAN.md:312 and prompt; R19-I2 commit `82daa9b` CA-v2 ew_tail cycle-2 missing from MERGE_PLAN.md:312 R19 list — present in chain, present in runbook §6, cited at MERGE_PLAN.md:4 + :68; R19-I3 cosmetic V/P count drift in v0.4 tag annotation vs compile report; R19-I4 consolidated `factcheck_status.md` not regenerated at v0.4 boundary, same as R17-I2 / R18-I3; R19-I5 `claim_level` grep target in MERGE_PLAN.md:413 should be `limit_type` per the schema). None blocks the v0.4 milestone. R19 is DONE; ready to proceed to R20 (meta-orchestration docs).
