# Review R02 — Phase-2 hole #4 spurion seed xfail repair

**Reviewer**: Claude Opus 4.7 (single-run reviewer)
**Date**: 2026-05-25
**Commits**: 16e6b36, 2871fce, 559b851, fd83d96, 7e679ea, 1fec2c7
**Files**: `quarkConstraints/benchmarks.py`, `tests/test_quark_fit.py`, `pyproject.toml` (no `pytest.ini`; xfail config went into `pyproject.toml` `[tool.pytest.ini_options]`), `tests/baselines/{pre-fix-xfail-output.txt,post-fix-test-output.txt}`, `docs/phase_logs/phase2_h4_{impl,review,signoff}.md`.

## Commits

- `16e6b36` — test fixture: pre-fix `--runxfail` baseline (`12 passed, 2 failed`, 135-line capture).
- `2871fce` — substantive fix: rewrote `default_spurion_seed()` numerics in `quarkConstraints/benchmarks.py:189-223` and removed the two `@pytest.mark.xfail` markers in `tests/test_quark_fit.py` at the previous lines 144 and 493.
- `559b851` — added `[tool.pytest.ini_options]` with `xfail_strict = true` to `pyproject.toml:70-71`.
- `fd83d96` — captured `14 passed` post-fix baseline.
- `7e679ea`, `1fec2c7` — phase-log seal commits (impl + review + signoff). Docs only.

## Physics

Physics: N/A — test-machinery repair only. Per MERGE_PLAN §B.1 checks column: `numerics, code`.

## Numerics

**Verdict**: PASS.

**Tests run** (wall time well under the 10 min PARTIAL threshold):
- `pytest tests/ -k 'spurion' --tb=short` → `1 passed, 543 deselected in 7.01s` (only one keyword hit, `test_quark_model.py`).
- `pytest tests/test_quark_fit.py --tb=short` → `14 passed in 3.06s` (the two formerly-xfail tests now pass cleanly).
- `pytest tests/test_quark_fit.py::test_fit_quark_sector_is_invariant_under_quotient_directions tests/test_quark_fit.py::test_fit_orientation_false_remains_deterministic_and_restricted -v` → both `PASSED`.
- `pytest tests/test_quark_fit.py tests/test_quark_deltaf2.py tests/test_quark_couplings.py tests/test_modern_scan.py tests/test_epsilon_k_physics.py -q` (all downstream consumers of `default_spurion_seed`) → `51 passed in 27.29s`.

**Strict-xfail confirmation**: `pyproject.toml:70-71` carries `[tool.pytest.ini_options]\nxfail_strict = true`. The note in the unit description references `pytest.ini`; the actual landing surface is `pyproject.toml`. There is no `pytest.ini` or `conftest.py` in the repo (R02-I1, INFO). A `grep -rn xfail tests/ pyproject.toml` returns only that single config line — zero stale `@pytest.mark.xfail` markers remain anywhere in `tests/`, so strict mode cannot trip on a stale survivor.

**Spurion seed re-derivation against PDG-2024**:
- `quarkConstraints/pdg_quark_masses.py` `PDG_2024_QUARK_MASSES` carries the PDG-2024 reference values: u=2.16(49) MeV @ 2 GeV, d=4.7(4.3) MeV @ 2 GeV, s=93.5(0.8) MeV @ 2 GeV, c=1.273(4.6e-3) GeV @ m_c, b=4.183(7e-3) GeV @ m_b, t=162.5(0.7) GeV @ m_t. All values match PDG 2024 §"Quark Masses".
- `pdg_up_down_arrays_at_scale(163.5)` (the fit scale, `qcd.constants.M_TOP_MS`) yields `up=[1.18954318e-03, 6.02285951e-01, 1.62422747e+02]` and `down=[2.58835785e-03, 5.14917998e-02, 2.73198368e+00]` GeV — bit-for-bit identical to the impl log values in `docs/phase_logs/phase2_h4_impl.md`.
- Live cross-check: running `fit_quark_sector(default_quark_targets(), seed=default_spurion_seed(), max_nfev=2000)` reproduces `initial_score = 5.62e-3` and `result.score = 1.13e-9`; `result.masses_up` and `result.masses_down` match the PDG-target arrays to all 8 displayed digits. The seed sits at a near-perfect PDG-target minimum, exactly as claimed in the impl log.

**Structural sanity of the new seed** (`quarkConstraints/benchmarks.py:189-223`):
- `overall_scale = 2.8` was preserved verbatim (impl log path A: divide fitted spectra by 2.8).
- `up_right` and `down_right` collapsed to default identity rotations — consistent with the fit converging in the MFV fundamental domain where only the left-rotation angles carry physical CKM information.
- `up_left` and `down_left` angles include components near π (e.g. `down_left.theta12 = 3.115`, `up_left.theta13 = -2.838`) — these are 2π-wrap equivalents reachable by quotient-equivalent seeds, consistent with the canonical-vector test now using a tight 5e-6 tolerance and passing.

## Code

**Verdict**: PASS.

**Symbols checked**:
- `git grep -l "default_spurion_seed"` returns 16 hits across `quarkConstraints/{__init__,benchmarks,fit,modern/scan,proxies,scan,validation}.py`, three `scripts/`, and 6 `tests/test_*.py`. All callers use the function form, not literal numerics — no caller depended on the legacy `[0.003, 0.12, 1.0]` / `[0.006, 0.035, 0.45]` array literals.
- The 6 test-file consumers (`test_epsilon_k_physics`, `test_modern_scan`, `test_quark_couplings`, `test_quark_deltaf2`, `test_quark_fit`, plus `test_quark_model` via the spurion keyword) all pass under the new seed — verified above (51 passed across the consumer subset).
- No symbol renames or signature changes — the diff in `quarkConstraints/benchmarks.py` only edits literal field values inside `default_spurion_seed()`.

**xfail-marker correspondence**: The two removed markers (formerly `tests/test_quark_fit.py:144` and `:493`, on `test_fit_quark_sector_is_invariant_under_quotient_directions` and `test_fit_orientation_false_remains_deterministic_and_restricted`) now both pass standalone — verified by direct invocation. The reasons given in the removed `xfail` blocks (legacy seed lands quotient-shifted starts in distinct local minima under PDG targets) match the diagnosis in `docs/phase_logs/phase2_h4_impl.md` and are resolved by the re-derived seed.

**Strict-xfail blast radius**: With zero remaining `@pytest.mark.xfail` markers in `tests/`, enabling `xfail_strict = true` is a forward-looking guard. It cannot break any existing test in this state. It will, however, surface any *future* xfail that gets added without `strict=False` and starts passing — that is the desired behavior per CLAUDE.md test-discipline norms.

**Pre/post baselines**: `tests/baselines/pre-fix-xfail-output.txt` (135 lines) shows the exact assertion failures: max canonical-vector mismatch 0.0157 (vs. 5e-6 tolerance) and a 93.3 GeV top-mass split between quotient-equivalent fits, both consistent with "distinct local minima around the legacy seed under PDG targets". `tests/baselines/post-fix-test-output.txt` shows the clean `14 passed in 3.46s` matching today's `14 passed in 3.06s`. Baselines are coherent.

## Issues filed

| ID     | Severity | Description |
|--------|----------|-------------|
| R02-I1 | INFO     | The MERGE_PLAN §B.1 row points at `pytest.ini` but the strict-xfail block actually landed in `pyproject.toml` `[tool.pytest.ini_options]`. Either is canonical for pytest; just an inventory mismatch worth noting in the merge plan. |
| R02-I2 | INFO     | The impl-log narrative is excellent but the re-derived seed values themselves are quasi-magic numbers in `quarkConstraints/benchmarks.py:189-223`. Consider adding a one-line in-source comment pointing to `docs/phase_logs/phase2_h4_impl.md` so future readers know they were computed by a deterministic LSQ from `default_quark_targets()` (path A) and are not hand-tuned. |

## Verdict

**Overall**: APPROVE.
