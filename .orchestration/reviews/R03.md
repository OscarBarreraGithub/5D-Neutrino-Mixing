# Review R03 — Phase-2 hole #5 bag-parameter audit (FLAG 2024 + BGS 2020)

**Reviewer**: Claude Opus 4.7 (single-run reviewer)
**Date**: 2026-05-25
**Commits**: `dc9c498`, `82a96f0`, `695f35e`, `2521f6b`
**Files**: `quarkConstraints/deltaf2.py`, `tests/test_quark_deltaf2.py`, `docs/audits/bag_param_inventory.md`, `docs/audits/epsilon_k_sm_decision.md`, `docs/quark_scan_methodology_note.{tex,pdf}`, `docs/phase_logs/phase2_h5_{impl,review,signoff}.md`, `.gitignore`.

## Commits

- `dc9c498` — physics: replace `B_1_K=0.717 -> 0.5503`, `B_4_K=0.78 -> 0.903`, `B_5_K=0.57 -> 0.691`, `EPSILON_K_SM=1.81e-3 -> 2.161e-3` in `quarkConstraints/deltaf2.py:618-623`; add `test_audited_deltaf2_hadronic_constants_match_selected_sources` in `tests/test_quark_deltaf2.py:111-117`.
- `82a96f0` — docs/provenance: new `docs/audits/bag_param_inventory.md` (86 lines, 36-row table) + appendix "Input Audits / Hadronic input provenance" in `docs/quark_scan_methodology_note.tex` + rebuilt PDF (16 pp).
- `695f35e` — docs: `docs/phase_logs/phase2_h5_impl.md` (73 lines) seal of the implementation log.
- `2521f6b` — docs+meta: adds `docs/audits/epsilon_k_sm_decision.md`, `phase2_h5_review.md`, `phase2_h5_signoff.md`; `.gitignore` adds two `!docs/audits/` allow-lines (verified ordering with `git ls-files docs/audits/` returning 10 tracked files).

## Physics

**Verdict**: APPROVE.

**Source verification (spot-checks)**:
- `B_K(MS-bar, 2 GeV) = 0.5503(66)` — matches FLAG 2024 (arXiv:2411.04268) N_f=2+1 kaon SM bag average. The pre-audit `0.717` matched the RGI `hat B_K ~ 0.7625` (after small scheme normalization) being mis-used as an MS-bar(2 GeV) input. This was a real scheme-confusion bug, correctly fixed (`quarkConstraints/deltaf2.py:618`).
- `B_4^K = 0.903(14)` (MS-bar at 3 GeV) — matches FLAG 2024 N_f=2+1 BSM kaon average (`bag_param_inventory.md:24`).
- `B_5^K = 0.691(14)` (MS-bar at 3 GeV) — matches FLAG 2024 N_f=2+1 BSM kaon average (`bag_param_inventory.md:25`).
- `EPSILON_K_SM = 2.161e-3` — matches Brod-Gorbahn-Stamou 2020 (arXiv:1911.06822) Eq. (20) central value `2.16(6)(8)(15) x 10^-3`. Lands in the SM ballpark ~(1.9-2.2) x 10^-3 (verified `1.5e-3 < EPSILON_K_SM < 2.5e-3`).

**"BGS 2020" identification clarified**: The MERGE_PLAN unit description is ambiguous ("Buras-Girrbach-Slominski or Bagger-Girrbach style"). The audit doc unambiguously identifies BGS as Brod-Gorbahn-Stamou 2020, arXiv:1911.06822 (`bag_param_inventory.md:28`, `phase2_h5_impl.md:30`). This is the standard reference for the SM `epsilon_K` central — not a basis convention. The "Bagger-Girrbach" naming in the prompt is a misattribution; no Wilson-basis convention change was made by this unit.

**Provenance documentation (`82a96f0`)**: `docs/audits/bag_param_inventory.md` is clear and complete — 36 rows, Green/Yellow/Red traffic-light classification with explicit drift percentages, every row cites a year + eprint/page. The methodology-note appendix (`docs/quark_scan_methodology_note.tex` appendix `app:hadronic-input-provenance`) summarises the red/yellow rows in a one-page table with hyperlinked arXiv refs. The decision file `docs/audits/epsilon_k_sm_decision.md` openly adjudicates the BGS-vs-CKMfitter/UTfit choice and prescribes a band quote for the final paper (see R03-I3 follow-up).

**Honest acknowledgement of band uncertainty**: BGS total uncertainty is 0.18e-3, larger than the central NP gap of 6.7e-5; the signoff (`phase2_h5_signoff.md:87-102`) correctly defers single-number M_KK bounds in favour of a band quote — this is good physics hygiene.

## Numerics

**Verdict**: PASS.

**Tests run** (well under the 10 min PARTIAL threshold):

`timeout 600 pytest tests/ -k 'bag or kaon or epsilon_k or deltaf2' --tb=short` -> `107 passed, 1 skipped, 436 deselected in 76.41s`. Suites exercised: `test_epsilon_k_physics`, `test_modern_phenomenology`, `test_modern_point_artifacts`, `test_modern_scan`, `test_paper_artifact_contracts`, `test_paper_benchmarks`, `test_paper_hadronic_inputs`, `test_paper_hadronic_lr_inputs`, `test_paper_mode_contracts`, `test_paper_observables`, `test_qcd_running`, `test_quark_deltaf2`.

**Targeted assertion** in `tests/test_quark_deltaf2.py:111-117` pins each of the four audited constants against the canonical source to `rtol=1e-3`. Passes.

**Spot-check downstream `epsilon_K` arithmetic** (direct Python evaluation):
- `EPSILON_K_EXP = 2.228e-3`, `EPSILON_K_SM = 2.161e-3` -> central NP budget `|exp - SM| = 6.7e-5`, matching the impl log and inventory.
- `EPSILON_K_SM = 2.161e-3` lands in the right SM ballpark (~2.2e-3).
- Pre-audit -> post-audit budget ratio `4.18e-4 / 6.7e-5 = 6.239`, matching the `6.24x` factor independently quoted in `phase2_h5_impl.md:69` and `bag_param_inventory.md:83`. INVALIDATION_GATE_TRIPPED is correctly recorded.

## Code

**Verdict**: APPROVE-WITH-FINDING.

**Symbols / callers checked**:
- `git grep` for `B_1_K|B_4_K|B_5_K|EPSILON_K_SM|EPSILON_K_EXP` finds all in-scope updates correctly propagated through `quarkConstraints/deltaf2.py` (definitions at 618-623; used at 687-689 in `_kaon_matrix_elements` and at 743 for the NP-budget denominator).
- Test consumers (`tests/test_epsilon_k_physics.py:19-24,302,309,317,446`) import the constants symbolically — no hard-coded numeric duplicates downstream of the test path.

**Finding — duplicate hadronic constants in the modern backend (R03-I1, HIGH)**:
`quarkConstraints/modern/phenomenology.py:421-431` carries a private copy of the kaon hadronic constants (`_KAON_B_1 = 0.717`, `_KAON_B_4 = 0.78`, `_KAON_B_5 = 0.57`, `_KAON_EPSILON_K_SM = 1.81e-3`) that were **NOT** updated by `dc9c498`. These literals are used by `_kaon_m12_np_from_bridge_match` (line 434), `_evaluate_epsilon_k_from_bridge` (line 458), and `_evaluate_delta_mk_from_bridge` (~line 500), which feed the modern pipeline at line 1323 in the `epsilon_K` path. The impl log (`phase2_h5_impl.md:71`) explicitly notes this duplicate was left out of scope — but the modern backend is live code (called by `build_modern_point_phenomenology_artifact`, imported by `scripts/export_collaborator_5tev_points.py:40`). Two independent kaon `epsilon_K` evaluators now disagree by the same 6.24x budget factor that the audit deliberately introduced; any artifact produced through the modern path is on stale inputs.

**Test test_audited_deltaf2_hadronic_constants_match_selected_sources is single-source**: it only pins `quarkConstraints.deltaf2.*` and does not guard against the modern-backend duplicate. See R03-I2.

**Provenance cross-references resolve**: there is no top-level `REFERENCES.md` in the repo (closest indices live under `flavor_catalog/references/`). The audit instead cites arXiv numbers + (FLAG 2024 / PDG 2024 / HPQCD 2019 / ETM 2015 / BGS 2020 / Buras-Guadagnoli-Isidori 2010) inline in both `bag_param_inventory.md:16-53` and the methodology-note appendix. All cited eprints are real and externally locatable. The follow-up note about lacking a centralised REFERENCES.md is filed as INFO (R03-I4).

**Methodology-note text consistency**: `docs/quark_scan_methodology_note.tex` was updated by `82a96f0` to quote `|epsilon_K^exp - epsilon_K^SM| ~ 6.7e-5` (matching the audited constants) and to add the hadronic-provenance appendix. The PDF was rebuilt and ships in the same commit.

**`.gitignore` change (`2521f6b`)**: two new lines `!docs/audits/` and `!docs/audits/**` are correctly placed after the `docs/*` deny block and before other unrelated allow-lines; `git ls-files docs/audits/` shows all 10 audit docs are now tracked.

## Issues filed

| ID     | Severity | Description |
|--------|----------|-------------|
| R03-I1 | HIGH     | `quarkConstraints/modern/phenomenology.py:421-431` carries a duplicate copy of `B_1_K`, `B_4_K`, `B_5_K`, `EPSILON_K_SM` still at pre-audit values, and is used by the live modern pipeline (`build_modern_point_phenomenology_artifact` -> `_evaluate_epsilon_k_from_bridge` at line 1323). Artifacts produced through the modern path therefore evaluate `epsilon_K` against a stale (looser by 6.24x) NP budget. The impl log declared this out-of-scope, but it is materially load-bearing for the collaborator export scripts. |
| R03-I2 | MEDIUM   | `tests/test_quark_deltaf2.py::test_audited_deltaf2_hadronic_constants_match_selected_sources` only pins constants in `quarkConstraints.deltaf2`. It does not guard the modern backend duplicates. After R03-I1 is fixed, this test (or a parallel test) should additionally assert that `quarkConstraints.modern.phenomenology._KAON_B_1`, `_KAON_B_4`, `_KAON_B_5`, `_KAON_EPSILON_K_SM` match the audited values, OR the modern backend should import them from `deltaf2`. |
| R03-I3 | MEDIUM   | The Opus signoff (`phase2_h5_signoff.md:104-136`) lists five follow-up tasks (methodology-note band quote, RUNA re-runs at three budget edges, sensitivity-band figure, etc.) that remain open and gate the final-claim invalidation gate. None of the four R03 commits closes them; they correctly depend on hole #6. Track in a separate review unit / issue so they are not forgotten. |
| R03-I4 | INFO     | The prompt expects audit cross-refs to point to a `REFERENCES.md`. The repo has no top-level REFERENCES.md; provenance is encoded as inline arXiv citations in `docs/audits/bag_param_inventory.md` and the methodology-note appendix. All cited eprints (2411.04268, 1911.06822, 1907.01025, 1505.06639, 1002.3612) are real and externally resolvable. Either create a consolidated REFERENCES.md or accept the inline-citation convention. |

## Verdict

**Overall**: APPROVE-WITH-CONCERN.

- Physics: APPROVE. Source values verified against FLAG 2024 and BGS 2020 (Brod-Gorbahn-Stamou 2020); the audit correctly identifies and fixes a real RGI-vs-MS-bar scheme bug in `B_K`; the BGS-2020 vs UTfit/CKMfitter choice is properly adjudicated with a band-quote prescription.
- Numerics: PASS. 107 targeted tests pass in 76s; downstream NP-budget arithmetic checks out.
- Code: APPROVE-WITH-FINDING. The R03 commit set itself is internally consistent and minimal, but `quarkConstraints/modern/phenomenology.py:421-431` shadows the audited constants with pre-audit values and is still live code (R03-I1, HIGH). This is technically out-of-scope per the impl log's own admission, but it makes the audit's invalidation gate only partially effective and should be tracked as a HIGH-severity follow-up in this review unit rather than silently inherited.
