% Review R17 — Catalog master compile v0.2 + audit/factcheck consolidation (75 -> 80 processes)

**Reviewer**: Claude Opus 4.7 (single-run reviewer)
**Date**: 2026-05-25
**Commits** (15, per MERGE_PLAN.md §B.1):
- Master compile v0.1 -> v0.2 (2): `9f65578` (v0.1 master compile, 75 OPUS-APPROVED), `835cf48` (v0.2 master compile, 80 OPUS-APPROVED after Wave-7).
- Opus round-2 sign-off (1): `aa8e3e8`.
- Factcheck scaffold (1): `a4245e2` (initialize `audits/factcheck_status.md`).
- Per-family factcheck deposits (6): `9fb5763` edm_neutrino, `a04a101` kaon, `7f79bab` charged_lepton, `0b07505` top_higgs_ew, `04b1ab1` charm, `994e346` beauty.
- T020 ATLAS h -> e mu digit fix (1): `6498fad` (`6.1e-5` -> `6.2e-5` observed, `5.9e-5` expected).
- Factcheck consolidation (1): `42ac647` (75 rows; 74 VERIFIED + 1 PARTIAL).
- External research import + review (3): `022a20c` (PDF + txt imports), `8fb5f91` (deepresearch_may16 comparison), `2c00d84` (deepresearch_may15 comparison).

**Files**: `flavor_catalog/catalog_master.tex`, `flavor_catalog/catalog_master.pdf`, `flavor_catalog/master_compile_v02_report.md`, `flavor_catalog/audits/factcheck_status.md`, six per-family factcheck reports under `flavor_catalog/audits/factcheck_<family>.md`, six per-family TOC stubs under `flavor_catalog/processes/<family>/index.tex`, four imported artifacts under `flavor_catalog/external_research/` (two PDFs + two text extracts) plus the two `_review.md` cross-comparison memos.

**Sample selection** (per MERGE_PLAN §C.2.1 family-coverage rule for R17 audit cross-check). Two spot-checks anchored on the prompt request:
- **K001** `epsilon_K` (kaon, VERIFIED anchor) - confirm the audit row matches the YAML sidecar.
- **E009** `Weinberg three-gluon operator` (edm_neutrino, the single PARTIAL row) - confirm the PARTIAL reason is documented in both the audit table and the YAML.

Additional shallow coverage of the master-compile structure (catalog_master.tex syntax + family index inputs) and external-research provenance (PDFs + text extracts + line-cited review memos).

## Summary

R17 closes the v0.1 -> v0.2 milestone. Two distinct workstreams ran in parallel on 2026-05-16: (i) the master compile, which built `catalog_master.tex` twice - first against the 75 OPUS-APPROVED PRIMARY processes that DA-4 had converged (`9f65578`), then expanded to 80 by absorbing the five Wave-7 promotions T003/T004/T008/T012/B012 (`835cf48`); and (ii) the family-by-family live-source fact-check, in which six codex-CLI sub-agents (`factcheck-codex-<family>`) walked the per-process source manifest, re-fetched every cited PDG / HFLAV / FLAG / experiment-collaboration URL, and verified the claimed numerical values appear at the cited source. The fact-check exposed exactly one value-bearing mismatch (T020 ATLAS h -> e mu observed limit was carried as `< 6.1 x 10^-5`; the live ATLAS/PDG sources give `< 6.2 x 10^-5` observed and `< 5.9 x 10^-5` expected). The mismatch was patched in commit `6498fad` and the T020 row in `factcheck_status.md` was rewritten to VERIFIED. In parallel, two GPT Deep Research PDFs were imported under `external_research/` and two line-cited comparison memos (`deepresearch_may15_review.md`, `deepresearch_may16_review.md`) flagged eight missing processes (most notably T003/T004/T008/T012/B012, which were independently promoted via the Wave-7 R16 pipeline). The R17 commit set therefore corresponds to the v0.1 -> v0.2 transition with all open issues either closed (T020) or documented (E009 INSPIRE JS-only render).

## Check 1 - Physics intuition / audit cross-check

**Verdict**: PASS

**Audit table cross-check** (at the R17 endpoint commit `42ac647`, before later Wave-8/9 expansion):

`flavor_catalog/audits/factcheck_status.md:32-108` (R17 endpoint) contains 75 process rows. Status counts (verified by `awk -F'|' '$2 ~ /^ [A-Z]+[0-9]+ $/ {print $4}' | sort | uniq -c`):
- **VERIFIED**: 74
- **PARTIAL**: 1 (E009 only)
- **MISMATCH**: 0
- **FAILED**: 0

This matches the merge-plan prompt's "73-75 VERIFIED" range. Family-by-family totals at R17 endpoint (`flavor_catalog/audits/factcheck_status.md:124-131`): kaon 13/13, charm 8/8, beauty 21/21, top_higgs_ew 15/15, charged_lepton 11/11, edm_neutrino 6/7 (E009 PARTIAL). The single PARTIAL row has a clear, documented reason that does not invalidate any numerical claim.

**E009 PARTIAL reason verified end-to-end**:
- Audit table row (`flavor_catalog/audits/factcheck_status.md:106` at R17, `:106` in current file): "All value-bearing neutron EDM and Weinberg-operator claims verified; INSPIRE Weinberg 1989 key-reference URL rendered JS-only, cross-checked via APS and local snapshot."
- Sidecar YAML (`flavor_catalog/processes/edm_neutrino/E009.yaml:83`): "Neutron anchor and Weinberg-operator benchmark values verified; INSPIRE Weinberg 1989 URL rendered JS-only, with metadata cross-checked via APS/local snapshot."
- Status history at `flavor_catalog/processes/edm_neutrino/E009.yaml:81` records `fact_check_verdict: PARTIAL` with `factcheck-codex-edm_neutrino` as the verifier.
- Local snapshot at `flavor_catalog/references/E009/weinberg1989_inspire_metadata.txt` (sha256 `d50b6648cc0077f8ffb802a917a1247a4fb8f17174afa2f94cf9edd7f486b150` recorded at `E009.yaml:98`) is the canonical source of record.
This is the same R15-I2 issue (cosmetic verdict-label artifact for JS-only INSPIRE rendering). The numerical PDG / Abel 2020 / Pospelov-Ritz / Haisch-Hala / Bhattacharya claims are all VERIFIED in the family report `audits/factcheck_edm_neutrino.md`. No new issue; cross-references R15-I2.

**K001 VERIFIED spot-check verified end-to-end**:
- Audit table row (`flavor_catalog/audits/factcheck_status.md:32` at R17): "PDG epsilon_K, BGS SM prediction, and FLAG bag values match."
- Sidecar YAML (`flavor_catalog/processes/kaon/K001.yaml:69-106`): PDG 2026 `|epsilon_K| = (2.228 +/- 0.011) x 10^-3` headline at `:73-79`; Brod-Gorbahn-Stamou SM prediction `2.16(18) x 10^-3` at `:84-94`; FLAG 2024 N_f = 2+1 epsilon_K theory inputs at `:99-106`. All three sources are placed in the canonical schema slots per the L001/B001/B003 precedent (PDG and BGS under `pdg_or_equivalent`-eligible blocks; FLAG under `auxiliary_theory_inputs`; CFW 0804.1954 RS context under `paper_era_reference` at `:111-114`).
- Status history at `K001.yaml:54-55` records `fact_check_verdict: VERIFIED` with the verifier note "Live source fetch and local snapshot checks verified all value claims for this process."
- Code coverage `YES` at `:119` is consistent with `quarkConstraints/deltaf2.py:211-212` and `quarkConstraints/modern/phenomenology.py:23/:431/:1320` (cited in the YAML's `existing_quarkConstraints_loop_evidence` block at `:132-149`).

**T020 mismatch resolution verified**:
- The fix commit `6498fad` modified both the sidecar (`flavor_catalog/processes/top_higgs_ew/T020.tex` / `.yaml`) and the audit table row.
- The current `factcheck_status.md:85` (and at R17 endpoint) reads "factcheck-codex-top_higgs_ew + factcheck-followup | Family report found an ATLAS h -> e mu 6.1e-5 mismatch; follow-up fixed the catalog to 6.2e-5 observed and 5.9e-5 expected, so current status is VERIFIED." This is the only digit-level mismatch the entire R17 fact-check pass caught, and it was patched in-cycle.

**External research import physics-relevance**:
- `external_research/deepresearch_may15_review.md:1-22` and `deepresearch_may16_review.md:1-20` each open with a Section 1 "Processes / observables we are MISSING" enumerating the same 5-8 candidates: T003/T004 (top photon FCNC), T008 (`t -> uH`), T012 (Z-pole charm), B012 (`B -> K* gamma` photon polarization), LFV kaon tail (K019-K021), LFV B (B036/B037), semileptonic asymmetries / `|q/p|_{d,s}`, rare-charm angular modes, tau/W universality ratios.
- Five of these (T003, T004, T008, T012, B012) were independently promoted via the Wave-7 R16 pipeline before the R17 master compile expanded from 75 to 80. The external research thus closes-the-loop on the Wave-7 promotion rationale rather than introducing new constraints. The remaining missing rows (K019-K021, B036/B037, neutral four-body charm, tau universality) are subsequently captured under the Wave-8 SECONDARY tier (R18) and DA-4 deferred scope.

**No physics misstatement.** The audit cross-check confirms 74/75 VERIFIED at R17 endpoint (within the prompt's "73-75 VERIFIED" range), the 1 PARTIAL row has a documented JS-rendering-only reason that does not invalidate any numerical claim, and the external research memos line-cite their RS-physics judgments against catalog files.

## Check 2 - Code consistency

**Verdict**: PASS

**Commands run**:
- `git log --oneline 835cf48^..2c00d84` and per-commit `git log -1 --format='%h %ad %s' --date=short` on each of the 15 R17 commits.
- `git show --stat` on representative commits `835cf48`, `42ac647`, `022a20c`, `8fb5f91`, `2c00d84`.
- `ls flavor_catalog/catalog_master.tex audits/ external_research/` to confirm presence of all three R17 surfaces.
- `file flavor_catalog/catalog_master.pdf` to confirm the compiled PDF exists (`PDF document, version 1.5`, 733862 bytes per `master_compile_v02_report.md:36`).
- `grep -nE '\\\\(begin|end|input|usepackage|documentclass|section|subsection)' flavor_catalog/catalog_master.tex` for structural LaTeX sanity.
- `git show 42ac647:flavor_catalog/audits/factcheck_status.md | awk '/^\| [A-Z]+[0-9]+/' | awk -F'|' '{print $4}' | sort | uniq -c` for the R17-endpoint status tally.

**Findings**:

- **Commit set self-consistent**. All 15 commits land on 2026-05-16 in the published order (factcheck scaffold `a4245e2` 17:32:56 < family deposits `9fb5763`/`a04a101`/`7f79bab`/`0b07505`/`04b1ab1`/`994e346` interleaved < T020 fix `6498fad` < consolidation `42ac647` < import `022a20c` 18:05:39 < two review memos `8fb5f91` 18:13 / `2c00d84` 18:15). The two master-compile commits (`9f65578`, `835cf48`) bracket the family-deposit batch and the Opus round-2 sign-off (`aa8e3e8`). No out-of-order commits.

- **`catalog_master.tex` syntax sound at R17**. `git show 835cf48:flavor_catalog/catalog_master.tex` lines 1-52 show a flat `\documentclass[11pt]{article}` -> `\usepackage{...}` (geometry, parskip, amsmath/amssymb, booktabs, array, xcolor, hyperref, longtable) -> `\input@path` extension to `{./}{flavor_catalog/}` -> `\input{latex/macros}` -> `\begin{document}` -> 6 `\section{...}` blocks each followed by `\input{processes/<family>/index}` -> `\end{document}`. No mismatched braces, no missing `\end{...}`, no unused environment. The 6 family indexes (`processes/<family>/index.tex`) each contain N `\IfFileExists{...}{\input{...}}{\typeout{Missing ...}}` lines whose `<ID>.tex` targets all exist on disk (verified by `ls flavor_catalog/processes/<family>/`). The current file (post-R18/R19) adds collider_rs and a SECONDARY section, but those modifications are out-of-scope for R17.

- **Compiled PDF exists**. `flavor_catalog/catalog_master.pdf` is present (`PDF document, version 1.5`); `master_compile_v02_report.md:36-38` records "Page count: 133, PDF size reported by pdfinfo: 733862 bytes, LaTeX notes: build completed with layout warnings from long monospace references and hyperref PDF-string warnings, with no process inputs skipped." The build command at `:25-33` is reproducible (`pdflatex -interaction=nonstopmode catalog_master.tex` x2).

- **Factcheck table well-formed**. At R17 endpoint (`42ac647`), the table contains 75 rows under a stable schema (`| process_id | family | status | date | fact-checker | mismatches/notes |`). 74 rows match `VERIFIED`, 1 row matches `PARTIAL` (E009). The family-totals summary at `:124-131` is internally consistent: 13+8+21+15+11+(6+1)=75. The Recommendation block at `:167-170` ("ready to tag flavor-catalog-v0.1") is consistent with the v0.1 milestone scope (the v0.2 expansion that adds the 5 Wave-7 entries is tracked separately in `master_compile_v02_report.md`).

- **Per-family factcheck reports exist for all 6 families**: `audits/factcheck_kaon.md`, `factcheck_charm.md`, `factcheck_beauty.md`, `factcheck_top_higgs_ew.md`, `factcheck_charged_lepton.md`, `factcheck_edm_neutrino.md` (current state). Each is the detailed per-process verifier output that the consolidated `factcheck_status.md` summarizes.

- **External research provenance** (acceptable with caveat). The four imported artifacts and two review memos all sit in `flavor_catalog/external_research/`:
  - `deepresearch_may15.pdf` (203806 bytes), `deepresearch_may15.txt` (22009 bytes), `deepresearch_may15_review.md` (13417 bytes)
  - `deepresearch_may16.pdf` (343307 bytes), `deepresearch_may16.txt` (33443 bytes), `deepresearch_may16_review.md` (15821 bytes)
  The review memos are *exhaustively* line-cited against both the imported `.txt` extracts (e.g. `deepresearch_may15.txt:691-722`, `:897-901`, `:832-838`) and the catalog files (e.g. `flavor_catalog/processes/beauty/B011.tex:13-19`, `flavor_catalog/audits/factcheck_status.md:74-79`). Cross-references appear in `flavor_catalog/signoff/round_003_index.md:10` as the Wave-7 source. **No README / MANIFEST / sha256 file** explicitly catalogues the PDFs as binary artifacts; they are simply checked in as `.pdf` + paired `.txt` extracts. See R17-I1 below.

- **Documentation drift between v0.2 master compile report and consolidated factcheck table**. `flavor_catalog/master_compile_v02_report.md:48` claims "79 VERIFIED, 1 PARTIAL" across 80 processes, but the consolidated `factcheck_status.md` at the R17 endpoint only documents 75 rows (the original DA-4-converged set). The 5 Wave-7 additions (T003/T004/T008/T012/B012) were independently verified inside `audits/factcheck_top_higgs_ew.md:294-352` (Wave-7 follow-up addendum lists T003/T004/T008/T012 each as VERIFIED 0/0) and `audits/factcheck_beauty.md` (B012 VERIFIED row, per commit `ca2beba` in R16), but those Wave-7 verdicts are *not folded into* the consolidated `factcheck_status.md` table at the R17 endpoint. The merge-plan §B.1 row R17 description "73-75 VERIFIED" matches the 75-row table (74/75 + 1 PARTIAL); the master-compile report's "79 VERIFIED of 80" requires reading both the consolidated table AND the Wave-7 addenda. The current `factcheck_status.md` (post-R19) does fold in all later waves and shows 88/89 VERIFIED. See R17-I2.

- **No file:line collisions, no regression in physics surfaces**. The R17 commit set does not modify any `quarkConstraints/`, `flavorConstraints/`, `solvers/`, `scanParams/`, or `tests/` file. All changes are confined to `flavor_catalog/{catalog_master.tex, catalog_master.pdf, master_compile_v02_report.md, audits/, external_research/, processes/<family>/index.tex}`. No risk of code regression downstream.

## Issues

- [R17-I1] severity:LOW tag:provenance
  Title: External-research PDFs lack an explicit sha256 / MANIFEST / README in `flavor_catalog/external_research/`
  Description: `022a20c` imports `deepresearch_may15.pdf` (203806 bytes) and `deepresearch_may16.pdf` (343307 bytes) alongside paired `.txt` extracts (1109 and 2092 lines respectively). The two `_review.md` memos line-cite the `.txt` extracts and the catalog, and `signoff/round_003_index.md:10` references the directory as the Wave-7 source. However the directory does not contain a `README.md`, `MANIFEST.{json,yaml,md}`, or `sha256sum.txt` documenting (a) the PDF binary digests, (b) the GPT Deep Research session URLs / dates, (c) the prompt that generated each PDF, or (d) the text-extraction provenance (i.e. whether `.txt` was generated by `pdftotext`, `pdfminer`, or hand-paste). This is acceptable for an external-research input source but lower-rigor than the per-process `source_shas` blocks and per-snapshot `sha256` entries that the catalog uses elsewhere (e.g. `K001.yaml:63-67`, `E009.yaml:96-98`). Cosmetic provenance gap; does not affect any catalog numerical claim because (i) no `pdg_or_equivalent` block depends on the deepresearch PDFs, and (ii) the Wave-7 promotions were independently re-verified against PDG / HFLAV / experiment sources in the `factcheck_<family>.md` addenda.
  Evidence: `git show 022a20c --stat` shows only the 4 PDF/text files added with no companion provenance file; `ls flavor_catalog/external_research/` lists only `deepresearch_may{15,16}.{pdf,txt}` and `deepresearch_may{15,16}_review.md`; `signoff/round_003_index.md:10` cites the directory as Wave-7 source.
  Recommended fix: Optional - add `flavor_catalog/external_research/MANIFEST.md` with PDF sha256, generation date, session URL (if available), and `pdftotext` invocation that produced the `.txt` extract. Pattern after the per-process `source_shas` schema.

- [R17-I2] severity:LOW tag:docs
  Title: `master_compile_v02_report.md` claims "79 VERIFIED of 80" but `factcheck_status.md` at R17 endpoint only documents 75 rows
  Description: `flavor_catalog/master_compile_v02_report.md:48` reads "Fact-check status: 79 VERIFIED, 1 PARTIAL (E009 INSPIRE JS-only; APS journal cross-check confirms content), 0 MISMATCH, 0 FAILED" - a count across 80 processes including the 5 Wave-7 additions (T003/T004/T008/T012/B012). However the consolidated table in `flavor_catalog/audits/factcheck_status.md` at the R17 consolidation commit `42ac647` only contains 75 rows (the original DA-4-converged set); the family totals there sum to 75, and the Recommendation block reads "ready to tag flavor-catalog-v0.1". The Wave-7 verdicts that justify the report's "79 VERIFIED" claim live in `audits/factcheck_top_higgs_ew.md:294-352` (Wave-7 addendum, deposited as part of R16 commit `7a6bd34`) and the B012 row in `audits/factcheck_beauty.md` (R16 commit `ca2beba`), but were not folded back into the consolidated R17 table at the R17 endpoint. The current `factcheck_status.md` (post-R19) does include all Wave-7+ rows (89 process entries, 88 VERIFIED + 1 PARTIAL per `:124-131`). Documentation-drift only - no numerical claim is wrong - but a strict reader of the R17 commit set will see the report's "79/80" and the consolidated table's "74/75" and have to reconcile by reading two extra family files.
  Evidence: `flavor_catalog/master_compile_v02_report.md:48`; `git show 42ac647:flavor_catalog/audits/factcheck_status.md` row count 75; `flavor_catalog/audits/factcheck_top_higgs_ew.md:294-352` (Wave-7 addendum with T003/T004/T008/T012 each VERIFIED 0/0); `flavor_catalog/audits/factcheck_beauty.md` (B012 VERIFIED row).
  Recommended fix: Optional - either (a) fold the Wave-7 verdicts directly into the consolidated `factcheck_status.md` table at the time of the v0.2 master compile, or (b) add a one-line note in `master_compile_v02_report.md:48` clarifying "75 in the consolidated table + 5 in the `factcheck_top_higgs_ew.md` Wave-7 addendum + 1 in `factcheck_beauty.md` = 79 VERIFIED of 80; the consolidated table was last regenerated at the v0.1 milestone".

- [R17-I3] severity:INFO tag:docs
  Title: Two distinct "v0.2" milestones exist - `catalog_master.tex` at `835cf48` (80 PRIMARY) vs `master_compile_v02_report.md` referring to the same point
  Description: The branch carries two artifacts named "v0.2": the v0.2 master compile (catalog covering 80 PRIMARY) and the v0.2 master compile report. They are mutually consistent. However later Wave-8 (R18) and Wave-9 (R19) added SECONDARY and collider_rs sections, producing master_compile_v03_report.md and master_compile_v04_report.md. The reader has to consult the report file to understand which "v0.2" snapshot was current at R17 close. The `catalog_master.pdf` in working tree is the latest re-compile (post-R19) and is *not* the v0.2 PDF that R17 sealed. No defect; a strict reader needs to use `git show 835cf48:flavor_catalog/catalog_master.tex` to inspect the actual R17-endpoint state. Cosmetic.
  Evidence: `flavor_catalog/master_compile_v02_report.md`, `master_compile_v03_report.md`, `master_compile_v04_report.md`; current `catalog_master.tex` lines 33-78 list 8 sections including collider_rs and a SECONDARY block that were not in `git show 835cf48:flavor_catalog/catalog_master.tex` (which had 6 sections).
  Recommended fix: None at present. Future master-compile cycles could tag annotated git tags (`v0.2-catalog`, `v0.3-catalog`, `v0.4-catalog`) so each milestone has a stable refname for downstream consumers.

## Verdict

**APPROVE** for R17. The 15-commit set delivers a coherent v0.1 -> v0.2 milestone:
- Master compile builds successfully twice (75 -> 80 PRIMARY) with clean LaTeX structure and a 133-page PDF.
- Family-by-family live-source fact-check covers all 75 DA-4-converged processes and catches one digit-level mismatch (T020), which is patched in-cycle.
- The single PARTIAL row (E009) has a documented JS-rendering-only reason that does not invalidate any numerical claim.
- External research is imported and line-cite-reviewed against the catalog; the 5 Wave-7 promotions identified are already independently captured in R16.
- Three LOW/INFO documentation-drift issues (R17-I1, R17-I2, R17-I3) are non-blocking provenance and counting-bookkeeping seams.

No code-side regression risk: R17 touches only `flavor_catalog/` documentation and audit surfaces.
