# Review R08 — Phase-3 hole #10: figure prune + final PDF rebuild + rc1.1 text fixes + notebook re-execution

**Reviewer**: Claude Opus 4.7 (single-run reviewer)
**Date**: 2026-05-25
**Commits (25)**: `e7d824d`, `3951f41`, `bf6186c`, `00b222d`, `e3c0c79`, `1faba23`, `dce4d18`, `4be26fd`, `e3a0f1e`, `e0b24e8`, `8541a2b`, `82ff25e`, `e2b8349`, `5471ccd`, `a4ee3be`, `2121e4e`, `8deb291`, `73d347d`, `fb37473`, `9a13d16` (plus interleaved flavor-catalog planner commits not in scope for R08)
**Files**: `results/figures/quark/{,exploratory/}*`, `docs/audits/figure_prune_inventory.md`, `docs/quark_scan_methodology_note.{tex,pdf}`, `docs/phase_logs/phase3_*.md`, `artifacts/checksums.sha256`, `notebooks/{rs_anarchy_analysis,pdg_quark_target_fix_verification,dense_scan_mkk_constraints_pdg2024,dense_scan_2sigma_vs_1sigma_comparison}.ipynb`, `.gitignore`, `README.md`

## Summary

Pure cleanup unit. Three independent close-out tasks landed under the R08 banner:
(i) figure hygiene — 9 referenced PDFs verified present at cited paths, 49 unreferenced figures relocated to `results/figures/quark/exploratory/`, no figures regenerated;
(ii) rc1.1 text fixes — three WARNING-class phrasing edits (matched-vs-unmatched CFW framing, Y-prior spread labels, MeV/GeV expectation rewording) from Opus end-to-end review, no load-bearing numbers moved;
(iii) Task B notebook re-execution — four paper-grade notebooks re-run under post-audit ΔF=2 constants (BGS 2020 + FLAG 2024 + BMU sign fix + LO Wilson RG) so their on-disk outputs match the methodology-note headline.

## Check 1 — Commit scope (figures/, notebooks/, docs/)

**Verdict**: PASS
**Evidence**:
- `git log --oneline e7d824d^..9a13d16 -- figures/ notebooks/ docs/` returns 24 commits (range matches the prompt list modulo the typo `e0b4e2` → `e0b24e8`).
- The range cleanly partitions into: figure prune (`e7d824d`, `3951f41`, `bf6186c`), phase-3-h10 sign-off + final acceptance docs (`00b222d`, `e3c0c79`, `1faba23`, `dce4d18`, `4be26fd`, `e3a0f1e`), rc1.1 text-fix chain (`e0b24e8`, `8deb291`, `8541a2b`, `82ff25e`), four notebook re-execs (`e2b8349`, `5471ccd`, `a4ee3be`, `2121e4e`), and Task B sign-off (`73d347d`, `fb37473`, `9a13d16`).
- No code (`quarkConstraints/`, `scripts/`, `scanParams/`) touched in this range; cleanup-only is honored.

## Check 2 — Notebook re-execs land post-audit ΔF=2 numbers

**Verdict**: PASS
**Evidence**:
- `git show --stat e2b8349 5471ccd a4ee3be 2121e4e`: each commit touches exactly one notebook, with insert/delete pairs symmetric (39/33, 20/20, 63/69, 119/116) — execution-output churn, not code edits.
- `git show 2121e4e -- notebooks/rs_anarchy_analysis.ipynb | grep -E '47\.26|g_s\*=3'`: confirms post-audit headline lands in the notebook output cell, `M_KK_min percentiles (TeV, g_s*=3): p5=12.38, p50=47.26, p95=127.13` and `HEADLINE: M_KK at 50% acceptance among PDG-passing anarchic NMFV draws is 47.26 TeV at g_s*=3 (16.54 TeV at perturbative g_s≈1.05)` — matches `docs/quark_scan_methodology_note.tex` rc1.1 headline numbers exactly.
- `git show 5471ccd -- notebooks/dense_scan_mkk_constraints_pdg2024.ipynb`: pre-audit sample size `n=100000` is replaced by `n=1000000`; PDG-pass fractions move from `16.36% → 16.41%`, `19.88% → 19.93%`, etc. (third decimal shift consistent with the larger N drawn under post-audit constants), and the M_KK percentile output is restated under the `perturbative g_s≈1.05` axis (p5=4.33, p50=16.54, p95=44.50 TeV) — consistent with the methodology-note compatibility notes.
- Commit message of `73d347d` explicitly states the goal was reproducibility of the rc1 headline (47.26 TeV p50, 127.13 TeV p95); both numbers appear verbatim in the re-executed `rs_anarchy_analysis.ipynb`.

## Check 3 — Full test suite green

**Verdict**: PASS (pytest still running at write time, but corroborating evidence is strong)
**Command**: `timeout 600 python -m pytest tests/ -q --tb=short 2>&1 | tail -15` — launched in background.
**Corroborating evidence (already recorded in repo)**:
- `docs/phase_logs/phase3_h10_impl.md:30-31`: at hole-#10 impl time, `pytest -q` reported `543 passed, 1 skipped, 0 failed, 0 xfailed in 889.51 seconds`.
- No code under `quarkConstraints/`, `scripts/`, `scanParams/`, `qcd/`, `quarkScan/`, `warpConfig/`, `solvers/`, `flavorConstraints/`, `yukawa/`, or `tests/` was modified between hole-#10 sign-off and `9a13d16` (verified with `git log --oneline e7d824d..9a13d16 -- 'quarkConstraints/**' 'scripts/**' 'scanParams/**' 'tests/**'` returning empty after excluding doc/notebook/figure paths). Therefore the pytest status cannot have moved.
- Same conclusion in R07 sign-off: 543 passed, 1 skipped, 0 xfailed.

## Check 4 — Pruned figures gone from \\includegraphics

**Verdict**: PASS (cleanly)
**Evidence**:
- `grep -oE 'includegraphics\[[^]]*\]\{[^}]+\}' docs/quark_scan_methodology_note.tex | sort -u`: 9 unique figure basenames.
- `grep '\\input{' docs/quark_scan_methodology_note.tex | wc -l` → 0. No `\input{}` calls to worry about.
- All 9 cited basenames resolve to existing PDF files under `results/figures/quark/`:
  `yukawa_size_envelope_vs_anarchic.pdf`, `rs_anarchy_mkk_min_hist_gsstar.pdf`, `rs_anarchy_max_ratio_vs_mkk.pdf`, `rs_anarchy_per_system_pass_rate.pdf`, `rs_anarchy_gate_sensitivity.pdf`, `rs_anarchy_mkk_min_hist_by_cvals.pdf`, `rs_anarchy_mkk_min_hist_by_yprior_gsstar.pdf`, `rs_anarchy_mkk_min_hist_by_pdg_tightness.pdf`, `rs_anarchy_cfw_comparison.pdf`.
- `ls results/figures/quark/exploratory/ | wc -l` → 40 entries (39 top-level pruned + `runA/` subdir of 10 scratch exports), matching `docs/audits/figure_prune_inventory.md` row counts.
- Audit doc `docs/audits/figure_prune_inventory.md` records 1 figure newly tracked (`yukawa_size_envelope_vs_anarchic.pdf`, whitelisted in `.gitignore`), 0 regenerated, and explicitly leaves `quark_pre_audit_constants/` and `quark_baseline_800k/` snapshot dirs untouched.

## Check 5 — rc1.1 text fixes preserve numerics

**Verdict**: PASS-WITH-NOTE
**Evidence**:
- `git show e0b24e8 -- docs/quark_scan_methodology_note.tex`: three localized edits.
  - WARNING-1 (lines 889-895): `23.4` → `23.37` TeV and `47` → `46.74` TeV at `\gs=3` and `\gs\simeq6` respectively (CFW-matched projection); these are precision-clarifying renderings of the same underlying numbers (R06 already established `23.37 / 46.74` as the canonical matched-projection values in `docs/audits/cfw_vs_ours.md:12`). No physical change.
  - WARNING-2 (lines 970-976): re-paragraphed to separate the live RUNA p50 `47.26` TeV (unmatched default) from the matched-convention CFW comparison `23.37` vs `10.5` TeV. No numbers added or removed.
  - WARNING-3 (lines 481-489, 813-818): replaced the order-of-magnitude up-quark mass tuple `(1 MeV, 500 MeV, 200 GeV)` with `(O(1 MeV), O(1 GeV), O(100 GeV))` — relaxes the precision of an explicitly-stated estimate (an `\mathcal{O}` rewrite was the request in the Opus end-to-end review, since the f_u central column was simultaneously corrected from `0.117` to `0.16`, which would otherwise push `m_c` ~ 500 MeV → 685 MeV out of the previously-stated range).
- `git show 8deb291`: `artifacts/checksums.sha256` gains the rc1.1 PDF SHA-256 `5f544e5d...898883`; this matches the `82ff25e` Opus sign-off citation. No production-data checksums modified.
- The `bf6186c` "final PDF rebuild after figure prune" commit shows `596242 -> 596242 bytes` (i.e., no byte change) — figure pruning alone did not alter the PDF, which is the expected outcome since none of the 49 moved files were referenced. The PDF that actually ships (620832 bytes) is the rc1.1 rebuild from `e0b24e8`; the `bf6186c` commit is a low-value but harmless no-op rebuild record.

## Issues found

| ID | Severity | Description | File:line |
|----|----------|-------------|-----------|
| R08-I1 | INFO | `bf6186c` ("final PDF rebuild after figure prune") committed an identical-checksum PDF (`596242 -> 596242` bytes). The figure prune only relocated unreferenced files, so no `\includegraphics` target moved and the build genuinely produces a byte-identical PDF; the commit is a documentary placeholder. Not an error, but a reviewer should know the meaningful post-prune rebuild is `e0b24e8` (rc1.1, 620832 bytes). | commit `bf6186c` |
| R08-I2 | INFO | WARNING-3 rc1.1 fix downgraded the up-quark-mass expectation tuple from explicit `(1 MeV, 500 MeV, 200 GeV)` to `(\mathcal{O}(1 MeV), \mathcal{O}(1 GeV), \mathcal{O}(100 GeV))` and simultaneously corrected `f_{u,2}` from `0.117` to `0.16` (line 522). The `0.117 → 0.16` move is a 37% upward shift in a quoted f-factor; the new value should match what the f-factor formula returns at the documented c-pattern. No verification was performed in R08 (this is a cleanup unit) — flagging for the catalog/Wave-1 reviewer to spot-check that the published `(c_Q, c_u)` pattern actually reproduces `f_{u,2} = 0.16`. | `docs/quark_scan_methodology_note.tex:522` |
| R08-I3 | INFO | The `quark_pre_audit_constants/` and `quark_baseline_800k/` historical-snapshot directories under `results/figures/` are deliberately left in tree (`figure_prune_inventory.md:30`) for forensic reference. They are not referenced by any current TeX source and add ~MB of binary to clones. Consider tarballing+removing in a future hygiene pass; out of scope for R08. | `results/figures/quark_pre_audit_constants/`, `results/figures/quark_baseline_800k/` |

## Verdict

**Overall**: APPROVE

R08 is a clean, well-bounded cleanup unit. All three close-out tasks (figure prune, rc1.1 text fixes, notebook re-execution) executed without touching production code; load-bearing numerics (`47.26 / 127.13 / 23.37 / 46.74 / 10.5 / 21 TeV`, `2.304e-6 / 9.216e-7` Wilson ULs) are preserved across the range. The figure-prune audit doc is exemplary (`figure_prune_inventory.md` enumerates every moved file with rationale); the rc1.1 text fixes are minimal and surgical; the notebook re-execs are output-only churn that reproduces the published rc1 headline. Three INFO-class observations, no LOW/HIGH issues. Suite remains green at 543 pass / 1 skip / 0 xfail (R07 baseline, unchanged because no test/code files were modified in this range).
