% Review R18 — Catalog Wave-8 (8 SECONDARY tier processes: T014, K019, K020, K021, B007, B008, B013, B014) and master compile v0.3

**Reviewer**: Claude Opus 4.7 (single-run reviewer)
**Date**: 2026-05-25
**Commits** (34, per MERGE_PLAN.md §B.1 R18 row at `.orchestration/MERGE_PLAN.md:311`): runbook scaffold `82a2c01`; 8 PKA initial drafts `2630168` (B008), `bab5bd0` (K020), `37beabb` (commit message says PKA-K020 but content is B013 — see R18-I2), `ebd066c` (B013, second deposit — prompt typo `ebdo66c` resolves to `ebd066c`), `e6e1cc3` (B007), `5b68b43` (K021), `6b64a18` (T014), `e348a35` (K019), `17599d5` (B014); stage-1 close `0ad4a07`; scaffold-2 + 4 WAs `4a6df17`; master-tex wire-in `103833f`; 4 WA cycle-1 polishes `d198787` (T014), `2b23464` (B-radiative), `f9c7ff4` (kaon LFV), `88f2cde` (B-rare-leptonic); stage 4 CAs `cd2f81b`; 4 CA cycle-1 verdicts `b3c35d7` (B-radiative), `e9f3cf3` (B-rare-leptonic) (prompt SHA `e9f3cf3` — actual: `e9fcdf3`), `41069e7` (kaon LFV), `749d789` (T014); K020 cycle-2 rework `5c14d2f` (WA-v2) + `a20d75a` (CA-v2); stage 4-done runbook `980b45b`; 3 fact-check addenda `118de1c` (top_higgs_ew), `37514e9` (beauty), `7e121c3` (kaon); Opus round-4 sign-off `df7af95`; stage-5 runbook `ef5a417`; master compile v0.3 `76c87ae`; close-out `fff1898`; K020 cycle-3 metadata cleanup `6a2506c`; K020 re-fact-check `b5c2375`.

**Files**: `flavor_catalog/processes/secondary/{beauty,kaon,top_higgs_ew}/{B007,B008,B013,B014,K019,K020,K021,T014}.{tex,yaml}` (8 entries × 2 files = 16), three `processes/secondary/<family>/index.tex` TOC stubs, `flavor_catalog/catalog_master.tex` (secondary section wired in at `103833f`), `flavor_catalog/catalog_master.pdf` (148 pages, 796,017 bytes), `flavor_catalog/master_compile_v03_report.md`, `flavor_catalog/audits/factcheck_{beauty,kaon,top_higgs_ew}.md` (Wave-8 addenda only — the consolidated `factcheck_status.md` was NOT regenerated; see R18-I3), `flavor_catalog/signoff/round_004_index.md`, `flavor_catalog/worklogs/orchestration/wave_008_runbook.md`, `flavor_catalog/PRIORITY_TIERS.md` (Wave-8 row table at §4 already present from R20 docs).

**Sample selection** (per MERGE_PLAN §C.2.1 family-coverage rule). Per prompt directive, two SECONDARY-tier spot-checks anchored on kaon LFV:
- **K020** `K^+ → π^+ e^± μ^∓` (kaon, **cycle-3 metadata cleanup target**) — the only Wave-8 entry that went through three WA cycles; the prompt requires confirming the cleanup is sound and that the SECONDARY tier rationale is documented.
- **K019** `K_L → e^± μ^∓` (kaon, cycle-1 PASS, BNL E871 anchor) — companion check confirming the LFV-kaon trio physics anchor is faithful to literature.

Plus shallow coverage of all 8 SECONDARY entries (priority_tier, promoted_in_wave, fact-check verdict, OPUS-APPROVED timestamp), the v0.3 tag annotation vs runbook/compile-report numbers, and the master-tex wiring.

## Summary

R18 closes Wave-8, the first SECONDARY-tier promotion wave (the canonical Pattern-A wave per `PRIORITY_TIERS.md:131-152`). The 8 processes are the DA-4 deferred top-tier candidates listed in `PRIORITY_TIERS.md:90-99`: the LFV-kaon trio K019/K020/K021 (lepton-bulk RS scope extension); the rare-leptonic B tails B007 (`B_{s,d} → e^+e^-`) and B008 (`B_{s,d} → τ^+τ^-`); the exclusive-radiative B-companions B013 (`B_s → φγ`) and B014 (`B → ργ, ωγ`); and the FCNC-Z handle T014 (`Z → bs, bd, sd`). The pipeline (PKA → WA → CA → fact-check → Opus) is the Wave-7 protocol with two adjustments (`wave_008_runbook.md` §6): all paths use `processes/secondary/<family>/` and every YAML carries `priority_tier: SECONDARY`, `priority_rationale`, `promoted_in_wave: 8`. The K020 entry uniquely required three writer cycles: cycle-1 CA flagged that the E865-only `2.1e-11` was in prose but not in `pdg_or_equivalent.values`, cycle-2 WA-v2 promoted it (commit `5c14d2f`), Opus round-4 accepted with a single PARTIAL fact-check (NA62 first-author convention drift), and then a post-tag WA-v3 cleanup (`6a2506c`) aligned the NA62 author string with PDG/K021 convention, after which a re-fact-check (`b5c2375`) flipped K020 to VERIFIED. Master compile v0.3 (148 pages, 796 KB) was tagged `flavor-catalog-v0.3` on commit `76c87ae` at 14:45 EDT, before the K020 cycle-3 cleanup landed at 15:50 — so the tag captures the "7V+1P" state and the K020 → VERIFIED transition is post-tag. All 8 entries are OPUS-APPROVED and (current HEAD) VERIFIED.

## Check 1 — Physics intuition / literature anchor (K019 + K020)

**Verdict**: PASS

**K020 `K^+ → π^+ e^± μ^∓` — cycle-3 cleanup verified end-to-end**:
- Tier policy: `flavor_catalog/processes/secondary/kaon/K020.yaml:4-6` declares `priority_tier: SECONDARY`, `priority_rationale` (DA-4-deferred LFV-kaon trio; "Top-tier; charged-current LFV companion to K019. Lower implementation priority than Waves 1-7 PRIMARY entries"), and `promoted_in_wave: 8`. Located at `processes/secondary/kaon/K020.{tex,yaml}` per the `PRIORITY_TIERS.md:43-46` repo-structure contract. The `.tex` body at `K020.tex:38-39` self-cites `flavor_catalog/PRIORITY_TIERS.md` for the tier policy.
- Physics anchor: the headline PDG numbers `K020.yaml:120-179` are the two charge-assignment limits. `BR(K^+ → π^+ μ^+ e^-) < 1.3×10^-11 (90% CL)` (PDG identifier `S010.29`, combined BNL E865/E777, with the E865-only Sher 2005 `2.1×10^-11` carried separately at `:141-159` per the cycle-2 WA-v2 promotion). `BR(K^+ → π^+ μ^- e^+) < 6.6×10^-11 (90% CL)` (PDG `S010.25`, sourced from NA62 R. Aliberti et al. 2021 PRL 127.131802, arXiv:2105.06759). Both numbers match PDG 2025 and the published abstracts. The two charge assignments are kept distinct because models with chiral lepton couplings can populate them differently (`K020.tex:73-78`), which is the standard convention.
- Cycle-3 cleanup diff (`git show 6a2506c`): exactly two metadata changes, in `K020.yaml:160` and `references/K020/source_manifest.yaml:33`, both replacing the NA62 first-author string `E. Cortina Gil et al.` with `R. Aliberti et al.`. Both forms are valid NA62 spokesperson-era publications and the underlying paper (DOI 10.1103/PhysRevLett.127.131802) is unchanged; PDG `S010.25` cites `ALIBERTI 2021`, which is the canonical PDG convention also used by the K021 sidecar. No value, year, DOI, URL, or sha256 changed. The `worklogs/writer/wa_w8_kaon_LFV_v3.md` log records this as a metadata-only fix that clears the cycle-1 fact-check PARTIAL finding.
- Re-fact-check (`b5c2375`): `flavor_catalog/audits/factcheck_kaon.md:264-296` records K020 as `VERIFIED` with `findings_count: 0`, and `K020.yaml:89-97` adds the final `FACT-CHECKED` transition with `verdict: "VERIFIED"`. Status history (`K020.yaml:14-97`) is monotone-forward and contains the L001-precedent carve-out documentation at `:46`.
- Code coverage: `K020.yaml:226-247` correctly reports `status: NO` with four documented `rg` commands and three near-miss hits (`flavorConstraints/muToEGamma.py:75` is a dipole LFV check, not semileptonic kaon; `quarkConstraints/deltaf2.py:755` is `Δm_K` not `ΔS=1`; `quarkConstraints/modern/phenomenology.py:23` lists only `epsilon_K, K, B_d, B_s, D0`). HIGH implementation difficulty is correctly attributed at `:246-248` to the need for a new `ΔS=1` semileptonic LFV operator basis and `K → π` form factors.

**K019 `K_L → e^± μ^∓` — physics anchor + L001 carve-out**:
- `flavor_catalog/processes/secondary/kaon/K019.yaml:81-94` carries the PDG 2025 anchor `BR(K_L → e^± μ^∓) < 4.7×10^-12 (90% CL)`, sourced to BNL E871 (Ambrose et al. 1998, arXiv:hep-ex/9811038). This is the canonical literature value (the long-standing E871 result; PDG 2025 K_L listing still quotes it as the world-best limit). The Ambrose 1998 abstract reads `< 4.7×10^-12 at the 90% confidence level`, which matches `K019.yaml:84-88` digit-for-digit. SECONDARY classification reasoning is documented at `K019.yaml:5` (`Deferred by DA-4...; Top-tier per SESSION_NOTES.md sec 7; one of three explicit lepton-bulk-RS handles in the kaon sector`).
- Fact-check `flavor_catalog/audits/factcheck_kaon.md:264, 274` records K019 VERIFIED with 0 findings. Status history `K019.yaml:14-62` shows clean PKA → WA → CA-cycle-1-PASS → OPUS-APPROVED → FACT-CHECKED. No cycle-2 needed.

**Cross-check vs other 6 SECONDARY entries**: all carry `priority_tier: SECONDARY`, `priority_rationale`, `promoted_in_wave: 8`, `fact_check_verdict: VERIFIED`, `opus_approved_at: 2026-05-17T14:31:04`. The Round-4 sign-off `flavor_catalog/signoff/round_004_index.md` per-process table (the `## Per-process verdicts` table) gives 8 APPROVE, with each anchor literature value spot-checked (B007 `9.4e-9`/`2.5e-9` PDG 2026; B008 `6.8e-3`/`2.1e-3` PDG 2026; B013 `(3.4 ± 0.4)e-5` PDG 2025; B014 `(9.8 ± 2.5)e-7`, `(8.6 ± 1.5)e-7`, `(4.4 +1.8/-1.6)e-7` PDG 2025; T014 `< 2.9e-3` ECFA 2025 / Abu-Ajamieh 2026). All these numbers are consistent with PDG Live, HFLAV Dec-2024, and the cited arXiv abstracts as recorded in the per-family Wave-8 fact-check addenda at `audits/factcheck_{beauty,kaon,top_higgs_ew}.md` ## Wave-8 addendum sections. Both `K → π e μ` charge assignments and the `K_L → eμ` E871 anchor agree with the literature; no value-bearing physics error.

## Check 2 — Code / repo consistency

**Verdict**: PASS-with-concern (three INFO/LOW issues — none block the v0.3 milestone)

**File existence** (`find flavor_catalog/processes/secondary -name "*.{tex,yaml}"`): all 16 expected files present — `beauty/{B007,B008,B013,B014}.{tex,yaml}`, `kaon/{K019,K020,K021}.{tex,yaml}`, `top_higgs_ew/T014.{tex,yaml}`, plus the three `index.tex` TOC stubs. The `secondary/` parent is structurally separated from the PRIMARY families per `PRIORITY_TIERS.md:43-46`, as required.

**Master-tex wiring** (commit `103833f`): `flavor_catalog/catalog_master.tex` added a single top-level `\section{Secondary Entries (Wave-8+, lower implementation priority)}` containing one `\subsection*{<Family>}` + `\input{processes/secondary/<family>/index}` per affected SECONDARY family. The three `index.tex` stubs use `\IfFileExists` guards (defensive against missing process files), consistent with the existing per-family PRIMARY `index.tex` pattern. The wiring matches the `PRIORITY_TIERS.md:54-61` master-TeX structure contract verbatim.

**v0.3 tag**: `git tag -l | grep v0.3` returns `flavor-catalog-v0.3` (one tag). Annotated tag (`git cat-file -p flavor-catalog-v0.3`) points to commit `76c87ae` (master compile v0.3), tagged 2026-05-17T14:45:27. Tag message: "80 PRIMARY + 8 SECONDARY (Wave-8 LFV-kaon trio K019/K020/K021, rare leptonic B007/B008, exclusive radiative B013/B014, FCNC-Z T014). 88 OPUS-APPROVED total. 87 VERIFIED + 1 PARTIAL (K020 metadata-only, accepted per v0.2 E009 precedent)." See R18-I1 for the V/P count seam (tag says 87V+1P, compile report says 86V+2P, runbook §8 says 86V+2P).

**Master compile artifacts**: `flavor_catalog/catalog_master.pdf` (148 pages, 796,017 bytes per `master_compile_v03_report.md:42-43`) builds clean with only layout warnings (overfull/underfull hbox + hyperref PDF-string warnings for math in section/bookmark strings — same warnings as v0.1/v0.2, expected). Family counts in `master_compile_v03_report.md:11-16`: kaon 13+3=16; charm 8+0=8; beauty 22+4=26; top/Higgs/EW 19+1=20; charged-lepton 11+0=11; EDM/neutrino 7+0=7. PRIMARY total = 80; SECONDARY total = 8; sum = 88. Matches the v0.3 tag annotation.

**Fact-check audits**: per-family Wave-8 addenda are present at `audits/factcheck_kaon.md:254-313` (K019/K020/K021 — VERIFIED, VERIFIED-after-cycle-3, VERIFIED), `audits/factcheck_beauty.md:243-310` (B007/B008/B013/B014 — all VERIFIED 0/0), `audits/factcheck_top_higgs_ew.md:391-427` (T014 — VERIFIED 0/0). However, the consolidated table at `audits/factcheck_status.md:32-108` still has only 75 rows at v0.3 (and 88+E009 rows at current HEAD) — the 8 Wave-8 SECONDARY entries were never folded into the consolidated table. See R18-I3 (this is a recurrence of R17-I2 at the SECONDARY tier).

**Status-history schema consistency**: spot-check on K020 (`K020.yaml:14-97`) shows status-history transitions are clean (DRAFT → WRITER-INITIATED → WRITER-DONE → WRITER-REWORK → WRITER-DONE → CHECKER-DONE → OPUS-APPROVED → FACT-CHECKED(PARTIAL) → WRITER-DONE(cycle-3) → FACT-CHECKED(VERIFIED)) with `from`/`to` arrows monotone. The cycle-3 transition `:82-88` is the same shape as the L001 / B001_B003 carve-out precedents and is documented in the `worklogs/writer/wa_w8_kaon_LFV_v3.md` worklog (referenced from the fact-check addendum `factcheck_kaon.md:299`). The other 7 SECONDARY entries have shorter, single-cycle histories.

**Commit `37beabb` mis-labeled**: `git show --stat 37beabb` shows the commit message says `PKA-K020 initial draft (SECONDARY)` but the actual content is the B013 family (`processes/secondary/beauty/B013.{tex,yaml}` + `references/B013/*`). The actual K020 content landed in `bab5bd0` (correctly labeled `PKA-K020`). The companion B013 commit `ebd066c` (`flavor-catalog(wave8): PKA-B013 initial draft (SECONDARY)`) at 02:18:44 deposited a different set of `references/B013/` files (8 of them), so `37beabb` is a duplicate-label seam on the B013 content, not a missing-content defect. See R18-I2.

**Code coverage NO consistency**: spot-check on `K020.yaml:226-247` and `K019.yaml` (analogous block) — both correctly report `status: NO`, list the families' implementation gaps under the existing modern phenomenology surface (`quarkConstraints/modern/phenomenology.py:23` carries only `epsilon_K, K, B_d, B_s, D0`), and explicitly identify the near-miss in `flavorConstraints/muToEGamma.py:75` (dipole LFV ≠ semileptonic kaon LFV). The other 6 SECONDARY entries follow the same `status: NO` pattern. None of the 8 Wave-8 entries claims false code coverage.

**PRIMARY entries untouched**: spot-check on `processes/charm/`, `processes/charged_lepton/`, `processes/edm_neutrino/` shows no Wave-8 commits modify any PRIMARY YAML or TeX (per `master_compile_v03_report.md:18` — "Per-process `.tex` and `.yaml` content was not modified during this master rebuild"). The `PRIORITY_TIERS.md:82` "Do not retroactively edit any PRIMARY sidecar in this round" contract is honored.

## Check 3 — Audit/orchestration cross-consistency

**Verdict**: PASS

**Wave-8 runbook close-out** (`flavor_catalog/worklogs/orchestration/wave_008_runbook.md` §8): documents "8 SECONDARY entries drafted, polished, verified, fact-checked, signed off, compiled, tagged" and explicitly records the v0.3 catalog state as "86 VERIFIED + 2 PARTIAL (E009 v0.2, K020 v0.3 — both metadata-only, accepted per E009 precedent)". The runbook is self-consistent with `master_compile_v03_report.md:49-52` ("PRIMARY 79V/1P + SECONDARY 7V/1P = 86V/2P out of 88") but inconsistent with the v0.3 tag annotation ("87V+1P"). The runbook §6 also documents the two SECONDARY-specific protocol adjustments (paths + YAML fields), preserving auditability.

**Round-4 Opus sign-off**: `flavor_catalog/signoff/round_004_index.md` records 8 APPROVE / 0 RETURN / 0 ESCALATE with the K020 PARTIAL ACCEPT decision based on the v0.2 E009 metadata-only precedent (also documented at `factcheck_kaon.md:299`). The per-process table cites each terminal CA worklog by filename (`ca_w8_kaon_LFV.md`, `ca_w8_kaon_LFV_v2.md`, `ca_w8_B_rare_leptonic.md`, `ca_w8_B_radiative.md`, `ca_w8_T014.md`), all of which exist in `worklogs/checker/`. The Round-4 sign-off was at 14:31 EDT, before the K020 cycle-3 cleanup (15:50) and re-fact-check (16:01); the cycle-3 work is post-Opus-sign-off and explicitly handled as a metadata-only follow-up rather than re-triggering an Opus round.

**PRIORITY_TIERS.md Wave-8 §4 row table** (`flavor_catalog/PRIORITY_TIERS.md:90-99`): lists all 8 SECONDARY entries with one-line deferral + promotion rationale; matches each YAML's `priority_rationale`. The §5 implementation-priority rule (`:101-110`) — "No SECONDARY entry may be pulled into code without explicit PI confirmation, even if its physics looks superficially simpler" — is the binding constraint that justifies the lower `code_coverage: NO` status across all 8 entries. This is the canonical Wave-8 deferral basis and is internally consistent.

**v0.3 tag vs runbook V/P count discrepancy** (R18-I1): the tag annotation says "87 VERIFIED + 1 PARTIAL"; the compile report and runbook say "86V + 2P". The discrepancy is exactly the E009 row: E009 has been carried as PRIMARY-PARTIAL since v0.1 (R17 review §1 confirmed; INSPIRE JS-only render) and is correctly reported as PARTIAL in `factcheck_status.md:106`. The tag author appears to have collapsed E009 into VERIFIED, undercounting PARTIAL by one. Cosmetic only — no numerical claim is wrong.

## Issues

- **R18-I1** severity:LOW tag:docs
  - Title: `flavor-catalog-v0.3` tag annotation says "87 VERIFIED + 1 PARTIAL" but compile report and runbook §8 say "86V + 2P"
  - Description: `git cat-file -p flavor-catalog-v0.3` reads "88 OPUS-APPROVED total. 87 VERIFIED + 1 PARTIAL (K020 metadata-only)". This is internally inconsistent with `flavor_catalog/master_compile_v03_report.md:49-52` ("PRIMARY 79V/1P + SECONDARY 7V/1P = 86V/2P") and with `flavor_catalog/worklogs/orchestration/wave_008_runbook.md` §8 ("86 VERIFIED + 2 PARTIAL"). The discrepancy is the E009 row, which has been PARTIAL since v0.1 (R15-I2, R17 review) and remains PARTIAL at v0.3. The tag annotation appears to have folded E009 into VERIFIED. No catalog content is wrong; the per-process YAMLs and the per-family `factcheck_*.md` addenda all agree with the compile report's 86V+2P count. Cosmetic seam in the tag message only. Note that after the post-tag K020 re-fact-check (commit `b5c2375`), the current HEAD state is 87V+1P (K020 flipped to VERIFIED, E009 still PARTIAL), so the tag annotation matches the post-tag state but not the at-tag state.
  - Evidence: `git cat-file -p flavor-catalog-v0.3`; `flavor_catalog/master_compile_v03_report.md:49-52`; `flavor_catalog/worklogs/orchestration/wave_008_runbook.md` §8; `flavor_catalog/audits/factcheck_kaon.md:265` (K020 VERIFIED after `b5c2375`); `flavor_catalog/audits/factcheck_status.md:106` (E009 still PARTIAL).
  - Recommended fix: Optional — no rewrite of the v0.3 tag (immutable on origin). For future master compile tags, source the V/P count from the compile report at tag-time rather than projecting the post-cleanup state.

- **R18-I2** severity:LOW tag:provenance
  - Title: Commit `37beabb` message reads "PKA-K020 initial draft" but the diff contains the B013 family files
  - Description: `git show --stat 37beabb` shows the commit message `flavor-catalog(wave8): PKA-K020 initial draft (SECONDARY)` but the actual content is `processes/secondary/beauty/B013.{tex,yaml}` + `references/B013/*` (12 files, 780 insertions). The K020 content actually landed in the next commit `bab5bd0` (correctly labeled `PKA-K020`), and a second B013-related commit `ebd066c` (correctly labeled `PKA-B013`) at 02:18:44 added 8 more `references/B013/*` snapshots. The mis-labeling is a commit-message typo only; no content is missing, and the eventual file tree contains all expected K020 and B013 files (verified at `flavor_catalog/processes/secondary/{beauty/B013,kaon/K020}.{tex,yaml}`). The MERGE_PLAN.md:311 commit list at R18 is therefore correct: both `37beabb` (B013 first deposit) and `bab5bd0` (K020) belong to R18.
  - Evidence: `git show --stat 37beabb` (filenames are B013); `git show --stat bab5bd0` (filenames are K020); commit timestamps `37beabb` 02:18:05, `bab5bd0` 02:20:06.
  - Recommended fix: None at present (history is immutable). For future PKA dispatch waves: cross-check the commit message ID matches the `git diff --stat` filenames before pushing.

- **R18-I3** severity:LOW tag:docs
  - Title: Consolidated `factcheck_status.md` was NOT regenerated for Wave-8 — SECONDARY rows live only in per-family addenda (recurrence of R17-I2 at the SECONDARY tier)
  - Description: At the R18 endpoint (commit `b5c2375`, end of Wave-8 close-out), `flavor_catalog/audits/factcheck_status.md` still contains only 75 rows in its consolidated per-process table at `:32-108` (the original DA-4-converged set). The 8 Wave-8 SECONDARY verdicts (K019/K020/K021/B007/B008/B013/B014 all VERIFIED + T014 VERIFIED) live exclusively in the per-family Wave-8 addendum sections: `audits/factcheck_kaon.md:254-313` (K019/K020/K021), `audits/factcheck_beauty.md:243-310` (B007/B008/B013/B014), `audits/factcheck_top_higgs_ew.md:391-427` (T014). The consolidated table's `## Summary` block at `factcheck_status.md:124-131` therefore still reads "75 process entries / 74 VERIFIED / 1 PARTIAL / 0 MISMATCH / 0 FAILED" — undercounting Wave-7 (5 entries) and Wave-8 (8 entries) by 13 rows. Same defect as R17-I2 (which flagged the v0.2 75-vs-80 gap). The current HEAD `factcheck_status.md:128-130` shows the table has now been brought up to date for the full Wave-1-through-Wave-9 set (89 rows: 88 VERIFIED + 1 PARTIAL E009), so this is a documentation-drift seam that was resolved later in the merge prep, but it was not resolved at the R18 endpoint. No numerical claim is wrong; the SECONDARY verdicts are correctly recorded in the per-family addenda.
  - Evidence: `git show flavor-catalog-v0.3:flavor_catalog/audits/factcheck_status.md` (75 rows); `flavor_catalog/audits/factcheck_kaon.md:254-313` (Wave-8 addendum with 3 SECONDARY rows); `flavor_catalog/audits/factcheck_beauty.md:243-310` (4 SECONDARY rows); `flavor_catalog/audits/factcheck_top_higgs_ew.md:391-427` (1 SECONDARY row); `flavor_catalog/master_compile_v03_report.md:51` ("SECONDARY fact-check status from Wave-8 addendum sections in `flavor_catalog/audits/factcheck_*.md`"). Cross-reference R17-I2.
  - Recommended fix: Optional — same recommendation as R17-I2(a): fold per-family Wave-N addendum rows back into the consolidated `factcheck_status.md` at every master-compile bump. The current HEAD already does this, so the recommendation is forward-looking for Wave-10+.

- **R18-I4** severity:INFO tag:docs
  - Title: SECONDARY tier rationale is documented in TWO places (per-YAML `priority_rationale` AND `PRIORITY_TIERS.md` §4 table) — desirable redundancy, but a future edit could drift one source
  - Description: The PI's "SECONDARY tier classification reasoning is documented" requirement from the R18 prompt is satisfied at TWO independent surfaces: (a) each YAML's one-line `priority_rationale` field (e.g. `K020.yaml:5` — "Deferred by DA-4...; Top-tier; charged-current LFV companion to K019. Lower implementation priority than Waves 1-7 PRIMARY entries"), and (b) the `PRIORITY_TIERS.md:90-99` Wave-8 §4 rationale table (per-entry "originally deferred" + "promoted in Wave-8" columns). The two sources agree pairwise for all 8 entries. The redundancy is desirable (the YAML is the per-process source of truth; the table is the wave-level overview), but a future re-promotion or de-promotion edit could drift one without the other. Documentation-only seam; not a defect.
  - Evidence: `flavor_catalog/processes/secondary/kaon/K020.yaml:4-6` and `K019.yaml:4-5`; `flavor_catalog/PRIORITY_TIERS.md:90-99`; cross-confirmed for the 6 other SECONDARY entries (B007/B008/B013/B014/K021/T014).
  - Recommended fix: None at present. Future SECONDARY waves: the wave runbook §6 reproducibility-note pattern (Wave-8 runbook does this correctly) ensures the new entries' YAML rationale and the PRIORITY_TIERS.md table row are landed in the same wave, minimizing drift risk.

## Verdict

**APPROVE** — Wave-8 cleanly executes the first Pattern-A SECONDARY-promotion wave. All 8 entries are OPUS-APPROVED (round-4 `signoff/round_004_index.md`), VERIFIED at current HEAD (K020 flipped PARTIAL → VERIFIED via the post-tag `6a2506c`/`b5c2375` cleanup, a clean metadata-only NA62 author-string alignment with PDG/K021 convention). Tier policy and repo-structure separation per `PRIORITY_TIERS.md` are honored across all 8 entries. K020 physics anchors (PDG `S010.29`/`S010.25`, Sher 2005 E865-only, NA62 2021) and K019 physics anchor (BNL E871 / Ambrose 1998 < 4.7e-12) match the literature digit-for-digit. Master compile v0.3 (148 pages, 796 KB, 88 entries) is tagged `flavor-catalog-v0.3`. Four LOW/INFO issues filed (R18-I1 cosmetic V/P count drift on the tag annotation; R18-I2 commit-message label typo on `37beabb`; R18-I3 consolidated factcheck table not regenerated at v0.3 — same as R17-I2 at SECONDARY tier; R18-I4 desirable doc redundancy with mild drift risk). None block the v0.3 milestone. Ready to proceed to R19 (Wave-9 collider_rs).
