You are agent1 fixing K010 (K_S→π⁰ee). Physics review .orchestration/runs/K010/agent2_out.md = PHYSICS-NEEDS-FIXES (1 SHOULD-FIX); code OK. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX: the a_S sign ambiguity isn't propagated — the verdict uses only a_S^eff=+|a_S|+Re(NP), but the experimental |a_S| leaves the sign free. Evaluate BOTH signs s_a=±1 (a_S^eff = s_a·|a_S| + δa_S) and either (a) use the conservative sign-envelope for the HARD verdict (the branch that is LEAST excluded, so we don't over-constrain), reporting both in diagnostics, OR (b) make the positive-branch assumption EXPLICIT in docstring/diagnostics with justification. Prefer (a) (sign-envelope). Keep RS proxy NEEDS-HUMAN-PHYSICS. Append-only (don't modify K008/K009 funcs). Run tests/constraints/ -q green. OUTPUT <=10 lines: which option, both-sign numbers, pytest counts.
