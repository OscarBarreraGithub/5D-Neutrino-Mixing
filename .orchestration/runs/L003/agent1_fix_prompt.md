You are agent1 fixing L003 (μ→e conversion in Al). Physics review .orchestration/runs/L003/agent2_out.md = PHYSICS-NEEDS-FIXES (2 SHOULD-FIX); code OK. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX:
1. Severity: the active limit (Mu2e Al 6.7e-17) is a PROJECTED/expected sensitivity, but base.py defines HARD as an OBSERVED experimental veto. Do NOT hard-veto present points on a future projection. Either (a) use SOFT severity for the projected Al limit, OR (b) use an OBSERVED bound for the HARD verdict (the YAML has SINDRUM Au observed 7e-13 — but that's a different target/nucleus; if you use it, translate/label correctly). Cleanest: report the projected-limit comparison as SOFT (sensitivity reach), and clearly document. Update L003.yaml/diagnostics to distinguish observed vs projected.
2. Interference: with only BR(μ→eγ) available the dipole/contact relative phase is unknown; the current constructive envelope can hard-fail ambiguous-phase points. Use a conservative treatment consistent with SOFT (report the allowed interval / fail only if the LEAST-excluded branch exceeds), and flag the phase as NEEDS-HUMAN-PHYSICS.
Reusable by L004/L005. Run tests/constraints/ -q green. OUTPUT <=10 lines: severity decision, interference handling, pytest counts.
