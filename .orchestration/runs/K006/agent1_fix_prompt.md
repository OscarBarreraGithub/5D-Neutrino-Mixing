You are agent1 fixing K006 (K_L→μμ) + module quarkConstraints/rare_kaon_dilepton.py. Reviews .orchestration/runs/K006/{agent2_out.md,agent3_out.md} = NEEDS-FIXES (SHOULD-FIX, no hard blockers). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX (SHOULD-FIX): the SM short-distance BR (~8.2e-10) is computed from hardcoded defaults and the test hardcodes it, while K006.yaml has only total BR + the <2.5e-9 SD bound — so the "YAML validation" claim is false. Choose ONE and implement cleanly: (a) add an explicit SM-SD theory anchor to flavor_catalog/processes/kaon/K006.yaml with provenance (source/year/url, the SM SD value + uncertainty) and load it via load_anchor, with the test deriving from the anchor; OR (b) clearly document the SM-SD as an internally-computed reference (cite the parametrization source in the docstring) and STOP claiming it is YAML-validated — adjust the test to recompute from the module, not assert a YAML number. Prefer (a) if a citable SM-SD value exists. Keep the NP proxy NEEDS-HUMAN-PHYSICS flag + the HARD SD-bound verdict. Isolated to K006.py, test, rare_kaon_dilepton.py, and K006.yaml (if option a). Run tests/constraints/ -q green. OUTPUT <=10 lines: which option, change, pytest counts.
