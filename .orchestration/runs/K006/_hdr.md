# Implement K006 — BR(K_L→μ⁺μ⁻). family=kaon. PIONEER: build shared s→dℓℓ module.
BUILD new quarkConstraints/rare_kaon_dilepton.py (ΔS=1 s→dℓℓ short-distance; K_L→μμ is long-distance dominated — the SHORT-distance piece is the constrained part, parametrize per Buras/Isidori). Wrap via NEW adapter physics_adapters/rare_kaon_dilepton.py. Design for reuse by K008 (K_L→π0ee), K009 (K_L→π0μμ), K010 (K_S→π0ee), K012 (K_S→μμ). Validate the SM short-distance number from K006.yaml. RS NP = documented Z/penguin proxy → NEEDS-HUMAN-PHYSICS. Budget from K006.yaml, HARD (or SOFT if LD-dominated; justify). Do NOT touch rare_kaon_snd.py (that's K→πνν).
