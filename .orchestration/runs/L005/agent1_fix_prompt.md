You are agent1 fixing a SHARED-MODULE normalization bug in quarkConstraints/mu_e_conversion.py, found via L005 but affecting L003/L004/L005 (all use this module). Reviews .orchestration/runs/L005/{agent2_out.md (2 BLOCKERS),agent3_out.md}. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX (BLOCKER 1 — units, affects the whole module):
The KKO (hep-ph/0203110) overlap integrals D, V^(p,n), S^(p,n) are DIMENSIONFUL (units of m_mu^(5/2)), but the code uses them as dimensionless in a GeV width — so the conversion rate is wrong by m_mu^5 (≈7.59e4). For Ti pure-dipole the code gives CR/BR=314 but the correct value is ≈0.00414. Fix the KKO normalization in mu_e_conversion.py so the conversion rate carries the correct m_mu^5 factor (match the standard KKO conversion-rate formula; verify Ti pure-dipole CR/BR ≈ 0.00414, and sanity-check Al/Au similarly).
FIX (BLOCKER 2 — L005 budget): L005.yaml/L005.py use 4.3e-12 but the SINDRUM II Ti observed limit is 6.1e-13 (KKO / Bernstein-Cooper). Update L005.yaml to the correct Ti limit 6.1e-13 with provenance and use it (HARD).
PROPAGATE: this core fix changes L003 (Al) and L004 (Au) conversion rates too. Update the EXPECTED/cross-check numbers in test_L003.py and test_L004.py (and any L003/L004 diagnostics that pin absolute rates) to the corrected normalization, so all three are physically correct and consistent. Do NOT change the pass/fail logic — just the corrected absolute normalization.
Keep RS proxy NEEDS-HUMAN-PHYSICS; conservative interference. Run `python -m pytest tests/constraints/primary/charged_lepton/ -q` and `tests/constraints/ -q` green. OUTPUT <=14 lines: the normalization fix, corrected Ti/Al/Au CR/BR numbers, L005 budget, which L003/L004 numbers changed, pytest counts.
