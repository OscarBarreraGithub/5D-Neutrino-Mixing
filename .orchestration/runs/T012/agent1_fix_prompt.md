You are agent1 fixing T012 (Z→cc̄) test. Code review .orchestration/runs/T012/agent3_out.md = CODE-NEEDS-FIXES (SHOULD-FIX: cross-check is SM-only, doesn't exercise shifted Zcc). Physics PHYSICS-OK. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX: add a test that manually computes dgL/dgR (the NP coupling shifts), calls the core quarkConstraints.zpole shifted_couplings + evaluate_quark_pseudo_observables for charm, and asserts R_c/A_c/ratio match the constraint's output for a shifted (non-SM) point — independent of the constraint's own adapter call. Isolated to test_T012.py. Run tests/constraints/primary/top_higgs_ew/test_T012.py -q + tests/constraints/ -q green. OUTPUT <=8 lines: change, the shifted R_c/A_c numbers, pytest counts.
