Proposed citable result: do **not** add a universal `m_t^2 B(c_u)` term to minimal SM-gauge RS `Zb_Lb_L`; in Casagrande minimal RS the standard large LH term is
`\delta g_{bL}^{min} = g_{bL}^{SM}[-m_Z^2/(2 Lambda_IR^2) * F_Q[2]^2/(3+2c_Q[2]) * (L - (5+2c_Q[2])/(2(3+2c_Q[2])))]`, with `g_{bL}^{SM}=-1/2+s_w^2/3`; no corresponding top-driven `g_R` term.

Source mapping: Casagrande et al. arXiv:0807.4937 Eq. `(ZbbRS)` gives this directly for `g_L^b`; Eqs. `(Zff),(gLR),(ZMA1),(ZMA2)` show that the fermion-admixture `m_f^2 B(c)` term uses `f=d` for `Zbb`, hence `m_b^2 B(c_d)`, while `m_t^2 B(c_u)` belongs to up-type/top couplings, not `b_L`. See also Carmona-Ponton-Santiago arXiv:1107.1500 Eq. `(33)`, which treats non-custodial `Zb_L` tree corrections as gauge-KK/profile dependent and adds fermion effects only by model-specific diagonalization; Agashe-Contino-Da Rold-Pomarol hep-ph/0605341 Eqs. `(18),(19)` make fermion/top-partner shifts representation dependent.

Convention: this is dimensionless in the repo’s `L_Z = g_Z Z_mu bbar gamma^mu(g_L P_L+g_R P_R)b`, so no extra `g_Z`; it adds to `z_delta_g_L_d[2,2]` only if the existing gauge builder has not already included the same profile term.

Sanity: vanishes for UV/light `F_Q[2]`, scales as `1/Lambda_IR^2`, positive `delta g_bL` for IR-localized third generation because `g_bL^SM<0` and the bracket is negative; typical size is per-mille to percent for few-TeV `Lambda_IR`.

Residual physicist decision: if the intended object is an exotic/custodial top-partner term, choose the representation/mixing coefficient first; there is no minimal non-custodial universal `m_t^2/m_b^2` Casagrande-B replacement to sign off. ZBB-PROPOSAL-DONE.