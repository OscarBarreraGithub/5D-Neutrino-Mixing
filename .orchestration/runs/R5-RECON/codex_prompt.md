R5 FACTOR-2 RECONCILIATION (codex). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. A prior review found that quarkConstraints/deltaf2.py (lines ~722,910) uses a Q1 M12 normalization that is HALF the paper-mode path (quarkConstraints/paper_0710_1869/eft_deltaf2/hadronic.py:762, observables.py:561): K Q1 = 0.004426 vs 0.008851 GeV^3 (factor 2, same for Bd/D). Opus separately found the CONSTRAINT-FACING results match PDG (eps_K^SM=2.161e-3, Dm's). RESOLVE definitively whether this factor-2 is a BUG or a CONVENTION difference. Do NOT modify code.
1. WHICH PATH do the 5 fully-rigorous DeltaF=2 anchor constraints (K001 eps_K, K002 Dm_K, B001 Dm_d, B003 Dm_s, C001 D-mixing) ACTUALLY call at runtime — quarkConstraints/deltaf2.py (the `evaluate_*`/`compute_m12*` functions) or the paper_0710_1869 path? Trace the imports/call chain from each constraint + its adapter.
2. Does the path the constraints USE reproduce PDG? Recompute eps_K^SM, Dm_K, Dm_d^SM, Dm_s^SM, x_D through the ACTUAL constraint path and compare to PDG (eps_K 2.16e-3, Dm_s 1.17e-11 GeV, etc.). If it matches, that path's normalization is the correct/used one.
3. IDENTIFY the factor-2 source: is it the M12 = <Mbar|H|M>/(2 m_M) vs <...> definition, the Q1 operator normalization (2/3 color / N_c factor), or a genuine inconsistency? Determine if deltaf2.py and the paper-mode path are TWO independent conventions (both self-consistent) or if one is wrong.
4. Do ANY constraints MIX the two paths (use deltaf2.py M12 but paper-mode matrix elements, or vice versa) in a way that would actually be off by 2? This is the real risk.
VERDICT: BUG (one path wrong, constraints affected) -> specify the fix; or CONVENTION (constraints consistently use the PDG-validated path; the other path is a separate convention) -> specify the documentation note. 
OUTPUT (<=16 lines): the call-chain (which path each anchor uses), the recomputed PDG comparison through the USED path, the factor-2 source, whether any constraint mixes paths, the verdict. End with: R5-BUG or R5-CONVENTION-OK.
