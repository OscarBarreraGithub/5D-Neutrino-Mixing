You are agent1 fixing L009 (τ→3μ) + its adapter quarkConstraints/lfv_three_body.py / physics_adapters/lfv_three_body_tau.py. Reviews .orchestration/runs/L009/{agent2_out.md,agent3_out.md} = NEEDS-FIXES (CODE BLOCKER + SHOULD-FIX). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX:
1. (BLOCKER) The τ adapter mislabels flavors: matrix/contact inputs without explicit flavors fall back to the shared core's μ→e default while diagnostics claim τ,μ (probe used overlap 0.09+0.01j instead of the τμ 0.003+0.004j → 328x wrong). PIN the flavors: normalize contact inputs to a τ→3μ-specific proxy input (or inject initial_flavor="tau", final_flavor="mu" before the core call) so eμ aliases are NOT silently interpreted as τμ. Reject/ignore eμ-named spurion keys for L009. Add a matrix-pinning regression test asserting the τμ overlap is used (not the μe default).
2. (SHOULD-FIX) Dipole-only explicit BR currently requires m_kk via a zero-contact placeholder (KeyError without it). Allow pure-dipole evaluation without m_kk (use a harmless zero-contact placeholder/reference scale).
Keep the interference (constructive envelope) + NEEDS-HUMAN-PHYSICS proxy. Append-only (don't modify L002's μ→3e funcs). Run tests/constraints/ -q green. OUTPUT <=12 lines: flavor-pin fix, the corrected τμ number, pytest counts.
