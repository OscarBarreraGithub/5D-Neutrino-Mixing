# Custodial RS-EW PR1 — COMPLETE the partial implementation (codex, gpt-5.x xhigh)
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. A prior codex run implemented most of the custodial branch but exited early (rc=1) BEFORE finishing T010/T011 + tests + summary. The CORE is already in the working tree (uncommitted): `quarkConstraints/rs_ew_couplings.py` (+227, custodial logic), `quarkConstraints/oblique_stu.py` (+75, custodial T), `flavor_catalog_constraints/physics_adapters/oblique_stu.py`, `flavor_catalog_constraints/primary/top_higgs_ew/EW001.py` (_resolve_ew_model), `rs_ew_builder.py`, `point_builder.py`. Existing tests still pass (16/16); imports clean (103 constraints, 0 import failures).

YOUR JOB: FIRST read the current uncommitted diff (`git diff`) to see exactly what's already implemented, then COMPLETE the rest per the DUAL-APPROVED plan `.orchestration/runs/CUSTODIAL-RESEARCH/plan_rev_codex_out.md` + spec `.orchestration/runs/CUSTODIAL-RESEARCH/CORRECTED_PRESCRIPTION.md`. Do NOT rewrite what's already correct; fill the gaps.

LIKELY-REMAINING (verify against the diff, do what's missing):
1. T010/T011 `_rs_zbb_matching_diagnostics`: make `custodial_variant_deferred` DATA-DRIVEN (False when custodial_protection_included in metadata); keep the top-partner-loop deferred flag. Confirm the harness tags the custodial tree ACTIVE (rigorous), not partial.
2. Verify the core custodial logic already in rs_ew_couplings is CORRECT per spec: custodial mode zeros ONLY protected DIAGONAL down-left z_delta_l_d[0,0],[1,1],[2,2]; OFF-DIAGONAL (i!=j) entries LEFT at minimal (T014 FCNC unchanged); residual z_delta_l_d[2,2]=kappa_b/L*minimal[2,2]; b_R z_delta_r_d[2,2]=0; metadata set. Fix if the partial got any of this wrong.
3. ADD ALL TESTS (the plan's list): minimal byte-identity (default ew_model => identical arrays + config hash); custodial diagonal-zeroed + residual; **T014 regression: off-diagonal z_delta_g_L_d[bs,bd,sd] and T014 result UNCHANGED minimal-vs-custodial**; custodial oblique T tiny + S unchanged + U=0; EW001 ew_model plumbing; T010/T011 active-not-deferred under custodial; **benchmark ~15 TeV point that minimal Z->bb vetoes now SURVIVES custodial (T010/T011 pass)**; determinism.
4. `python -m pytest tests/ -q` must end GREEN.

OUTPUT (<=18 lines): what was already done vs what you completed; confirm diagonal-only zeroing + T014 unchanged (numbers) + residual + oblique T-swap/S-unchanged + data-driven deferred flag + minimal byte-identity + the 15 TeV-survives-custodial benchmark; pytest counts. End with: CUSTODIAL-IMPL-DONE.
