# RESOLVE the custodial Zb_L protection contradiction (codex, gpt-5.x xhigh) — physics only, no code
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. An expert (ChatGPT) answered our custodial problem statement: `.orchestration/runs/CUSTODIAL-RESEARCH/chatgpt_answer.md`. It CONTRADICTS our earlier Opus panel finding on the single most important point. Resolve DEFINITIVELY from the primary literature; do not defer to either side.

## THE CRUX (resolve this first, with primary-source justification)
- **ChatGPT:** custodial P_LR protects BOTH the leading **gauge-wavefunction** Zb_L piece (the F(c_Q3)^2-enhanced, compositeness-driven one) AND the fermion-mixing piece. So in our code you must zero the leading gauge piece too; zeroing only the m_b^2/Lambda_IR^2 admixture while keeping the full minimal F(c_Q3) gauge piece would OVERCONSTRAIN the custodial scan.
- **Our earlier Opus panel:** P_LR protects ONLY the fermion-mixing admixture; the gauge-wavefunction piece is NOT protected (survives, reduced but nonzero).

WHO IS RIGHT? Resolve from **Agashe-Contino-Da Rold-Pomarol hep-ph/0605341** (the protection theorem: WHICH corrections to the b_L Z-vertex does it protect — does it protect the leading gauge/compositeness-driven delta g_L^b, or only fermion-mixing?). Decisive physics consideration to address: the ENTIRE PURPOSE of ACDRP custodial is to solve the RS Zbb problem, whose DOMINANT correction in minimal RS is precisely the gauge/compositeness-driven delta g_L^b ∝ F(c_Q3)^2 (volume/L-enhanced). If custodial protected only the tiny m_b^2 fermion-mixing piece, it would NOT solve the Zbb problem — so does that not imply ChatGPT is right that the gauge piece is protected? Examine ACDRP's actual theorem (the corrections it covers are the non-universal vertex corrections, which the gauge-KK profile piece IS) and state definitively. NOTE our repo cross-check found the m_b^2 full-flavor-sum admixture currently dominates our delta g_L^b ~29x over the gauge piece — address how that interacts with the protection (is the admixture also protected? ChatGPT says yes if the bottom Yukawa respects P_LR).

## ALSO VALIDATE the rest of ChatGPT's prescription (flag any error)
- All-generation (2,2)_{2/3} bidoublet + mass-basis rotation (vs 3rd-gen-only).
- The residual model delta g_L,residual^b ~ kappa_b (1/L) delta g_L,gauge,minimal^b, kappa_b=O(1) (or zero-in-ideal-limit + flag).
- T_cust = -pi/(4 c_W^2 L) v^2/M_KK^2 (the -1/(2L^2) suppression) — already cross-checked earlier as defensible; re-confirm.
- S form: ChatGPT gives Delta S ~= 2 pi (v^2/M_KK^2)(1 - 1/L) and claims S (not T) becomes the dominant tree oblique. Validate this S coefficient/claim against the literature and against our repo's current c_S.
- b_R default delta g_R^b ~= 0; A_FB^b only via explicit b_R model.
- One-loop top-partner: flag now; Delta T_loop ~ O(0.1), delta g_L^b|loop ~ 1e-3 at 2-3 TeV.
- M_KK convention (1/R_IR vs m_1 ~= 2.4 M_KK) — tie to our repo's physical-vs-Lambda_IR finding.

## OUTPUT (<=16 lines)
The DEFINITIVE verdict on the gauge-piece protection (ChatGPT vs our Opus panel) with the ACDRP justification; whether our earlier Opus finding was WRONG; validation/correction of each other ChatGPT claim; the corrected FINAL custodial prescription to implement. End with EXACTLY ONE line: `CUSTODIAL-RESOLVE: CHATGPT-CORRECT` or `CUSTODIAL-RESOLVE: OPUS-PANEL-CORRECT` or `CUSTODIAL-RESOLVE: BOTH-PARTLY` (+ one-line summary of the corrected prescription).
