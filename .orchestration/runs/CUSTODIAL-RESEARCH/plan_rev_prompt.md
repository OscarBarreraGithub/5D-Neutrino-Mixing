# Custodial plan REVISION (codex, gpt-5.x xhigh) — address the review's blocking error + gaps. PLAN ONLY, no code.
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Your prior plan `.orchestration/runs/CUSTODIAL-RESEARCH/plan_codex_out.md` got NEEDS-FIXES. Revise it to fix EXACTLY these (the corrected spec is `.orchestration/runs/CUSTODIAL-RESEARCH/CORRECTED_PRESCRIPTION.md`):

1. **BLOCKING - T014 / down-FCNC.** Your `z_delta_l_d[:,:]=0` zeros the WHOLE 3x3 down-left block, but T014 (secondary/top_higgs_ew/T014.py ~L450-461) reads the OFF-DIAGONAL z_delta_g_L_d[i,j] for Z->bs/bd/sd. Custodial P_LR protects the flavor-DIAGONAL non-universal Zb_L vertex, NOT down-sector FCNC. FIX: zero ONLY the protected DIAGONAL down-left entries (the universal coupling shifts) and LEAVE the off-diagonal FCNC entries at their minimal values; add a T014 regression test asserting off-diagonal Z->down-FCNC is UNCHANGED by custodial. (If you believe custodial also modifies the off-diagonals, do NOT assume it - flag it as a deferred custodial-FCNC question, keep off-diagonals minimal for PR1.)
2. **Oblique ew_model plumbing.** evaluate_rs_oblique_proxy / rs_minimal_oblique_proxy do NOT accept ew_model, and EW001.evaluate passes no model and has no access to rs_ew_couplings.metadata. Specify the FULL plumbing: add an ew_model (or custodial T-coefficient) kwarg through rs_minimal_oblique_proxy -> evaluate_rs_oblique_proxy, and specify exactly how EW001 obtains ew_model (new kwarg/extra, or read a custodial flag) - this is new code that must be scoped.
3. **Data-driven deferred flag.** T010/T011 hardcode `"custodial_variant_deferred": True` (~L650) and the builder hardcodes metadata `ew_model="minimal_rs"` (~L676). Specify making these data-driven: builder sets ew_model + custodial_protection_included; T010/T011 read it and STOP marking custodial-active as deferred (so the harness tags the custodial tree correctly, not partial).
4. **Residual definition.** Confirm the kappa_b*(1/L) residual multiplies the saved minimal gauge+admixture SUM (delta g_L,minimal^b), and applies only to the protected DIAGONAL b entry [2,2] (consistent with fix #1).

Keep everything the review PASSED (residual-capture ordering, minimal byte-identity gating, c_S unchanged / T-swap, b_R default-zero, top-partner deferred-flag, omissions metadata, tests). Re-output the FULL revised plan (file+function level), with the T014 diagonal-only zeroing and the oblique plumbing explicit.

OUTPUT (<=20 lines): the revised plan; explicitly call out the 4 fixes. End with: CUSTODIAL-PLAN-REV-DONE.
