# Custodial branch — IMPLEMENTATION PLAN (codex author, gpt-5.x xhigh). PLAN ONLY, no code.
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Produce a concrete, reviewable implementation plan for the custodial mode, following the DUAL-CONSENSUS corrected spec `.orchestration/runs/CUSTODIAL-RESEARCH/CORRECTED_PRESCRIPTION.md` (read it fully). A second agent will review this plan; both must APPROVE before any code. Read the target files: `quarkConstraints/rs_ew_couplings.py` (the gauge z_delta_g_L/R_d build ~505-551 and the m_b^2 Zbb admixture ~704-744), `quarkConstraints/rs_ew_spectrum.py` (a(c)/Omega), `flavor_catalog_constraints/physics_adapters/oblique_stu.py` (the S/T proxy + minimal_rs_t_coefficient), `flavor_catalog_constraints/primary/top_higgs_ew/{T010,T011,EW001}.py`, and how `build_from_rs_ew_inputs`/`build_rs_ew_extras` thread options.

## THE PLAN MUST SPECIFY (concretely, file+function level)
1. **The custodial switch**: a config flag (e.g. `ew_model="minimal"|"custodial_rs_plr"` + representation metadata qL_rep/tR_rep/bR_rep/protect_scope/bR_strategy/kappa_b/include_top_partner_loops) — where it lives (ScanConfig? build_from_rs_ew_inputs kwarg?) and how it threads to rs_ew_couplings + oblique_stu. Default OFF => byte-identical minimal behavior.
2. **Zb_L zeroing (the corrected core)**: in custodial mode, zero BOTH the leading gauge-profile entry AND the m_b^2 admixture in z_delta_g_L/R_d for the protected down-LEFT entries (all-gen bidoublet => all 3 down-left, in mass basis), and substitute the residual δg_L^b ≈ κ_b·(1/L)·δg_L,minimal (κ_b=O(1), default could be 0 + breaking flag). EXACTLY which array entries, how the residual is formed, how to keep the minimal-mode path untouched.
3. **Oblique (EW001/oblique_stu)**: KEEP c_S (physical-mass convention) unchanged; replace ONLY the T coefficient minimal πL/(2c_W²) -> custodial −π/(4c_W²L); U=0. Where (oblique_stu) and how gated by the flag.
4. **b_R**: δg_R^b≈0 default; optional explicit bR model behind bR_strategy.
5. **One-loop top-partner**: a FLAG + a place to add ΔT_loop and δg_L^b|loop later; default deferred-but-flagged (diagnostics must say so).
6. **Honest diagnostics**: metadata fields recording ew_model, reps, protect_scope, and the explicit omissions (SU(2)_R tower, custodian spectrum, exact NC mixing, BKT, deferred loops). T010/T011/EW001 already consume the slots; confirm no constraint silently mis-tags.
7. **Tests**: custodial-mode unit tests (Zb_L tree ~0 protected => T010/T011 relax/pass where minimal vetoed; custodial T tiny; S unchanged; minimal-mode byte-identical; determinism); a sanity that a benchmark point excluded by minimal Z->bb at ~15 TeV now survives custodial.
8. **Scope/sequencing**: smallest correct first PR (tree-level custodial switch + Zb_L zeroing + oblique T swap + flags), with the one-loop top-partner numerics explicitly deferred.

## OUTPUT (<=20 lines)
The plan: each file/function to touch, the flag design, the exact Zb_L-zeroing + residual mechanism, the oblique T swap, the b_R/loop flags, the diagnostics, the test list, and the PR sequencing. Flag any risk (e.g. mass-basis rotation of the protection, double-zeroing, minimal-path regression). End with: CUSTODIAL-PLAN-DONE.
