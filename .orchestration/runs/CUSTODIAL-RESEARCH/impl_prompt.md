# Custodial RS-EW branch — IMPLEMENT PR1 (codex author, gpt-5.x xhigh). Build code+tests.
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Implement EXACTLY the DUAL-APPROVED plan `.orchestration/runs/CUSTODIAL-RESEARCH/plan_rev_codex_out.md` (and the physics spec `.orchestration/runs/CUSTODIAL-RESEARCH/CORRECTED_PRESCRIPTION.md`). First a SHORT plan, then implement code + tests. A codex reviewer AND an Opus reviewer will independently review; BOTH must APPROVE before commit. PR1 = TREE-LEVEL custodial proxy ONLY (one-loop top-partner numerics + custodial-FCNC modeling are DEFERRED to PR2, flagged not computed).

BUILD (per the approved plan, file+function level):
1. Allow `ew_model="custodial_rs_plr"` in `build_from_rs_ew_inputs` / `build_rs_ew_extras` / `build_rs_ew_couplings` (default stays `"minimal_rs"`, BYTE-IDENTICAL). Add custodial kwargs/metadata: qL_rep, tR_rep, bR_rep, protect_scope (default all-gen down-left diagonal), bR_strategy="elementary_zero", kappa_b=0.0, custodial_PLR_breaking_residual=False, include_top_partner_loops=False.
2. In `rs_ew_couplings.build_rs_ew_couplings`: build minimal arrays exactly as today (incl. optional Casagrande Zbb admixture at [2,2]); SAVE `minimal_z_delta_l_d_full` AFTER that. Then in custodial mode: zero ONLY the protected DIAGONAL down-left entries z_delta_l_d[0,0],[1,1],[2,2]; LEAVE all off-diagonal (i!=j) entries at minimal (T014 FCNC must be unchanged). Residual: z_delta_l_d[2,2] = kappa_b*(1/L)*minimal_z_delta_l_d_full[2,2] (else exact 0). b_R: bR_strategy="elementary_zero" sets z_delta_r_d[2,2]=0 only. Hermiticity preserved.
3. Metadata: set ew_model, custodial_protection_included=True, protected-diagonal mask, residual source/value, bR strategy, top_partner_loops="deferred", and omissions (SU2_R_tower=False, custodian_spectrum=False, exact_NC_mixing=False, BKT=False).
4. Oblique: in `physics_adapters/oblique_stu.py` + `quarkConstraints` oblique, add `ew_model` kwarg through rs_minimal_oblique_proxy -> evaluate_rs_oblique_proxy; keep c_S and U=0; custodial T coefficient = -pi/(4*cW^2*L). EW001.evaluate: add `_resolve_ew_model(point)` (read rs_ew_couplings.metadata["ew_model"], else top-level, else minimal_rs) and pass it; report ew_model in diagnostics.
5. T010/T011 `_rs_zbb_matching_diagnostics`: make `custodial_variant_deferred` data-driven (False when custodial_protection_included); keep top-partner deferred flag.
6. Remove the minimal-only hard rejection in rs_ew_builder; validate supported models.

TESTS (add, all must pass): minimal byte-identity (ew_model default => identical arrays + config hash); custodial Zb_L diagonal zeroed + residual; **T014 regression: off-diagonal z_delta_g_L_d[bs,bd,sd] and T014 results UNCHANGED vs minimal**; custodial oblique T tiny + S unchanged + U=0; EW001 ew_model plumbing; T010/T011 tagged active (not deferred) under custodial; a benchmark ~15 TeV point that minimal Z->bb vetoes now SURVIVES custodial (T010/T011 pass); determinism. `python -m pytest tests/ -q` green.

OUTPUT (<=18 lines): short plan; files/functions changed; the diagonal-only zeroing + residual + T014-unchanged confirmation (with numbers); oblique T-swap + S-unchanged; data-driven deferred flag; minimal byte-identity; the 15 TeV benchmark survive-under-custodial result; pytest counts. End with: CUSTODIAL-IMPL-DONE.
