You are agent1 (codex implementer). Implement constraint K004 = BR(K+ -> pi+ nu nubar), the "golden" short-distance-dominated rare kaon mode. Unlike the ΔF=2 wave, the repo has NO ΔS=1 s->d nu nubar machinery — you must BUILD it (correctly, from established theory), behind the physics-adapter boundary. Two codex reviewers (physics + code) and an Opus reviewer will check you, so be rigorous and cite your physics. First write a SHORT plan, then implement. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing (you are -C'd in).

STUDY FIRST (read, don't modify): the gold-standard committed example flavor_catalog_constraints/primary/kaon/K001.py + test (mirror its structure, anchor loading, real-number contract, adapter boundary); the scaffold (base.py, anchors.py, registry.py, point_builder.py, TEMPLATE.py); the catalog sidecar flavor_catalog/processes/kaon/K004.yaml AND its .tex writeup (source of truth: NA62 measured BR, SM prediction, and the RS context); existing RS machinery you can reuse for inputs (warpConfig/, quarkConstraints/ for CKM/couplings conventions, qcd/ if relevant).

PHYSICS TO BUILD (K+ -> pi+ nu nubar):
- SM short-distance: BR = kappa_+ * [ (Im(lambda_t)/lambda^5 * X_t)^2 + (Re(lambda_c)/lambda * P_c + Re(lambda_t)/lambda^5 * X_t)^2 ], with the Inami-Lim top function X_t, charm contribution P_c, CKM factors lambda_q = V_qs* V_qd, and the hadronic factor kappa_+ extracted from K_l3 (isospin-rotated). Use the standard Buras/Brod-Gorbahn-Stamou parametrization. Take numerical inputs (kappa_+, P_c, X_t or m_t, CKM) from the K004.yaml anchor / .tex where given; clearly cite any constant you introduce and its source. Reproduce the SM BR (~8e-11) as a validation check.
- RS new physics: KK gauge-boson / Z-prime exchange generates an effective s->d nu nubar operator; in anarchic RS the dominant effect is a shift to the effective Z-d-s coupling / the X function from KK modes. Implement the leading NP contribution to the s->d nu nubar Wilson coefficient from the mass-basis couplings available on the ParameterPoint (analogous to how the ΔF=2 path uses quark_mass_basis_couplings + KK masses). If the precise RS matching is uncertain, implement the defensible leading form and DOCUMENT the assumption explicitly for reviewers.
- Constraint verdict: compare predicted BR (SM+NP) against the experimental anchor with an uncertainty-aware budget (NA62 measurement + SM theory error), severity HARD. Follow the K001 convention for uncertainty-aware budgets.

IMPLEMENTATION RULES:
- Put the new physics in a NEW module (e.g. quarkConstraints/rare_kaon_snd.py or similar) OR a clearly-scoped new function set — do NOT shove it into deltaf2.py (that's ΔF=2). Wrap it via a NEW adapter module flavor_catalog_constraints/physics_adapters/rare_kaon.py (append-only boundary; constraints never import physics directly).
- K004.py: typed anchor from K004.yaml (no hardcoded experimental numbers), evaluate() reaching physics only via the adapter, real-number ConstraintResult fields (complex in diagnostics), missing-couplings non-crashing, informative notes citing the SM parametrization + RS assumption.
- Tests tests/constraints/primary/kaon/test_K004.py: registration, anchor matches yaml, SM-limit BR reproduces the known ~8e-11 (numerical validation), a safe point passes and a large-NP point fails, evaluate pure.
- Keep changes ISOLATED to: the new physics module, the new adapter, K004.py, its test. Do NOT modify other constraints or deltaf2.py.
- If any required piece genuinely cannot be grounded in established physics with the available inputs, implement what you can and explicitly mark the gap NEEDS-HUMAN-PHYSICS in the docstring + your output, rather than inventing numbers.
- Run `python -m pytest tests/constraints/ -q` until green.

OUTPUT (stdout, concise, <=20 lines): plan; the SM parametrization + constants used (with sources/citations); the RS NP matching form + any assumption; SM-limit BR validation number; budget value + source; files created; numerical cross-check/test numbers; pytest counts; git diff --stat; and explicitly list anything marked NEEDS-HUMAN-PHYSICS. Do not paste full files.
