You are agent1 fixing K004 (BR(K+->pi+nunu)). Read the code review .orchestration/runs/K004/agent3_out.md (verdict CODE-NEEDS-FIXES). Physics review .orchestration/runs/K004/agent2_out.md was PHYSICS-OK (the RS NP proxy + NEEDS-HUMAN-PHYSICS flag are accepted — do NOT remove them; keep the documented proxy). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX:
1. (BLOCKER) Anchor loading in K004.py bypasses the scaffold's load_anchor (it uses load_pdg_block/find_block directly). Route BOTH the experimental BR anchor and the SM-prediction anchor through the scaffold load_anchor (extend its candidate-key handling if needed) so loading is schema-flex, typed, and fails loud on missing/mismatch. No hardcoded experimental/SM numbers in K004.py.
2. (SHOULD-FIX) tests/constraints/primary/kaon/test_K004.py hardcodes anchor-derived budget/SM constants; derive them from K004.yaml / the anchor object instead (keep the SM-limit BR~8.5e-11 numerical validation).
IGNORE the "isolation not clean" blocker about B001.yaml and untracked .orchestration/runs/K004 — the B001.yaml change has been committed separately and the runs dir is the expected audit trail; they are NOT K004 concerns. Do NOT touch B001 or other constraints.
Optional NITs from physics (do if quick): add the (1+Delta_EM)=0.997 factor to the SM BR; correct the doc citation (the 8.60±0.42e-11 SM anchor is Buras-Venturini 2022, not BGS 2021 which quotes 7.73e-11).
Keep changes ISOLATED to K004.py, its test, and (only if needed for the EM factor) quarkConstraints/rare_kaon_snd.py + the adapter. Run `python -m pytest tests/constraints/ -q` until green.
OUTPUT (<=12 lines): what changed per item, confirm load_anchor now used, SM-limit BR after any EM-factor change, pytest counts, git diff --stat. No full files.
