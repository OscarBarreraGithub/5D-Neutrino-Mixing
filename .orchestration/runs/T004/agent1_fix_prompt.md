You are agent1 fixing T004 (t→uγ). Physics review .orchestration/runs/T004/agent2_out.md = PHYSICS-NEEDS-FIXES (1 SHOULD-FIX, chirality-tight budget); code OK. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX: the budget always uses the LH limit (0.85e-5), too tight for pure-RH points (RH limit 1.2e-5). Apply the SAME treatment already accepted for T002: document the conservative chirality-tight LH-limit choice as explicit _BUDGET_POLICY in the code, and retain the RH (and CMS observed) limits in diagnostics. (If a per-chirality limit selection is cleaner and matches the catalog, that's fine too — but the documented-conservative-policy approach used by T002 is acceptable.) Keep RS proxy NEEDS-HUMAN-PHYSICS. Isolated to T004.py (+ test if needed). Run tests/constraints/ -q green. OUTPUT <=8 lines: change, budget policy, pytest counts.
