# W2 PHASE 4 — PLAN ONLY (codex, gpt-5.x xhigh). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Produce a concrete IMPLEMENTATION PLAN (NO code) for Phase 4 (lepton neutral currents) of the approved design `.orchestration/rs_ew_sector_design_CONSENSUS.md` (§5 item 4; §1,§4,§6). Codex + Opus will dual-review this PLAN before implementation. WRITE the plan to `.orchestration/runs/W2-P4/plan.md` (be concrete; each line can be a dense paragraph).

PHASE 4 SCOPE (per design §5.4): fill `lepton_mass_basis_couplings`; build charged-lepton AND active-neutrino mass-basis Z matrices (same `s_Z g_A^SM (m_Z^2/M_KK^2) U†diag(a(c)-a_ref)U` form as the 3a quark builder, now for leptons); build the LFV neutral contacts WITH the lepton δg_l (completing the full `(g_q^SM+δg_q)(g_l^SM+δg_l)−SM` product that 3a deferred — and now the heavy-vector lepton coupling g_Vl too, since lepton profiles exist); and REWIRE: Z-LFV (T015/T016/T017), LFV rare K (K019/K020/K021), LFV rare charm (C006/C008), μ→3e (L002), τ→3ℓ (L009), μ-e conversion (L003/L004/L005), and b→sνν (B022/B023, K004/K005 K→πνν).

THE PLAN MUST SPECIFY:
1. **Builder/inputs**: where the lepton c-values (c_L, c_E[3], c_N) + charged-lepton rotations U_e_{L,R} + PMNS + Majorana/Dirac neutrino structure come from — i.e. the LEPTON Yukawa fit. Read the `yukawa/` module (`compute_all_yukawas` / its result) + `neutrinos/` to find the exact objects (c-values, PMNS, U_e). State the new builder path (extend `build_from_rs_ew_inputs` or a lepton-aware variant) and the new sweep inputs the quark-only path lacks.
2. **Lepton Z matrices + neutrino**: charged-lepton z_delta_g_{L,R}_e via U_e and c_L/c_E; active-neutrino z_delta_g (LH, shares c_L) for the νν channels; store in `lepton_mass_basis_couplings` (dimensionless additive, zpole convention, no extra g_Z). Hermiticity; PMNS/charged-lepton-basis handling (stay in charged-lepton mass basis — avoid double rotation).
3. **Contacts completion**: the LFV neutral contacts now carry δg_l (off-diagonal lepton) → drives Z→ℓ_iℓ_j and LFV semileptonic; and the heavy-vector lepton coupling g_Vl from lepton profiles. State how this updates `rs_semileptonic_wilsons` / a new `rs_lfv_*` bundle and whether the quark-NP×lepton-SM terms from 3a are superseded or augmented (no double count).
4. **Rewire map**: per constraint (T015/T016/T017, K019/K020/K021, C006/C008, L002, L009, L003/L004/L005, B022/B023, K004/K005): current proxy → new rigorous lepton/ν input → formula; SM side unchanged; remove NEEDS-HUMAN only where now rigorous (note what stays PARTIAL, e.g. μ-e conversion overlaps, LFV 3-body box/photon pieces, Majorana phases).
5. **Sub-step breakdown**: independently-testable/reviewable sub-steps (e.g. 4a lepton builder+matrices+contacts no-rewire; 4b Z-LFV; 4c LFV rare K/charm + LFV 3-body + μ-e conv; 4d b→sνν / K→πνν). Order + what each touches.
6. **Validation**: universal-c (lepton) ⇒ δg_l=0 ⇒ LFV contacts 0 ⇒ rewired constraints recover SM; a known RS LFV scaling sanity (e.g. μ→eγ-adjacent); each rewired constraint SM-limit unchanged; full suite green; cross-checks independent of adapters.
7. **Risks**: PMNS/Majorana basis double-rotation; neutrino Dirac-vs-Majorana coupling; reusing quark a_ref for leptons (should leptons have their own a_ref?); double-count between 3a quark-NP×lepton-SM and 4 full product; which pieces remain genuinely loop/NEEDS-HUMAN (μ-e conversion dipole, LFV box).

OUTPUT (<=20 lines): the lepton-input source (exact objects/module), the sub-step breakdown, the rewire map summary, the validation checks, the top risks (esp. a_ref-for-leptons and 3a/4 double-count). Confirm plan.md written. PLAN ONLY.
