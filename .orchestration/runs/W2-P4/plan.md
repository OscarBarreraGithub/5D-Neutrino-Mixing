1. Phase 4 target: promote lepton neutral currents from diagnostic proxies to tree-level RS-EW inputs by filling `lepton_mass_basis_couplings`, adding charged-lepton and active-neutrino Z matrices to `rs_ew_couplings`, completing full neutral contacts, and rewiring only the listed LFV and `nunu` consumers while leaving all SM anchor/normalization code unchanged.
2. Exact lepton source: use `yukawa.compute_yukawas.compute_all_yukawas()` and its `YukawaResult`; read `c_L`, `c_E`, `c_N`, `M_N`, `lightest_nu_mass`, `ordering`, `majorana_alpha`, `majorana_beta`, `k`, and `v` from `YukawaResult.params` only, not top-level result fields; outputs include `Y_E`, `Y_E_bar`, `Y_N`, `Y_N_bar`, unbarred `Y_N_matrix = get_pmns(ordering, alpha, beta) @ diag(Y_N)`, `f_L`, `f_E`, `f_N`, `f_N_UV`, and `epsilon`; `Y_E_bar`/`Y_N_bar` are `(3,)` vectors, so callers must build `np.diag(...)` and PMNS-dressed matrix forms explicitly; `neutrinos.neutrinoValues.get_pmns()` supplies the PMNS matrix with NuFIT Dirac phase plus optional Majorana phases.
3. Charged-lepton rotations: the current lepton Yukawa fit is diagonal charged-lepton inversion (`yukawa/charged_lepton.py`), so Phase 4 v1 must store `U_e_L = U_e_R = I_3` explicitly, treat the current basis as charged-lepton mass basis, and leave a future non-diagonal `Y_E_bar[3,3]`/`U_e_{L,R}` hook without rotating PMNS a second time.
4. Builder path: extend `flavor_catalog_constraints.point_builder.build_from_rs_ew_inputs` plus `flavor_catalog_constraints.rs_ew_builder.build_rs_ew_extras` with `lepton_yukawa_result` or `lepton_sweep_inputs`; if raw inputs are given call `compute_all_yukawas()`, otherwise consume the typed result; new sweep inputs missing from the quark-only path are `c_L`, `c_E[3]`, `c_N`, `M_N`/`MN_over_k`, `lightest_nu_mass`, `ordering`, `majorana_alpha`, `majorana_beta`, and optional future `U_e_{L,R}`/direct `Y_N_matrix`, in addition to existing `Lambda_IR`, `k`, and EW truncation inputs.
5. `lepton_mass_basis_couplings`: add a typed `RSLeptonMassBasisCouplings` under the existing placeholder key, storing broadcast real `c_L[3]`, `c_E[3]`, `c_N[3]`, overlaps, `Y_E_bar_vector`, `Y_E_bar_matrix = np.diag(Y_E_bar)`, `Y_N_bar_vector`, unbarred `Y_N_matrix`, derived `Y_N_bar_matrix = pmns @ np.diag(Y_N_bar)`/equivalent `2*k*Y_N_matrix` when that relation is enforced, `pmns`, `U_e_L`, `U_e_R`, `U_nu_L`/basis metadata, `lfv_dipole_spurion = Y_N_bar_matrix Y_N_bar_matrix^dagger`, and matching-status diagnostics.
6. Lepton Z matrices: extend `quarkConstraints.rs_ew_couplings.build_rs_ew_couplings` or a lepton-aware sibling to build `z_delta_g_L_e`, `z_delta_g_R_e`, and `z_delta_g_L_nu` with the same dimensionless zpole convention as quarks, `s_Z*g_A^SM*(m_Z^2/M_KK^2)*U^dagger diag(a(c)-a_ref)U`; set the shared EW-universal subtraction to `a_ref = spectrum.a(DEFAULT_A_REF_C=0.65)` for quarks and leptons because `a(c)` is species-agnostic, and diagnostics must call it EW-universal, not quark-only; use `U_e_L/R` with `c_L/c_E` for charged leptons, use the active-neutrino basis carefully from `c_L` and PMNS metadata, do not use electroweak-singlet `c_N` for Z charge, and Hermitian-project/validate all matrices.
7. Neutrino basis rule: store active-neutrino Z couplings in the charged-lepton mass basis expected by `b -> s nu nu` and `K -> pi nu nu`; existing rare cores use the LH active-neutrino current with `X_NP = C/g_SM^2` and no `_wilson_prefactor`; Majorana-vs-Dirac gives no extra rate factor for light active-`nunu` observables, and because current `c_L` is universal PMNS rotations cancel in `U_nu^\dagger (a*I) U_nu = a*I` with Majorana phases cancelling too, but the code must still retain basis metadata and tests to prevent PMNS/Majorana double-rotation when non-universal `c_L[3]` appears.
8. Contact completion: replace Phase 3a's quark-NP times lepton-SM light-Z tensor with the full product-minus-SM tensor `(g_q^SM delta_ij + dg_q)(g_l^SM delta_ab + dg_l) - g_q^SM g_l^SM delta_ij delta_ab` for charged leptons and active neutrinos, preserving `GeV^-2` units and superseding the old tensor in the same `rs_ew_couplings.neutral_contacts` path rather than adding it again.
9. Heavy neutral exchange: `RSEWSpectrum` has no true `Z'/gamma'` neutral mass-eigenvector layer, so Phase 4 keeps `includes_heavy_neutral_exchange=False`/`includes_heavy_neutral_lepton=False`; Phase 4 is rigorous for light-Z exchange only, heavy neutral exchange is deferred, affected LFV observables stay PARTIAL where heavy-neutral pieces would be needed, and no heavy-neutral FULL status is claimed.
10. Wilson/contact bundle: extend `quarkConstraints.rs_semileptonic_wilsons.RSSemileptonicWilsonBundle` or add a reviewed `rs_lfv_neutral_wilsons` bundle because the current bundle only emits diagonal-lepton `_ll`; Substep 4a must add off-diagonal `lfv_llqq` blocks plus active-neutrino `_nunu` blocks (`b_to_s_nunu`, `s_to_d_nunu`); map contacts to `C9,C10,C9p,C10p` with the legitimate adapter `G_F/alpha/lambda_ckm` factor (`_dimensionless_wilson_prefactor`) for charged-lepton rare cores, and map `nunu` directly to `X_NP_L/R = C_LL/RL / g_SM^2` with no `_wilson_prefactor`; never reuse the forbidden old one-Z-like `1/M_KK^2` proxy or single-Z-like helpers.
11. Substep 4a, builder only: add lepton typed extra, lepton Z matrices, full light-Z contacts, explicit heavy-contact flags kept false, off-diagonal `lfv_llqq` Wilson blocks, `b_to_s_nunu`/`s_to_d_nunu` blocks, and focused unit tests; no constraint rewire in this substep.
12. Substep 4b, Z-LFV: rewire T015/T016/T017 from `(m_Z/M_KK)^2 lepton-overlap proxy` to `z_delta_g_L/R_e[e,mu]`, `[e,tau]`, `[mu,tau]` and call the existing `quarkConstraints.zpole_lfv.z_lfv_branching_fraction_from_couplings` helper; remove NEEDS-HUMAN only for tree-level light-Z LFV.
13. Substep 4c, charged-LFV rare/contact processes: rewire `secondary/kaon` K019/K020/K021 and C006/C008 from quark proxy plus lepton LFV proxy to LFV `llqq` contacts/Wilsons feeding the existing `YV/YA` or `C9/C10` rare cores; rewire L002 and L009 from Z-penguin overlap proxy to rigorous tree neutral-current `G_AB` contacts while keeping heavy-neutral, dipole, dipole-contact phase, and box pieces PARTIAL; rewire L003/L004/L005 vector `g_LV/RV^{p,n}` from up/down `llqq` contacts while scalar, dipole, and interference remain PARTIAL.
14. Substep 4d, `nunu`: rewire B022/B023 and K004/K005 from the current one-Z-like `Delta_q Delta_nu/(g_SM^2 M_KK^2)` proxy to `rs_semileptonic_wilsons.b_to_s_nunu` and `.s_to_d_nunu` using full active-neutrino contacts with direct `X_NP=C/g_SM^2` and no `_wilson_prefactor`; preserve the existing SM side, HPQCD/NA62/KOTO anchors, long-distance treatment, and `epsilon/eta/r_K/r_Kstar` response.
15. Validation: force lepton `a(c)=a_ref` with `a_ref=spectrum.a(DEFAULT_A_REF_C=0.65)` or `c_L,c_E` equal to the EW-universal reference so `dg_l=0`, LFV contacts vanish, and rewired LFV constraints return zero-NP/SM limits; separately test family-universal leptons not equal to `a_ref` gives no off-diagonal LFV; add a `nunu` convention test that the LH active current uses `X_NP=C/g_SM^2`, bypasses `_wilson_prefactor`, has no Majorana/Dirac rate multiplier, and cancels PMNS/Majorana phases for universal `c_L`; verify Hermiticity, no `g_Z` double count, no second `1/M_KK^2`, old SM-limit predictions unchanged, full suite green, and adapter-independent direct contact-to-Wilson checks.
16. Scaling sanity: construct a small non-universal `c_L/c_E` toy with a controlled `U_e` rotation and verify LFV Z/contact rates scale as `|U^\dagger diag(a-a_ref)U|^2 * m_Z^4/M_KK^4` for light-Z pieces, consistent with RS LFV expectations adjacent to the existing `mu->e gamma` spurion checks but not claiming loop-dipole or deferred heavy-neutral rigor.
17. Dual-review resolutions, no blockers: `a_ref` is the shared EW-universal `spectrum.a(DEFAULT_A_REF_C=0.65)` subtraction and is resolved, not NEEDS-HUMAN; `RSEWSpectrum` lacks `Z'/gamma'` neutral mass-eigenvectors, so heavy neutral exchange remains deferred/false and affected LFV stay PARTIAL; `nunu` uses the LH active current with `X_NP=C/g_SM^2`, no `_wilson_prefactor`, and no Majorana/Dirac rate factor; NIT fixes above are binding for params access, vector-to-matrix construction, helper/path names, and Wilson adapter usage.
