You are agent1 fixing C007 (D⁺→π⁺μμ) + module quarkConstraints/rare_charm_semileptonic.py. Physics review .orchestration/runs/C007/agent2_out.md = PHYSICS-NEEDS-FIXES (2 SHOULD-FIX); code OK. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX:
1. The HARD verdict integrates the FULL q² range but the diagnostics call it the "short-distance window" — these disagree, and charm D→πμμ is resonance-dominated. Either (a) apply the LHCb nonresonant dimuon q² window/acceptance if the value is sourced in C007.yaml/snapshots, OR (b) clearly RELABEL the verdict as a full-q² PROXY (rename the diagnostic, document that resonance/LD windows are not applied, and note this in NEEDS-HUMAN-PHYSICS). Make the verdict's q²-treatment and its label CONSISTENT.
2. The exclusive D→π form-factor inputs (f_+(0)=0.67(3), m_pole=1.90(8), a=0.28(14), b=1.27(17)) are hardcoded. Either add them to C007.yaml with provenance and load via load_anchor, OR document them in-code as cited constants (with the source) and note the ~9% normalization uncertainty in the budget/diagnostics. Prefer adding to yaml.
Keep RS proxy NEEDS-HUMAN-PHYSICS. Append-only to the module (don't modify C004/C005 funcs). Run tests/constraints/ -q green. OUTPUT <=10 lines: which options, changes, pytest counts.
