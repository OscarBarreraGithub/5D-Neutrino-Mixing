1. BLOCKER: K_S SD CP projection is wrong: code lifetime-rescales K_L SD using the K_L Re amplitude (`rare_kaon_dilepton_kshort_mumu.py:113`, `quarkConstraints/rare_kaon_dilepton.py:303`, `K012.py:502`). Correct K_S ell=0 SD is Im-sensitive, roughly Im[-lambda_c Y_c + lambda_t C10]^2; source also says it extracts |V_ts V_td sin(beta+beta_s)|. See https://arxiv.org/abs/2104.06427.
2. BLOCKER: SM SD sanity check exposes the same error: current K012 SM proxy = 1.44e-12 from tau_KS/tau_KL * KL(Re)^2, while same normalization with Im amplitude gives 1.86e-13; YAML’s 5e-12 is total-SM context, not SD (`K012.py:457`, `K012.yaml:138`).
3. SHOULD-FIX: tests lock in the wrong physics: independent recomputation uses `(lambda_t*y_t).real` and `lambda_c.real` then lifetime-rescales (`test_K012.py:95`), so a real NP coupling can fail K012 even though the CPV K_S SD piece should be Im-sensitive (`test_K012.py:302`).
4. SHOULD-FIX: mandatory running criterion is not literally satisfied: verdict calls `kshort_mumu_short_distance_from_couplings` -> K006 non-running path, no `*_with_running` or `mu_had=2 GeV` (`K012.py:502`, `rare_kaon_dilepton_kshort_mumu.py:220`). For color-singlet semileptonic Y/C10 the LO QCD factor may be 1, but K012 should state/diagnose that explicitly if this is the convention.
5. NIT: Budget is conservative/loose but defensible against K012.yaml HARD limit: 2.1e-10 total-rate 90% CL (`K012.yaml:90`) with no SM subtraction (`K012.py:376`); it is 42x above SM total context 5e-12 and ~1.1e3x above proper Im-SD estimate, so not too tight.
6. NIT: Anchor spot-checks pass: snapshots/YAML match PDG <2.1e-10, LHCb <2.2e-10 standalone and <2.1e-10 combined, Run-1 <0.8e-9 at 90%, SM total ~5e-12, hadronic target <1% (`K012.yaml:89`). Units are dimensionless BR; GeV/M12 checks are N/A for this Delta S=1 decay.

PHYSICS-NEEDS-FIXES