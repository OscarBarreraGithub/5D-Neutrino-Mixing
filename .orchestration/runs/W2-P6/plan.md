1. Existing machinery: `solvers/bessel.py` solves fermion `++/--` KK masses and returns Bessel `b`, `warpConfig/wavefuncs.py` has only zero-mode endpoint factors, `rs_ew_spectrum.py` has normalized gauge profiles/`a(c)`, `rs_ew_couplings.py` has gauge light-Z shifts with `fermion_kk_mixing_included=False`, and `yukawa/*`/`neutrinos/*` provide diagonal charged-lepton `Y_E_bar`, PMNS, and neutrino spurions; missing are normalized fermion KK wavefunctions, zero-mode-KK brane-Yukawa overlap matrices, full zero/KK fermion mass diagonalization, and W/Z/H vertices between mixed fermion eigenstates.
2. Phase-6 default remains minimal-RS leading-order/ZMA matching, not a fake full tower: add an explicit `RSFermionKKMixingZbb` result attached to/merged into `rs_ew_couplings` that computes the standard Casagrande minimal non-custodial fermion-admixture pieces using `bulk_state.c_Q[2]`, `bulk_state.c_d[2]`, `bulk_state.F_Q`, `bulk_state.F_d`, `bulk_state.Y_d_bulk_basis`, `m_b=fit_result.masses_down[2]`, and `Lambda_IR=spectrum.lambda_ir_gev` directly; do not re-divide `kk_ew_mass_gev` by `x_1`. Diagnostics must state no exact tower diagonalization was done.
3. Zbb formula in repo `zpole` convention: keep existing gauge `z_delta_g_L/R_d[2,2]`, add the minimal fermion piece from Casagrande eq. `ZbbRS`; `delta g_L^b,ferm = +(m_b^2/(2 Lambda_IR^2))*B_d(c_d,Y_d)` and `delta g_R^b,ferm = -(m_b^2/(2 Lambda_IR^2))*B_Q(c_Q,Y_d)`. Spell the sums explicitly: `g_L^b,ferm` uses `sum_i |Y_d[2,i]|^2/|Y_d[2,2]|^2` weighted with `c_d[i]`, while `g_R^b,ferm` uses `sum_i |Y_d[i,2]|^2/|Y_d[2,2]|^2` weighted with `c_Q[i]`; add an implementation comment pinning this L/R `B_d`/`B_Q` cross-assignment to prevent a future swap. Here `M_KK` in Casagrande means the geometric `Lambda_IR`, not the physical first gauge mass.
4. The top-driven/compositeness dependence enters the minimal diagnostic through `c_Q3` being forced IR by the top fit (`Y_u_bulk_basis`, `F_Q3*F_u3`, top mass); a genuinely top-partner/custodial/exotic-fermion `m_t^2/M_KK^2` Zb_L term requires representation assignments and remains `NEEDS-HUMAN`, not silently inferred from the current SM-representation quark fit.
5. Custodial policy: default `ew_model="minimal_rs"` computes the non-custodial ZMA value and marks `custodial_protection_included=False`; custodial-protected RS, `P_LR`, extended gauge groups, exotic fermions, and BKT choices remain the standing HUMAN-INPUT item. T010/T011 become FULL only as documented minimal-RS leading tree results with `minimal_rs_tree_complete=True` and `custodial_variant_needs_human=True`; the custodial variant remains diagnostic/NEEDS-HUMAN.
6. Higgs LFV object: add optional `rs_higgs_yukawas: RSHiggsYukawaCouplings` to `KNOWN_EXTRA_KEYS`, implemented in new module `quarkConstraints/rs_higgs_yukawas.py` from `lepton_mass_basis_couplings`, `spectrum.lambda_ir_gev`, charged-lepton masses from `yukawa.constants.LEPTON_MASSES` passed/derived explicitly (not stored on `RSLeptonMassBasisCouplings`), `Y_E_bar_matrix`, `c_L/c_E`, and `U_e_L/R`; expose `higgs_yukawa_matrix`/`Y_h_mass` in the convention consumed by `higgs_lfv.py`, plus `matching_assumption`, `units="dimensionless"`, `source`, `includes_fermion_kk_mixing`, and diagnostics.
7. Higgs LFV formula: lepton analog of Casagrande `Delta g_h`: `Y_h_mass = M_e/v - Delta g_h`, with off-diagonal `Y_ij = -[(m_i/v)(delta_E)_{ij} + (delta_L)_{ij}(m_j/v)]`, `delta_L = x_e U_e_L^dag diag[B(c_E)] U_e_L x_e`, `delta_E = x_e U_e_R^dag diag[B(c_L)] U_e_R x_e`, `x_e=diag(m_e,m_mu,m_tau)/Lambda_IR`, scaling as `v^2/M_KK^2` times Yukawa/overlap structure and chirally suppressed by lepton masses.
8. Important v1 honesty: with the current diagonal charged-lepton fit (`Y_E_bar_matrix=diag`, `U_e_L=U_e_R=I`, scalar/broadcast `c_L`) this tree Higgs-LFV matrix is exactly diagonal, so T018/T019/T020 evaluate non-vetoing zero rather than a nonzero PMNS/neutrino-spurion effect; nonzero tree Higgs LFV requires non-diagonal charged-lepton `Y_E_bar_matrix`/rotations or a future full lepton anarchy fit. Generic RS Higgs-LFV therefore stays PARTIAL outside the explicit supplied charged-lepton structure.
9. Builder wiring: extend `build_rs_ew_extras(..., include_fermion_kk_mixing=True, include_higgs_yukawas=True)`; 6a must remove the hard guard in `flavor_catalog_constraints/rs_ew_builder.py` that currently raises `ValueError("fermion-KK mixing is deferred beyond Phase 3a")` and replace it with the real minimal fermion-KK path that updates `rs_ew_couplings.z_delta_g_L/R_d[2,2]` and metadata atomically. 6b emits `rs_higgs_yukawas`; fail loud if the requested matcher lacks `bulk_state.Y_d_bulk_basis`, `bulk_state.c_Q/c_d`, `bulk_state.F_Q/F_d`, `fit_result.masses_down[2]`, lepton rotations, charged-lepton constants, or `spectrum.lambda_ir_gev`.
10. Rewire T010/T011: continue reading `rs_ew_couplings.z_delta_g_L/R_d[2,2]`, now gauge plus minimal fermion-KK piece; remove the old PARTIAL/NEEDS-HUMAN-for-fermion-KK wording and replace it with `minimal_rs_tree_complete=True` plus `custodial_variant_needs_human=True`. SM anchors and `zpole` observable formulas remain unchanged.
11. Rewire T018/T019/T020: change `_REQUIRED_EXTRA` from `lepton_mass_basis_couplings` to `rs_higgs_yukawas` and pass that object directly to adapter `higgs_lfv_branching_fraction_with_proxy` (core function `h_lfv_branching_fraction_with_proxy`). The adapter already reads `higgs_yukawa_matrix` through `_first_present_attr`, so only the required-extra rewire is needed on the production path; keep SM=0 and BR formula `m_h(|Y_ij|^2+|Y_ji|^2)/(8*pi*Gamma_h)`, and leave legacy explicit proxy tests only as adapter/core tests.
12. Substep 6a touches/tests: new Zbb fermion-mixing helper/dataclass, `rs_ew_couplings` extension or immutable replacement helper, explicit `rs_ew_builder` hard-guard removal, T010/T011 notes/tests. Validate alignment and `m_b->0` give zero fermion piece, `1/M_KK^2` scaling holds, and Casagrande sign is pinned with `dgL>0` and `dgR<0`.
13. Substep 6b touches/tests: `rs_higgs_yukawas` dataclass/builder, `KNOWN_EXTRA_KEYS`, `include_higgs_yukawas` kwarg in `rs_ew_builder`, T018/T019/T020 rewires/tests. Validate diagonal v1 gives exactly zero off-diagonal `Y_h_mass` and non-vetoing T018/T019/T020 zero; injected non-diagonal `U_e`/anarchic toy gives nonzero `Y_ij` and nonzero BR matching the existing Higgs-LFV core; verify `v^2/M_KK^2` scaling.
14. Risks: `M_KK` convention mismatch (`spectrum.lambda_ir_gev`/geometric `Lambda_IR` vs first physical gauge root), double-counting gauge-Z shifts and fermion admixture, Zb_L sign relative to LEP `R_b/A_b/A_FB`, custodial protection accidentally enabled or double-counted, confusing tree Higgs LFV with loop dipole LFV, and over-promoting exact tower/custodial/loop pieces that remain HUMAN/partial.
