# WQ NOTEBOOK RE-REVIEW (codex, gpt-5.x xhigh) — verify the NEEDS-FIXES items are resolved
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing (env: `source ~/.bashrc && conda activate ising_bootstrap`). Your prior review of `notebooks/wq_quarkonly_explore.ipynb` returned NEEDS-FIXES (4 items). An agent applied fixes. INDEPENDENTLY VERIFY each is actually resolved — read the executed notebook (.ipynb cell outputs) + the .py source; recompute from data where needed. Do NOT trust the fixer's claims.
1. **4-TeV floor:** confirm A4's input panel (and EVERY panel/aggregate) now contains ZERO sub-4-TeV rows (no 1/2/3 TeV). Grep the code for any `~skipped`/`df[` source that bypasses the M_KK≥4 filter. Confirm the analysis frame is M_KK∈{5,7,10,15,20,30,50} only.
2. **Axis floors:** confirm no `set_ylim(3.5` / sub-4 M_KK axis floor remains (C2 was 3.5→4.0; C3b x-floor 4.0).
3. **T010 wording:** confirm T010/T001/T002/T011 are now labeled `partial`/HARD (non-vetoing because PARTIAL=incomplete matching), NOT INFO/SOFT, in BOTH the C1c figure and the markdown cells. Confirm the advisory set is still excluded from carving/survival.
4. **Scale-mixing:** (a) A/B panels: confirm there is a verification cell proving fitted-Yukawa & bulk-c M_KK-independence (max σ across M_KK ≈ 0, executed output present) and that every A/B panel is annotated accordingly (faceted by r, M_KK-pooling justified as exact). (b) C3b: confirm binary veto-vs-M_KK is now PER r (one line/facet per r), not pooled. (c) C3a: confirm it is explicitly labeled as the alternative-axis (compositeness re-expresses r) exploration.
5. **Rendered + clean:** confirm the executed .ipynb has embedded image/png outputs for all figures (no empty/broken) and ZERO error/traceback outputs. (Re-run nbconvert --execute --inplace if you want, but the committed .ipynb must already carry rendered outputs.)
Also reconfirm the items that PASSED before are still intact (per-row tags; fitted-recompute; quark-only labeling; headline numbers EW001 carves at 5 TeV / nothing ≥7 TeV / strict survival=1 at ≥5 TeV).
OUTPUT (≤12 lines): per-item PASS/FAIL with the verifying evidence (numbers/line refs); rendered-outputs confirm; any remaining issue. END with EXACTLY ONE line: `WQ-NB-REVIEW2: APPROVE` or `WQ-NB-REVIEW2: NEEDS-FIXES`.
