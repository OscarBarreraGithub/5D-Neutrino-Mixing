1. NIT: `rare_kaon_lfv_dilepton.py:8-16` says the equal-muon axial limit reduces to `kappa_mu*|Y/lambda^5|^2`, but with default `charge_state_factor=2` (`:69,:83,:391-395`) it reduces to `2*kappa_mu*|Y/lambda^5|^2`; computation is OK for charge-summed `e±mu∓`, doc should say “per charge state” or `charge_state_factor=1`.
2. NIT: ΔF=2 amplitude-part check is N/A for K019: this is a LFV branching-ratio bound, not Δm or CPV mixing; code correctly uses `|Y_V,A|^2` in the two-body rate (`rare_kaon_lfv_dilepton.py:386-395`) and bounds BR directly (`K019.py:403-427`).
3. NIT: QCD-running check is N/A for this restricted color-singlet semileptonic ΔS=1 proxy; K019 calls `klong_emu_from_couplings` (`K019.py:385`) through dilepton matching, not ΔF=2 `*_with_running`; expected multiplicative running effect is 1.0 / 0% per `rare_kaon_dilepton.py:691-698`.
4. NIT: Budget is defensible: LFV SM≈0, so pure-NP `BR_NP <= 4.7e-12` at 90% CL, not a residual; code uses the YAML current limit as `budget` (`K019.py:121-127,:403-427`), matching `K019.yaml:81-90,:126-141`.
5. NIT: Anchor spot-check passes: PDG snapshot lists `< 4.7 x 10^-12, CL=90%` (`pdg2025_kl_emu.txt:17`); Ambrose/BNL E871 snapshot gives `B(K_L^0 -> mu± e∓) < 4.7 x 10^-12` (`ambrose_1998_arxiv_hepex9811038.txt:17-21`).
6. NIT: Severity/units/diagnostics are appropriate: HARD direct upper limit (`K019.py:317,:421-427`), dimensionless branching fraction enforced (`K019.py:169-182`), GeV masses used (`rare_kaon_lfv_dilepton.py:80-83`), and LFV proxy is correctly flagged NEEDS-HUMAN-PHYSICS (`K019.py:85-89,:428-432`).
PHYSICS-OK