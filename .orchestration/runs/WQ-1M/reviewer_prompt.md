# WQ-1M — INDEPENDENT REVIEW (codex reviewer, gpt-5.x xhigh)

Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. INDEPENDENTLY review the AUTHOR's uncommitted WQ-1M change (sharded 1M quark-only r×M_KK scan launcher + serialization + `scripts/analyze_wq_quarkonly.py` plots). Do NOT trust the author self-report (`.orchestration/runs/WQ-1M/author_out.md`); RE-DERIVE. Inspect `git diff` + the new files: `scripts/run_wq_quarkonly_1m_array.sbatch`, `scripts/wq_quarkonly_1m_plan.py`, `scripts/analyze_wq_quarkonly.py`, `tests/test_wq_quarkonly_analysis.py`, and the `scripts/run_full_catalog_scan.py` serialization additions.

## VERIFY (recompute)
1. **SEED-DISJOINTNESS (THE critical correctness gate).** The author claims: 50 tasks; `base=202606040000+20000000*task_id`; `tile_seed=base+1000003*tile_id`; `draw_seed=tile_seed+draw_idx`; with 10 tiles × 2000 draws/tile/task, max intra-task offset = 1000003*9+1999 = 9,002,026 < 20,000,000 ⇒ disjoint. RE-DERIVE this rigorously: confirm NO two (task,tile,draw) triples across all 50 tasks AND all r values can ever collide on the same draw_seed, AND that the harness actually consumes `draw_seed` to seed the per-draw RNG the way the proof assumes. A seed collision = duplicate physics points = corrupted statistics. State PASS/FAIL with the inequality.
2. **r-SHARD / YUKAWA PAIRING (design check, flag don't block).** With fully-disjoint seeds, each r value draws an INDEPENDENT anarchic-Yukawa ensemble (unpaired across r). Consider whether a PAIRED design (same Yukawa draws reused across the 5 r values, so c_Q evolution at fixed input is visible) would better serve the physicist's ask "evolution of the yukawas at independent r." Both satisfy "not mixed together"; 20k/r is plenty for unpaired survival statistics. Give a recommendation (keep unpaired vs switch to paired) with reasoning — but this is NOT a blocker unless you judge the unpaired choice scientifically wrong.
3. **SERIALIZATION.** `quark_fit_r` recorded per row/params/provenance; fitted up/down Yukawa singular values added to `fit_diagnostics`; FULL-mode (non-quark-only) output + config-hash BYTE-IDENTICAL (flag off). Confirm.
4. **ANALYSIS CORRECTNESS.** `analyze_wq_quarkonly.py`: M_KK survival + per-constraint veto curves + constraining-power ranking (rigorous vs proxy separated honestly); Yukawa singular-value + bulk-c + max|Yq| distributions; per-r EVOLUTION facets that NEVER aggregate across r OR across M_KK. Re-run it on `.orchestration/runs/WQ-QUARKONLY/smoke-1k/` and confirm it emits the PNGs + non-empty report without error. Check it reads `quark_fit_r` from rows (and degrades sanely on old rows lacking fitted singular values).
5. **SBATCH RESOURCE SIZING.** The REAL cost is ~1.2 s/point (fit-dominated; the 100k took 42 min wall on 48 cores for 100k pts), NOT the eval-only 0.24 s/point in the report. Per task = 10 tiles × 2000 = 20k pts ⇒ ~20000*1.2/48 ≈ 500 s + per-tile spectrum build (~34 s × 10 ≈ 340 s one-time) ≈ ~14 min/task. CONFIRM the sbatch `--time`, `--cpus-per-task`, `--mem`, partition/account are adequate (≥30 min time, enough mem) and the array size/`%`-concurrency is sane. Flag if `--time` is too tight.
6. **COSMETIC (note, non-blocking):** the run report is headed "W6b Full-Catalog Smoke Report" and shows "universal-c sanity: False" though quark-only INTENTIONALLY skips that sanity. The new analysis report should be labelled correctly for a quark-only run and not present skipped-sanity as a failure. Confirm the analysis report does this (or note it as a fix).
7. Run `python -m pytest tests/ -q` in the FOREGROUND; report counts.

## OUTPUT (≤14 lines)
Per-area findings; the seed-disjointness PASS/FAIL with the inequality; the paired-vs-unpaired recommendation; confirm serialization + full-mode byte-identity + analysis-on-smoke + sbatch sizing; pytest counts. END with EXACTLY ONE line:
`WQ-1M-REVIEW: APPROVE`  or  `WQ-1M-REVIEW: NEEDS-FIXES`
(If NEEDS-FIXES, list the precise blocking items above that line.)
