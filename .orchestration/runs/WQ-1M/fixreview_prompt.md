# WQ-1M SBATCH-FIX REVIEW (codex, gpt-5.x xhigh)
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Review the UNCOMMITTED 3-line fix to `scripts/run_wq_quarkonly_1m_array.sbatch` (see `git diff scripts/run_wq_quarkonly_1m_array.sbatch`). Intent: the previous version computed a per-task `date` TIMESTAMP so 50 array tasks scattered output into separate roots; the fix derives the output root from `SLURM_ARRAY_JOB_ID` (constant across all tasks of one array) so ALL tasks share ONE root. VERIFY: (1) `SLURM_ARRAY_JOB_ID` is indeed identical across all array tasks (standard SLURM) so every task resolves to the SAME OUTPUT_ROOT; (2) the unset/non-array fallback (`SLURM_JOB_ID:-manual`) is sane; (3) the WQ_OUTPUT_ROOT override still works; (4) the seed logic (base/stride/draws) and the harness invocation are UNCHANGED — disjointness preserved; (5) per-task `${WQ_R_LABEL}/shard-*` subdir layout unchanged so the 50 (r,shard) dirs remain unique under the shared root. Confirm `git diff` shows ONLY the timestamp→array-id change. END with EXACTLY ONE line: `WQ-1M-FIXREVIEW: APPROVE` or `WQ-1M-FIXREVIEW: NEEDS-FIXES`.
