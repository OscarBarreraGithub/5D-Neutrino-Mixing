You are agent1 fixing L001 (μ→eγ). Physics review .orchestration/runs/L001/agent2_out.md = PHYSICS-NEEDS-FIXES (code .orchestration/runs/L001/agent3_out.md = CODE-OK). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX (SHOULD-FIX, physics semantics):
When the lepton dipole inputs are missing/invalid, L001 currently returns passes=True with no prediction — for a HARD MEG II bound that misreads as "μ→eγ constraint satisfied." Make the unevaluated case UNAMBIGUOUS: keep passes=True ONLY in the non-vetoing sense (an unevaluated constraint must not exclude points), but set predicted=None, add diagnostics["evaluated"]=False with an "unevaluated_reason", and make notes state clearly "NOT EVALUATED — no lepton dipole prediction available (lepton-sector RS couplings not on ParameterPoint)". Do NOT report a numeric BR or a genuine pass when there is no prediction. Keep the NEEDS-HUMAN-PHYSICS proxy flag for the evaluated-proxy path. When a real (proxy) dipole IS provided, the normal BR-vs-1.5e-13 HARD verdict stands.
Isolated to L001.py, its test (add a test asserting the unevaluated case has evaluated=False and is not presented as a real pass), and the lepton adapter if needed. Run `python -m pytest tests/constraints/ -q` green. OUTPUT <=10 lines: changes, pytest counts.
