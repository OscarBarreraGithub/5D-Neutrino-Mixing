# Implement L001 — BR(μ→eγ). family=charged_lepton.
CHECK flavorConstraints/muToEGamma.py (check_mu_to_e_gamma) — REUSE it if usable. It takes a YukawaResult, a dipole coefficient C, and M_KK. The SM rate is ~0, so this is a pure NP dipole bound vs the MEG II limit (from L001.yaml). If the ParameterPoint does NOT carry the lepton YukawaResult / dipole inputs needed, build a thin lepton adapter and use a DOCUMENTED proxy for the lepton dipole, flagged NEEDS-HUMAN-PHYSICS (the lepton-sector RS couplings are not on the quark-only ParameterPoint). Budget = MEG II BR limit, HARD. Reach physics only via an adapter (new physics_adapters/lepton.py if needed).
