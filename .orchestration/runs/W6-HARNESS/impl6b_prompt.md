W6b — FULL-CATALOG SCAN HARNESS DRIVER + smoke scan (codex, gpt-5.x xhigh). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Implement the harness per the DUAL-APPROVED plan .orchestration/runs/W6-HARNESS/plan.md, using the W6a perf hook (committed 7956106: RSEWOverlapSplineCache + spectrum/spline injection). First a SHORT plan, then implement. Codex + Opus dual-review (both must APPROVE). Reuse run_rs_anarchy.py's multiprocessing/tiling/seeding STYLE only (it lacks full catalog/leptons/fitted-quarks/resume/cache).

BUILD a new driver (e.g. scripts/run_full_catalog_scan.py) that:
1. SWEEP: per tile a fixed (Lambda_IR, M_KK) + deterministic per-tile seed; n_draws of anarchic 5D Yukawas + lepton inputs (c_L,c_E[3],c_N,M_N,lightest_nu_mass,ordering,majorana phases). Ranges per CLAUDE.md (c in [0.3,0.9], M_KK 1-10 TeV, M_N 1e10-1e15). Run the quark fit -> real QuarkFitResult and compute_all_yukawas for leptons. Skip+log invalid/failed fits, c=0.5 singularities, nonperturbative Yukawas.
2. PER-TILE CACHE: build the RSEWSpectrum + RSEWOverlapSplineCache ONCE per tile (fixed Lambda_IR) and INJECT (rs_ew_cache=/spectrum=+a_of_c=+omega_of_c=) into build_from_rs_ew_inputs(..., include_charged_current=True, include_fermion_kk_mixing=True, include_higgs_yukawas=True) for every draw -> populate ALL extras so constraints BITE.
3. EVALUATE: registry.discover() once (assert 103, 0 import failures); registry.evaluate_all(point) per draw.
4. HONEST TAGGING: read each result's diagnostics to TAG matching status rigorous|proxy|partial|stub (use needs_human_physics / matching_status / proxy flags). Compute survives_all_HARD TWO ways: strict (passes all rigorous HARD) and inclusive (passes all HARD incl. proxy). Record excluded_by_rigorous vs excluded_by_proxy, hard_not_evaluated, coverage_complete.
5. OUTPUT: stream compact per-tile JSONL rows {draw_id,tile_id,seed,params,fit_diagnostics,constraints:{id:{passes,severity,ratio,active,evaluated,match_status}},survives_all_HARD_strict,survives_all_HARD_inclusive,excluded_by_rigorous,excluded_by_proxy,advisory_flags,provenance:{git_sha,dirty,registry_count}} + a per-tile summary.json; atomic temp->final rename; resume skips completed tiles.
6. PARALLELISM: multiprocessing pool; per-tile seed = base_seed + stride*tile_id; no shared RNG/cross-tile writes; per-constraint exception isolation (evaluate_all already isolates).
7. SANITY: a universal-c / diagonal-lepton sanity draw recovers SM (no spurious HARD exclusions).
8. RUN A SMOKE SCAN: execute ~1e4 points (a few tiles) NOW; report: actual POST-CACHE wall-seconds/point, evaluated/active counts, the ranking of most-constraining HARD vetoes (rigorous vs proxy), exception rate, and the extrapolated 1e8 core-hours. Write smoke output + a short report.

CONSTRAINTS: the driver is a script (not imported by constraints); do NOT change constraint physics; numeric outputs sane. `python -m pytest tests/ -q` stays green (add harness unit tests; the scan script itself is exercised by the smoke run).

OUTPUT (<=18 lines): short plan; the driver path + a harness unit test; the SMOKE results (post-cache s/point, 1e8 extrapolation, top rigorous vetoes, top proxy vetoes, exception rate, universal-c SM sanity); pytest counts. End with: W6B-DONE.
