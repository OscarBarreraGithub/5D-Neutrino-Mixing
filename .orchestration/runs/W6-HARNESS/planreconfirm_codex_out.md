1 RESOLVED - `a(c)` spline plus `Omega_n(c)` lookup recorded.
2 RESOLVED - per-tile spectrum and `spectrum=`/`a_of_c=` hook recorded as required dual-gated code change.
3 RESOLVED - honest veto tagging, strict/inclusive survival, exclusion split, and coverage fields recorded.
4 RESOLVED - real `QuarkFitResult` for `include_fermion_kk_mixing` recorded.
5 RESOLVED - new-driver framing with `run_rs_anarchy.py` style reuse only recorded.
6 RESOLVED - post-cache smoke timing gate for `1e8`, `1.2-3 s/point`, `4-8` core-years recorded.
7 RESOLVED - advisory lists, schema, and guards kept.
PLAN-OK