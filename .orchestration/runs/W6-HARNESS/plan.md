1. Driver scope: implement W6 as a new harness driver that reuses only `run_rs_anarchy.py` multiprocessing, tiling, deterministic seeding, and anarchic-prior style; do not extend that script directly because it lacks the full catalog, leptons, fitted-quark objects, atomic resume, and cache hooks needed here.
2. Sweep inputs: per point store geometry `(Lambda_IR, M_KK=xi_KK*Lambda_IR, xi_KK, k)`, quark anarchic seed/fit inputs, and lepton inputs `(c_L,c_E[3],c_N,M_N,lightest_nu_mass,ordering,majorana_alpha,beta)`; ranges from CLAUDE/theory priors are `c in [0.3,0.9]`, `M_KK in [1,10] TeV`, `M_N in [1e10,1e15] GeV`, with exact singular `c=0.5` skipped/logged for Higgs `B(c)`.
3. Required per-tile RS-EW precompute: because tiles are fixed-`Lambda_IR`, each worker/tile must build `RSEWSpectrum` once for `(Lambda_IR,k,n_modes,quadrature)` and reuse it for every draw; `build_rs_ew_extras` currently calls `RSEWSpectrum.build()` directly and costs about `8.8-9s` per point, so the uncached `11.6s` build timing must not be used as the production estimate.
4. Required `a(c)`/`Omega_n(c)` cache: build once per tile a precomputed `CubicSpline` or equivalent for `a(c)` on `c in [0.3,0.9]` per `(Lambda_IR,k)` with verified max relative error about `6e-8`, comfortably below the overlap `rel_tol=1e-3`; the one-time spline build is about `34s` and evaluation is microseconds, while the per-instance `_omega_cache`/`_a_cache` memo cannot be relied on because continuously sampled `c` values will not hit.
5. Charged-current contact precompute: charged-current contacts require the full `omega(c)` vector, so the tile cache must also precompute/lookup `Omega_n(c)` vectors component-wise over the same `c` grid or derive them from the same spline basis used for `a(c)`.
6. Required builder hook code change: W6 implementation must add a small injection hook such as `spectrum=` and `a_of_c=`/`omega_of_c=` to `build_rs_ew_extras` and `build_from_rs_ew_inputs`, so the harness can pass the prebuilt per-tile spectrum and spline cache instead of rebuilding; this code change is not part of this plan-only revision and must be dual-gated when implemented.
7. Point build: for each valid draw, run the repo quark fit to `default_quark_targets()` and keep a real `QuarkFitResult` object, because `run_rs_anarchy.py` rows are insufficient for `include_fermion_kk_mixing`; require quark fit success/residual thresholds and perturbative lepton Yukawas, then build the full `ParameterPoint` with `include_charged_current=True`, `include_fermion_kk_mixing=True`, `include_higgs_yukawas=True`, and `lepton_yukawa_result=...`.
8. Failure policy: quark/lepton fit failures, invalid `c` values, builder errors, nonfinite extras, missing injected caches, or constraint exceptions produce compact skipped-row/error-log diagnostics and never abort the tile.
9. Evaluate: each worker calls `registry.discover()` once, verifies the expected `103` constraints and zero import failures, then calls `registry.evaluate_all(point)` per draw; current `evaluate_all` around `2.0s` is mostly real constraint math using prebuilt extras, with optional future de-duplication for DeltaF=2 adapters that recompute Wilson/RG data across related constraints.
10. Honest veto tagging: since `Severity` has only `HARD/SOFT/INFO`, the harness must read each result's diagnostics such as `matching_status`, `needs_human_physics`, proxy flags, and quarantine markers, then tag every constraint as `rigorous`, `proxy`, `partial`, or `stub`; incalculable-SM stubs self-quarantine as `INFO/SOFT` and are non-veto, while documented HARD proxies still veto in inclusive mode, including collider `sigma*BR` mass-limit `CR*`, `B013/B014` `C7` dipole proxies, and any remaining proxy-NP constraints.
11. Veto outputs: serialize both `survives_all_HARD_strict` meaning all evaluated rigorous HARD constraints pass and `survives_all_HARD_inclusive` meaning all evaluated HARD constraints pass including proxy HARD; record `excluded_by_rigorous`, `excluded_by_proxy`, `hard_not_evaluated`, and `coverage_complete`, and never allow `SOFT/INFO`, `partial`, `stub`, missing, invalid, exception, or quarantine results to masquerade as rigorous physics exclusions.
12. Output JSONL row: `{draw_id,tile_id,seed,params,fit_diagnostics,constraints:{id:{passes,severity,ratio,active,evaluated,tag,matching_status,needs_human_physics,proxy_flags}},survives_all_HARD_strict,survives_all_HARD_inclusive,excluded_by_rigorous,excluded_by_proxy,hard_not_evaluated,coverage_complete,advisory_flags,cache_metrics,provenance:{git_sha,dirty,registry_count,config_hash}}`.
13. Files: stream compact per-tile `tile-XXXXX.jsonl` plus `tile-XXXXX.summary.json`; atomic temp-to-final rename marks completion, resume skips final tiles whose summary row count/hash/cache-key matches, and production output should use compact maps and/or compression to control JSONL volume.
14. Parallelism: preserve `run_rs_anarchy.py` multiprocessing/SLURM style with deterministic tile seeds `base_seed + tile_stride*tile_id`; no shared RNG state, no cross-tile writes, per-tile spectrum/spline caches are worker-local, and summaries merge after jobs finish.
15. Correctness guards: prove `SOFT/INFO` cannot enter either exclusion list; quarantine `NEEDS-HUMAN/PARTIAL` exceptions; require `hard_not_evaluated` to make coverage gaps explicit; run a universal-`c`/diagonal-lepton sanity point that gives SM-like zero FCNC/LFV shifts and no spurious HARD exclusions.
16. Determinism guards: same `(tile_id,draw_id,seed,config_hash,git_sha)` must reproduce params, fits, cache keys, extras hashes, constraint tags, and constraint ratios bitwise/tolerance-wise; one constraint exception must not abort a tile.
17. Smoke plan and performance gate: run `1e4`, then `1e5-1e6` points before production, validate schema/resume/provenance, count evaluated/active/tagged constraints, rank rigorous and proxy vetoes separately, inspect exception/advisory/cache-hit rates, and measure the real post-cache seconds per point instead of the uncached `11.6s` path before scheduling `1e8+`.
18. Scale verdict: expected post-cache target is roughly `1.2-3 s/point`, making `1e8` points about `4-8` core-years and cluster-feasible only if the smoke benchmark confirms this range; if post-cache timing misses the gate, defer `1e8` and profile spectrum injection, spline lookup, DeltaF=2 recomputation, semileptonic/top/rare-B/K evaluators, and JSONL serialization.
19. Advisory in v1: non-veto `SOFT` IDs are `EW002,EW003,K018,L003`; non-veto `INFO` IDs are `B032,B033,B034,C003,CR011,E004,E006,E007,E008,E009,K003,K013`; the verified catalog total remains `103` constraints.
20. Also advisory/interpret-with-care: diagonal-lepton tree LFV/Higgs LFV zeros, EDM/qCEDM/loop-dipole-deferred pieces unless explicit proxies are supplied, collider/custodial mass-proxy recasts, and `eps'/eps`/other PARTIAL adapters flagged `NEEDS-HUMAN-PHYSICS`.
21. Review status: both dual-review passes were `PLAN-NEEDS-FIXES`; this revision records the required performance fixes and honest-veto policy while leaving physics/schema/guards/advisory lists intact, and no production code changes are included in this step.
