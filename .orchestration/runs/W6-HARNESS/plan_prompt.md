W6 — FULL-CATALOG SCAN HARNESS, PLAN ONLY (codex, gpt-5.x xhigh). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Produce a concrete IMPLEMENTATION PLAN (NO code) for a cluster-ready harness that sweeps parameter points, builds each with the FULL RS-EW extras, evaluates ALL catalog constraints, and serializes results — enabling a 100M+ point run. Codex + Opus dual-review this PLAN. WRITE to .orchestration/runs/W6-HARNESS/plan.md.

CONTEXT: the RS-EW build (W2 P1-P6) is COMPLETE — constraints now consume rigorous RS couplings via build_from_rs_ew_inputs(..., include_charged_current=True, include_fermion_kk_mixing=True, include_higgs_yukawas=True) which populates rs_ew_spectrum/rs_ew_couplings/rs_charged_current/rs_higgs_yukawas/lepton_mass_basis_couplings. There is ALREADY a cluster driver scripts/run_rs_anarchy.py (multiprocessing pool, per-tile seeding, JSONL streaming, per-draw try/except, git-SHA provenance) but it evaluates ONLY the 5 hardcoded DeltaF=2 systems via quarkConstraints.deltaf2, bypassing the 103-constraint registry.

THE PLAN MUST SPECIFY:
1. SWEEP INPUTS: the full parameter set a point needs (quark sector: c_Q[3],c_u[3],c_d[3] or the anarchic 5D Yukawa draw + the fit; lepton sector: c_L,c_E[3],c_N,M_N,lightest_nu_mass,ordering,majorana phases; geometry: Lambda_IR/M_KK, k). How to generate them (reuse run_rs_anarchy.py's anarchic draw + the Yukawa fit for quarks AND leptons). What ranges (CLAUDE.md: c 0.3-0.9, M_KK 1-10 TeV, M_N 1e10-1e15).
2. POINT BUILD: per draw, run the quark+lepton Yukawa fit, then build_from_rs_ew_inputs with ALL include_* flags so EVERY constraint's required extra is present and the constraints BITE. Handle fit failures / invalid draws gracefully (skip + log).
3. EVALUATE: call registry.discover() once, registry.evaluate_all(point) per draw. Collect per-constraint {passes, severity, ratio, active, evaluated}.
4. OUTPUT SCHEMA: per-point JSONL row = {draw_id, seed, params, per-constraint pass/fail/ratio/severity/evaluated, survives_all_HARD (bool), excluded_by (list of HARD constraint ids that vetoed), provenance(git-SHA)}. Enough to answer "which points survive all HARD constraints" + "which constraint excluded each point". Stream to JSONL; per-tile files.
5. PARALLELISM/SCALE: multiprocessing pool (reuse run_rs_anarchy pattern); tiling; CHECKPOINT/RESUME (skip completed tiles); deterministic per-tile seeding. PER-POINT COST: estimate it (the RS-EW builder adds spectrum/a(c)/matrix cost beyond the ~65ms/103-constraint baseline; note the a(c)/spectrum caching). Extrapolate single-core CPU-time for 1e8 and confirm cluster-feasibility; identify the most expensive constraints/builder steps.
6. CORRECTNESS GUARDS: confirm INFO/SOFT (non-vetoing) constraints don't veto; the NEEDS-HUMAN/PARTIAL constraints (EDMs, dipoles, custodial, eps'/eps, collider recasts) are correctly non-vetoing or advisory; a universal-c sanity point recovers SM (no spurious exclusions). Determinism. One constraint's exception cannot abort a tile (evaluate_all already isolates).
7. SMOKE PLAN: a small smoke scan (1e4-1e6) to validate output schema + that constraints bite sensibly (report which constraints are most/least constraining) BEFORE the 100M.
8. WHAT STAYS ADVISORY in the run: list the constraints that will be non-vetoing in v1 (tree-LFV=0 for diagonal lepton fit; EDMs/dipoles loop-deferred; collider mass-proxies; INFO stubs) so the run's "survives all HARD" is interpreted correctly.

OUTPUT (<=20 lines): sweep inputs + generation, the point-build path, the output schema, parallel/checkpoint design, per-point cost estimate + 1e8 feasibility, correctness guards, the smoke plan, and what stays advisory. Confirm plan.md written. PLAN ONLY.
