You are agent1 fixing T019 (h→eτ). Physics review .orchestration/runs/T019/agent2_out.md = PHYSICS-NEEDS-FIXES (1 SHOULD-FIX); code OK. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX (SHOULD-FIX): result.yukawa_norm is dimensionally misleading — the core stores |Y_eτ|²+|Y_τe|² but it's reported as "yukawa_norm" (e.g. 5.25e-8 reported vs true sqrt-norm 2.291e-4). Either rename the diagnostic to the squared quantity (e.g. yukawa_norm_squared) OR report sqrt(|Y_eτ|²+|Y_τe|²) consistently. Make the label and the value consistent; verify against the limit (1.278e-3). Apply the same consistency to the shared higgs_lfv core if it affects T018/T020 (but don't break T018 — check). Isolated to T019.py / higgs_lfv.py diagnostic label + test. Run tests/constraints/ -q green. OUTPUT <=8 lines: rename-vs-sqrt decision, change, pytest counts.
