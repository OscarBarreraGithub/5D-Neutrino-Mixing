You are agent1 fixing L002 (μ→3e) + module quarkConstraints/lfv_three_body.py. Reviews .orchestration/runs/L002/{agent2_out.md (PHYSICS BLOCKER),agent3_out.md (CODE BLOCKER+SHOULD-FIX)}. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX:
1. (PHYSICS BLOCKER) The μ→3e rate omits dipole-contact INTERFERENCE: code does BR = dipole_component + contact_component. The full amplitude has linear Re(A_{L,R} G*) cross terms (chiral dipole amplitudes/phases × contact). Implement the interference term with the proper chiral structure. If the relative phase between the dipole and contact RS contributions isn't groundable, implement the interference with a documented phase assumption (and/or a conservative sign-envelope) and flag that assumption NEEDS-HUMAN-PHYSICS — do NOT silently drop interference. (Restricting the HARD verdict to dipole-only/contact-only is an acceptable fallback ONLY if clearly documented.)
2. (CODE BLOCKER) The adapter/constraint lets caller initial_flavor/final_flavor override L002's μ/e defaults (a τ→μ input was accepted while reporting "BR(μ→3e)"). Pin μ→3e (reject other flavors) in L002/the adapter; add a regression test.
3. (SHOULD-FIX) The numerical test uses a copied local formula; recompute expected via the core lfv_three_body_contact_amplitudes + lfv_three_body_from_components and the L001 dipole core (not the adapter).
Keep RS proxy NEEDS-HUMAN-PHYSICS. Reusable by L009/L010. Run tests/constraints/ -q green. OUTPUT <=12 lines: interference treatment, flavor-pin, cross-check numbers, pytest counts.
