# DUAL-REVIEW of the G1 consensus design (codex, gpt-5.x xhigh). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Do NOT modify code. Read `.orchestration/rs_ew_sector_design_CONSENSUS.md` (and, only if needed for arbitration, the two source drafts `.orchestration/rs_ew_sector_design.md` + `.orchestration/runs/G1-DESIGN/codex_design.md`). Produce findings + verdict + an arbitration of the open items. This design will be IMPLEMENTED next, so it must be sound, complete, and implementable.

ASSESS:
1. **Physics soundness** — are the RS-EW formulas (esp. the Z-coupling shift `δg_f/g_Z ∝ (m_Z²/M_KK²)[a_f(c_f) − ⟨a⟩]`, KK gauge masses, mass-basis rotations) correct and consistent with `derivations/` and standard RS literature? Check the normalization-pin claim (universal-c ⇒ all δg=0; IR b_R ⇒ negative δg_R^b ~1e-3 at M_KK~3 TeV) is the right test and the right sign/magnitude.
2. **Completeness/implementability** — is the ParameterPoint field set (`rs_ew_spectrum`, `rs_ew_couplings`, `rs_semileptonic_wilsons`, `rs_charged_current`, optional `rs_dipole_wilsons`/`rs_higgs_yukawas`) sufficient and non-redundant? Is the point_builder logic + new sweep inputs (c_L, c_E[3], c_N, M_N) concrete enough to build? Is the matching map complete (every affected family covered)?
3. **No double-counting** — verify the design's separation of light-Z (δg) vs heavy-KK contact exchange avoids double counting, and that rare-decay Wilsons don't re-apply g_Z.
4. **Convention consistency** — will the new couplings plug into the EXISTING validated `zpole`/`rare_*`/`bsgamma` cores without sign/normalization clashes? This is the top risk both drafts flagged.
5. **ARBITRATE the open items** the consensus doc lists as "unresolved for reviewers": exact closed-form `a(c)` prefactor; complete classic Zbb (fermion-KK mixing) treatment; custodial/BKT defaults; finite dipole-loop normalization; observable-side long-distance/covariance choices. For EACH: either RESOLVE it (cite `derivations/` or standard RS result) or state it genuinely needs a HUMAN MODELING DECISION (and why) — do NOT paper over.
6. **Phasing** — is the proposed sub-phase order correct (each independently testable, dependencies right)?

OUTPUT (<=22 lines): numbered findings (BLOCKER/SHOULD-FIX/NIT) on soundness/completeness/conventions; the per-item ARBITRATION of the open list (RESOLVED-as-X vs NEEDS-HUMAN-because-Y); whether the phasing is sound. End with: DESIGN-OK or DESIGN-NEEDS-FIXES.
