# COMBINED REVIEW — Z to bb re-tag + collider add (independent codex reviewer, gpt-5.x xhigh)
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Two changes were made concurrently and share `scripts/run_full_catalog_scan.py`. Independently review the FULL uncommitted diff (`git diff`); do not trust author self-reports. Verify BOTH changes are correct AND coexist cleanly (no concurrent-edit clobber).

CHANGE 1 (Z to bb re-tag, T010/T011): the minimal non-custodial Zbb prediction (already computed: gauge tower + m_b^2 admixture) must now be VETOING (tag rigorous), with custodial/top-partner/BKT refinements DEFERRED but recorded (not veto-suppressing). The computed R_b/A_b pulls MUST be numerically unchanged (only tag/routing changes). Verify: T010/T011 tag `rigorous` and route HARD failures to excluded_by_rigorous; the deferred custodial note is present but non-suppressing; predicted/ratio values are byte-identical to before; A_FB^{0,b} stays context-only.
CHANGE 2 (collider add): the lepton-free direct KK-mass searches were added to the quark-only allowlist. Verify EACH added CR reads ONLY quark/geometry extras (no swept-lepton data, no silent SM/zero default), the quark-only point supplies its inputs, and the lepton ones (CR005/006/009/011/014) remain OUT/deferred. Confirm tag=proxy/HARD and the new allowlist count.
COEXISTENCE: confirm run_full_catalog_scan.py contains BOTH the collider allowlist additions AND correct tagging for the re-tagged T010/T011 (i.e. nothing from either change was clobbered). Confirm test coverage for BOTH survived in tests/ (re-tag tests + collider allowlist test), none lost.
SMOKE (already run): at M_KK=1 TeV, T010/T011 (rigorous) and CR001 (proxy) both exclude; by 30 TeV they pass. Confirm this is consistent with the code.
Run `python -m pytest tests/ -q` in the foreground; report counts.
OUTPUT (<=14 lines): per-change PASS/FAIL with evidence (file:line); the coexistence/clobber check; numeric-invariance check for Zbb; collider IN/OUT verification; pytest counts. END with EXACTLY ONE line: `COMBINED-REVIEW: APPROVE` or `COMBINED-REVIEW: NEEDS-FIXES`.
