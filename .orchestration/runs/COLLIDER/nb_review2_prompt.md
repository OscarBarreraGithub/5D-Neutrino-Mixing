# Notebook re-point REVIEW (codex, gpt-5.x xhigh)
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing (env: source ~/.bashrc && conda activate ising_bootstrap; if scipy import crashes with GLIBCXX, `export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:$LD_LIBRARY_PATH"`). The notebook `notebooks/wq_quarkonly_explore.ipynb` was UPDATED to point at the NEW 1M run `scan_outputs/wq_quarkonly_1M_20128400` (Z to bb now rigorous/vetoing + lepton-free collider CR* added, allowlist 46). Independently verify (do NOT trust the builder report):
1. RENDERED: the committed .ipynb has embedded image/png outputs for every figure cell and ZERO error/traceback outputs. (Re-execute with `jupyter nbconvert --to notebook --execute --inplace` if you want, but it must already carry rendered outputs.)
2. POINTS AT NEW RUN: no remaining reference to `wq_quarkonly_1M_full` anywhere in .py/.ipynb; SCAN_ROOT / input is `wq_quarkonly_1M_20128400`; the cache it reads was rebuilt from the new run (1,000,000 rows; contains CR001..CR013 columns and T010/T011 as rigorous/HARD).
3. NARRATIVE MATCHES NEW DATA: the markdown now says Z to bb is the DOMINANT rigorous constraint pushing the rigorous M_KK floor to ~25-30 TeV (non-custodial RS Zb_L problem, ~draw-independent); eps_K/Delta-m and the collider floor are subdominant. Confirm NO stale text remains claiming rigorous reach ~1-3 TeV or Z to bb advisory. Confirm the honest caveats are present (non-custodial; custodial deferred relaxes it; ~25-30 TeV pending literature cross-check).
4. PLOTS CONSISTENT: Z to bb (T010/T011) appears as a dominant RIGOROUS carver (not in the advisory panel); collider CR* appear as proxy carvers; M_KK>=4 TeV floor applied; no pooling across r or M_KK. Independently recompute from the cache/JSONL a couple of headline numbers (e.g. T010 veto fraction ~1.0 at 15 TeV, ~0.89 at 20 TeV, 0 at 30; strict survival 0% below 20 TeV, 100% by 30) and confirm they match what the notebook shows.
5. `python -m pytest tests/ -q` still green (the notebook change should not affect it; confirm no production code changed).
OUTPUT (<=12 lines): rendered confirm (fig/error counts), new-run-pointing confirm, narrative-matches confirm with any stale text found, plot/number spot-check vs cache, pytest/scope note. END with EXACTLY ONE line: `NB-REVIEW2: APPROVE` or `NB-REVIEW2: NEEDS-FIXES`.
