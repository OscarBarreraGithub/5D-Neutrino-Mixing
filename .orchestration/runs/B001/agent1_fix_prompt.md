You are agent1 fixing B001 (Delta m_d). Read the code review: .orchestration/runs/B001/agent3_out.md (physics review .orchestration/runs/B001/agent2_out.md was PHYSICS-OK). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX (SHOULD-FIX): the SM theory uncertainty for Delta m_d is hardcoded (0.062) in B001.py and duplicated in the test. Source it from the anchor instead. If flavor_catalog/processes/beauty/B001.yaml already carries the SM prediction uncertainty, load it via the scaffold anchor loader. If it does NOT, add it to the appropriate SM-prediction block of B001.yaml WITH provenance (matching the existing yaml structure), then load it. Add a test that a missing/mismatched SM-uncertainty anchor fails loudly (AnchorError). Remove the duplicated hardcoded literal from the test.
IGNORE the NIT that the adapter contains D0/Bs/K helpers — that is an expected artifact of a parallel implementation wave, NOT a B001 issue; do not touch the adapter.
Keep changes ISOLATED to B001.py, its test, and B001.yaml. Run `python -m pytest tests/constraints/primary/beauty/test_B001.py -q` and `python -m pytest tests/constraints/ -q` until green.
OUTPUT (<=12 lines): what you changed, where the SM uncertainty now comes from, pytest counts, git diff --stat. No full files.
