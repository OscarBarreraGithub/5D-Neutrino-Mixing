# W2 PHASE 5 — PLAN ONLY (codex, gpt-5.x xhigh). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Produce a concrete IMPLEMENTATION PLAN (NO code) for Phase 5 (charged-current sector) of the approved design `.orchestration/rs_ew_sector_design_CONSENSUS.md` (§5 item 5; §1,§4,§6). Codex + Opus dual-review this PLAN before implementation. WRITE to `.orchestration/runs/W2-P5/plan.md` (concrete; dense paragraphs ok).

PHASE 5 SCOPE (per design §5.5): build W/W' (charged gauge-KK) shifts, `delta_G_F_over_G_F`, and charged-current contacts on the builder; then REWIRE: EW002 (CKM first-row unitarity), K017/K018 (|Vus| from K_l3 / Kμ2), B009 (B→τν), B025 (semileptonic LFU e.g. R(D)/R(D*) or LFU ratios), then EW003 (|Vcb|/|Vub| incl-vs-excl, with a covariance note).

THE PLAN MUST SPECIFY:
1. **Charged-current physics**: how the W-KK spectrum is obtained (the charged gauge-KK tower — same NN gauge roots as the Z sector? confirm via solvers/bessel.py) and how `delta_G_F/G_F` arises (W-KK exchange in muon decay + W-fermion vertex overlap shifts) in terms of the existing a(c)/overlap machinery + f-factors. The W-fermion coupling shift analog of the Z `z_delta_g` (charged-current is purely left-handed). Reference design §1/§4 and derivations/.
2. **Builder extension**: new `rs_charged_current` typed extra (W' mass, δG_F/G_F, charged-current vertex shifts / |Vij| corrections), added to KNOWN_EXTRA_KEYS; computed from c_Q/c_L (left-handed) + the W-KK overlaps; dimensionless conventions; fail-loud. New sweep inputs if any.
3. **Rewire map**: per constraint (EW002, K017?, K018, B009, B025, EW003): the CURRENT proxy/NEEDS-HUMAN → the new rigorous δG_F / |Vij|-shift input → formula. CRUCIAL: the SM-vs-data PULL is already rigorous (that IS the constraint); only the RS shift was a proxy. State which become FULLY rigorous and which stay PARTIAL. EW003 covariance note (incl-vs-excl |Vcb|/|Vub| correlated uncertainties).
4. **v1 behavior**: with universal c_L and the diagonal fits, what is the size/sign of δG_F/G_F and the |Vij| shifts at M_KK~3 TeV? Will these constraints bite or be ~SM in v1? (Charged-current shifts are typically universal ⇒ partially absorbed in G_F definition — clarify what's physical vs absorbed, analogous to the a_ref subtraction for neutral current.)
5. **Sub-step breakdown**: independently-testable sub-steps (e.g. 5a builder+W shifts+δG_F no-rewire; 5b EW002+K018+B009+B025 CKM/CC rewire; 5c EW003 with covariance). Order + what each touches.
6. **Validation**: universal-c ⇒ the absorbed/reference piece subtracted so SM recovered; δG_F/G_F sign+magnitude sanity vs known RS bounds; each rewired constraint SM-limit unchanged; full suite green; independent cross-checks.
7. **Risks**: what's absorbed into measured G_F (the analog of a_ref — avoid double-count); CKM-element definition/scheme; EW003 correlation; whether B025's LFU ratio cancels universal pieces; what stays NEEDS-HUMAN.

OUTPUT (<=20 lines): the charged-current physics source, the builder extra, the rewire map, the v1-behavior expectation (bites vs SM), sub-step breakdown, validation, top risks (esp. G_F-absorption/double-count + EW003 covariance). Confirm plan.md written. PLAN ONLY.
