1. Review gate: this is plan-only; implementation starts only after Codex review plus external Opus signoff, since no Opus sub-agent is available in this session.
2. Charged spectrum source: use the same exact gauge NN Bessel roots as the neutral sector, via `solve_kk("gauge", "NN", exact=True)` (`m_n=x_n Lambda_IR`, `x_1~2.4505`, not the `2.405` small-epsilon limit), and include the zero mode in the W diagonalization; W/W' is not a new root problem, but the charged mass matrix from `derivations/rs_ew_gauge_kk_coupling.tex`, `(m_n^2 delta_mn + g2^2 v^2 chi_m(1) chi_n(1)/4)`, diagonalized on that zero-plus-KK tower.
3. Charged-current matching: reuse `Omega_n(c)` and the identical shared EW-universal scalar object `a_ref = spectrum.a(DEFAULT_A_REF_C=0.65)` already used by neutral/lepton matching; do not recompute a separate charged-current reference. With `V_CKM=U_L_u^dag U_L_d`, store the left-handed analogue of `z_delta_g`, `delta_g_W_ud_L = eta_W (m_W^2/M_KK^2) U_L_u^dag diag(a(c_Q)-a_ref) U_L_d` relative to `V_CKM`, where `eta_W` is pinned from the W mass-eigenvector sign convention, the charged-current analogue of neutral `s_z=-1.0`, not hardcoded; `delta_g_W_lnu_L` comes from `c_L` relative to the PMNS/charged-lepton basis; minimal RS has no right-handed W term.
4. Heavy charged contacts: build explicit `charged_contact_LL` tensors in GeV^-2 from the charged tower mode sum, using the same current normalization as `L_W=g2/sqrt2 W J`; define `C_SM` consistently with that convention, and expose amplitude shifts `epsilon_ij^a = delta_g_W_ud_L[i,j]/V_ij + delta_g_W_lnu_L[a,a] + charged_contact_LL[i,j,a,a]/C_SM - delta_G_F_over_G_F`.
5. `delta_G_F_over_G_F`: compute the total muon-decay amplitude shift in the same `C_SM` convention as the charged-current epsilons, from light-W e/mu lepton vertex shifts plus W' exchange, with raw components kept in diagnostics; adapters subtract the stored value exactly once in each consumed epsilon, analogous to `a_ref` absorption, with no double-count, and the universal/reference subtraction must make all consumed effective shifts vanish in the SM-universal test.
6. Builder extension: add `quarkConstraints/rs_charged_current.py` with frozen `RSChargedCurrentCouplings`, add `rs_charged_current` to `KNOWN_EXTRA_KEYS`, and have `build_rs_ew_extras` populate it after `rs_ew_spectrum` and lepton mass-basis inputs; if `c_L`/lepton inputs are absent while charged current is requested, raise loudly.
7. Extra contents: include `m_w_gev`, `m_wprime_gev`, charged mixing metadata including `eta_W`, `delta_G_F_over_G_F`, `delta_g_W_ud_L`, zero `delta_g_W_ud_R` with status string, `delta_g_W_lnu_L`, zero `delta_g_W_lnu_R` with status string, `charged_contact_LL`, per-channel `epsilon`/`delta_abs_vij_over_vij`, `a_profile_values` for `c_Q,c_L`, units, input bundle, and matching-assumption diagnostics; no new sweep inputs beyond existing `lepton_sweep_inputs`, only an optional constants/input dataclass.
8. EW002 rewire: current state is rigorous data-level first-row pull with no RS shift; new input is `epsilon_ud^e`, `epsilon_us^{K avg}` and optional `epsilon_ub`; formula `Delta_CKM_NP ~= 2 sum |V_ij|^2 Re epsilon_ij`, compared against the existing YAML first-row sum/budget. This becomes rigorous for minimal LH W/W' shifts, still SOFT and not a veto.
9. K018 rewire: current state is rigorous YAML `|Vus| f_+(0)/f_+(0)` extraction with no point shift; new formula for K_l3 `|Vus|` is `|Vus|_app = |Vus| |1+epsilon_us^l|` or linear `delta|Vus|/|Vus|=Re epsilon_us^l`, with e/mu mode weights only if the YAML exposes them. Full for minimal LH W/W' shifts, but the constraint remains SOFT; radiative/isospin/mode-weight bookkeeping stays partial if unavailable.
10. K017 rewire: current repo observable is LFU `R_K`, not a standalone K_mu2 `|Vus|`; replace the electron-only `m_K^2/M_KK^2` proxy by `R_K = R_K^SM |1+epsilon_us^e|^2/|1+epsilon_us^mu|^2`. Full for minimal LH W/W' LFU/LNU vector effects and remains HARD; charged-Higgs/heavy-neutrino/radiative nonminimal effects remain NEEDS-HUMAN.
11. B009 rewire: current state is a `m_B^2/M_KK^2` charged-current proxy; new formula is `BR(B->tau nu)=BR_SM |1+epsilon_ub^tau|^2`, using the YAML `f_B` and `|Vub|` anchors unchanged. Full for minimal LH W/W'; charged-Higgs/RH/scalar pieces remain partial.
12. B025 rewire: current state is a scalar `m_b m_tau/M_KK^2` stress proxy; replace it by the minimal-vector LFU-ratio tree `R(D)=R_SM |1+epsilon_cb^tau|^2/|1+epsilon_cb^light|^2` where light is the e/mu denominator convention. This vector LFU-ratio tree is rigorous, but B025 stays PARTIAL because scalar/RH WET terms and form-factor integration are not built, and universal vector shifts should cancel. This corrects the consensus design section 4 label from FULL to PARTIAL, so the agreed accounting is FULL 25 / PARTIAL +1, consistent with the exclusive-channel handling for B013/B014.
13. EW003 rewire: keep EW003 data-level plus charged-current diagnostics for `epsilon_cb` and `epsilon_ub`, but keep the main inclusive-vs-exclusive pull data-level unless a covariance/scheme input is supplied; common vector rescalings cancel within each inclusive/exclusive pair, and correlated CKM/form-factor uncertainties must not be treated as independent quadrature.
14. v1 expectation: at `M_KK~3 TeV`, `(m_W/M_KK)^2~7e-4`; with universal `c_L` and diagonal charged-lepton fits, lepton and G_F pieces are mostly absorbed, first-row shifts are usually tiny, and B025 LFU cancels, so most constraints stay near SM. Third-family channels can see residual quark-doublet shifts of order `1e-4-1e-3`; sign follows the W mixing convention (`eta_W` analogous to neutral `s_z`, expected negative for more IR-composite left doublets, while direct W' contact is positive before G_F subtraction).
15. Substep 5a: extend `RSEWSpectrum` with the charged W zero-plus-KK diagonalization helper, then implement only builder/dataclass/tests for charged spectrum, `eta_W`-signed W shifts, W' contacts, `delta_G_F_over_G_F`, and zero/SM/universal limits; touch `quarkConstraints/rs_charged_current.py`, `rs_ew_builder.py`, `point_builder.py`, and tests, with no constraint rewires. Add an explicit test asserting the `eta_W` sign is pinned from the W mass eigenvector.
16. Substep 5b: rewire EW002, K018, K017, B009, and B025 plus adapter helpers to consume explicit `epsilon` or shifted CKM inputs; remove proxy diagnostics where replaced and leave precise PARTIAL flags for missing nonminimal operators.
17. Substep 5c: update EW003 with charged-current diagnostics and a covariance-aware note/interface, but do not change its scalar pull unless covariance inputs are present; add regression tests that the SM-limit output is unchanged.
18. Validation and risks: validate exact NN roots, W' masses, shape/unit/finiteness, universal-a_ref SM recovery, `delta_G_F` magnitude/sign sanity, no second `1/M_KK^2`, and full suite green; top risks are G_F/a_ref double-counting, CKM extracted-vs-Lagrangian scheme, PMNS/neutrino-sum conventions, B025 universal cancellation, K017 vs K_mu2 naming, EW003 covariance, and all RH/scalar/charged-Higgs/fermion-KK/custodial effects remaining NEEDS-HUMAN.
