# Headline figures REVIEW (codex, gpt-5.x xhigh)
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing (env: source ~/.bashrc && conda activate ising_bootstrap; if scipy GLIBCXX-crashes, export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:$LD_LIBRARY_PATH"). An Opus agent produced 5 clean headline figures in `scan_outputs/wq_quarkonly_1M_20128400/analysis/headline/` (+README.md + read-only generator `_make_headline_figs.py`) from the 1M quark-only run. Independently verify FAITHFULNESS + READABILITY; do not trust the builder.
1. Each PNG is non-empty and renders (open/inspect). Fonts large, few elements, clear titles/axis labels/legends, M_KK axes floored at 4 TeV.
2. Numbers faithful: independently recompute from `scan_outputs/wq_quarkonly_1M_20128400` (cache parquet or JSONL): (a) rigorous strict survival vs M_KK (0 below 20 TeV, ~0.108 at 20, 1.0 by 30); (b) T010 Z->bb veto fraction 1.0/0.892/0.0 at 15/20/30 TeV; (c) the per-constraint reach ranking - Z->bb ~20 TeV highest, then EW001/CR001 ~5 TeV, eps_K/Dm ~1 TeV. Confirm fig2 (reach ranking) and fig3 (Zbb veto) match. Confirm survival is computed over EVALUATED rows and `partial` placeholders (B034/EW003) are NOT in the gate.
3. No scale-mixing: per-r or clearly-stated pooling; rigorous vs proxy honestly colored/labeled (Z->bb rigorous, CR*/EW001 proxy).
4. The figures' message matches the run: Z->bb dominant, ~25-30 TeV non-custodial floor, eps_K/collider subdominant.
OUTPUT (<=10 lines): render confirm; your recomputed survival/veto/reach numbers vs the figures; readability assessment per figure; any fix needed. END with EXACTLY ONE line: `HEADLINE-REVIEW: APPROVE` or `HEADLINE-REVIEW: NEEDS-FIXES`.
