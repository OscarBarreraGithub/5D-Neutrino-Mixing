OPUS independent verification of a Z to bb magnitude cross-check. Work SYNCHRONOUSLY; end with a verdict line. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing (env: source ~/.bashrc && conda activate ising_bootstrap; if scipy GLIBCXX-crashes, export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:$LD_LIBRARY_PATH").

A codex cross-check (`.orchestration/runs/ZBB-XCHECK/author_codex_out.md`) found that the now-dominant Z to bb constraint's left-handed coupling shift delta g_bL is dominated by the m_b^2 fermion-KK admixture term (NOT the gauge term), and that its coefficient B_d ~= -4.43e4 is ~4.5x larger than the compact Casagrande "ZbbRS" bracket value ~-9.78e3 - implying the ~25-30 TeV physical M_KK floor is either (a) convention-consistent with the literature >=10 TeV once you account for physical-M_KK = ~2.45 * Lambda_IR, or (b) ~4.5x over-normalized if the compact formula was intended. RE-DERIVE INDEPENDENTLY; do not trust codex.

DO:
1. Reproduce the benchmark: pick the same kind of fitted point (c_Q3~0.364, F_Q3, Lambda_IR~8.2 TeV, physical M_KK~20 TeV). Independently compute the scan's delta g_bL and DECOMPOSE it into the gauge-tower piece (z_delta from a(c)) vs the m_b^2 Casagrande admixture (build_rs_zbb_fermion_kk_mixing). Confirm/refute that the m_b^2 admixture DOMINATES and that the gauge piece is ~20x smaller. (rs_ew_couplings.py ~505-551 gauge, ~704-744 + ~920-960 admixture; rs_ew_spectrum.py a(c).)
2. Read the ACTUAL B_d/B_Q definition in the code (_casagrande_zbb_B_profile + the light-generation Yukawa-ratio sum sum_i |Y_d[2,i]|^2/|Y_d[2,2]|^2). Determine: is this the FULL Casagrande ZMA2 expression, or the compact leading "ZbbRS" bracket, or something else? Is the large B_d (~-4.43e4) driven by the Yukawa-ratio sum (a real anarchic-hierarchy enhancement) or by a profile/normalization factor? Is B_d INTERNALLY CONSISTENT with whatever convention the code claims (check the docstring/comments and the cited equation)?
3. Resolve the units question: confirm whether the scan's "M_KK" in the veto is the physical first KK gauge mass (~2.45*Lambda_IR) or Lambda_IR, and therefore whether ~25-30 TeV physical corresponds to ~10-12 TeV in Casagrande's Lambda_IR units (=consistent with the literature) or not.
4. VERDICT: is the ~25-30 TeV physical Zbb floor (i) physically correct & convention-consistent with the literature >=10 TeV (no fix), (ii) high but within legitimate convention/observable spread, or (iii) over-aggressive due to a specific B_d/B_Q normalization error (name the exact factor and where). If (iii), state the suspected fix but DO NOT implement it. Be explicit about which part is a genuine PHYSICS-CONVENTION choice the physicist must make vs a clear code error.

OUTPUT (<=16 lines): your independent delta g_bL decomposition (gauge vs m_b^2) + which dominates; what B_d/B_Q actually is in the code and whether the ~4.5x vs compact-ZbbRS is a convention difference or an error; the units resolution (physical vs Lambda_IR; does 25-30 TeV map to ~10-12 TeV Lambda_IR?); agree/disagree with codex; verdict (i/ii/iii) + what (if anything) the physicist must decide. End with EXACTLY ONE line: `ZBB-XCHECK-VERIFY: <CONSISTENT|CONVENTION-CHOICE|ERROR>`.
