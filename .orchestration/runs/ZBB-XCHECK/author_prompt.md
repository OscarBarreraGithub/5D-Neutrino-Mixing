# Z to bb magnitude CROSS-CHECK (codex, gpt-5.x xhigh) — VERIFICATION ONLY, no code change
Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing (env: `source ~/.bashrc && conda activate ising_bootstrap`; if scipy crashes on GLIBCXX, `export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:$LD_LIBRARY_PATH"`).

The quark-only scan now has Z to bb LIVE and it is DOMINANT: T010/T011 veto ~100% of points up to ~15 TeV and ~89% at 20 TeV, implying a rigorous M_KK floor of ~25-30 TeV. The literature non-custodial RS Zbb bound is usually quoted as M_KK >~ 10 TeV. Independently verify whether the scan's ~25-30 TeV is CORRECT or whether the minimal Zb_L normalization is too aggressive. NO code changes - this is a verification/diagnosis pass.

## DO
1. Read the implementation: the gauge Zb_L shift in z_delta_g_L/R_d[2,2] via the exact tower a(c) (quarkConstraints/rs_ew_couplings.py ~505-551; rs_ew_spectrum.py a(c)/Omega ~789-917), the m_b^2 Casagrande admixture (rs_ew_couplings.py ~704-744), and the T010 R_b/A_b pull + LEP anchors (T010.py; flavor_catalog/processes/top_higgs_ew/T010.yaml).
2. Pick a representative benchmark fitted point (e.g. run `fit_quark_sector(default_quark_targets(), ...)` once, or pull a typical row's c_Q3 from `scan_outputs/wq_quarkonly_1M_20128400`). Extract the scan's actual delta g_bL (and g_bR) and the R_b, A_b predicted pulls as a function of M_KK; find the M_KK where T010's max abs_pull crosses 1 (the scan's effective Zbb reach). Confirm it is ~25-30 TeV from the real ratio scaling (ratio ∝ 1/M_KK^2).
3. INDEPENDENTLY compute delta g_bL from the standard analytic Casagrande gauge formula (arXiv:0807.4937, "ZbbRS"): delta g_bL ≈ g_bL^SM * [ -m_Z^2/(2 M_KK^2) * F(c_Q3)^2/(3+2 c_Q3) * (L - (5+2 c_Q3)/(2(3+2 c_Q3))) ], L = ln(M_Pl/TeV)-ish warp log. Use the SAME c_Q3, F(c_Q3), L, M_KK. Compute the implied R_b/A_b shift and the M_KK where it crosses the LEP bound. Compare to the scan's exact-tower value: do they agree (ratio ~1) or differ by an O(1)/factor?
4. DIAGNOSE the ~25-30 TeV vs ~>=10 TeV gap. Specifically check: (a) is the dominant pull R_b or A_b? (b) A_b at LEP/SLC has a ~2-3 sigma SM tension; does the RS shift add to that tension (inflating the reach) or not, and is T010 using A_b's measured central value (tension-inflated) vs an SM-reference? (c) is L (the warp log) or the a_ref subtraction or the chiral (g_L vs g_R) assignment plausibly inflating it? (d) is the literature ">~10 TeV" quoting a DIFFERENT observable normalization (e.g. only R_b, or custodial-partial)? 
5. VERDICT: is the scan's ~25-30 TeV (i) physically correct for the minimal non-custodial setup, (ii) somewhat high but within convention/observable-choice spread, or (iii) too aggressive due to a specific normalization issue (name it). If (iii), state the suspected fix but DO NOT implement it.

## OUTPUT (<=18 lines)
Benchmark c_Q3/F/L/M_KK used; scan delta g_bL + R_b/A_b pulls + crossing M_KK (the ~25-30 TeV); your independent analytic delta g_bL + crossing M_KK; the scan-vs-analytic ratio; which observable dominates (R_b vs A_b) and whether A_b tension inflates it; comparison to literature >~10 TeV; the verdict (i/ii/iii) with the specific cause if (iii). End with: ZBB-XCHECK-DONE.
