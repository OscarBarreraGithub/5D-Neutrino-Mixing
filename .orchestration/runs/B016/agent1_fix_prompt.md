You are agent1 fixing B016 (B→Kℓℓ exclusive) + the exclusive machinery in quarkConstraints/rare_b_dilepton.py. Physics review .orchestration/runs/B016/agent2_out.md = PHYSICS-NEEDS-FIXES (3 SHOULD-FIX); code CODE-OK. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX:
1. The low-q² "validation" uses a hardcoded bin (1.1-6.0) with no sourced value — stop claiming YAML validation for it; either anchor to a sourced partial/differential BR from B016.yaml/snapshot if available, or clearly label it an internal formula benchmark (not a data-validated number) in docstring + test.
2. Budget too tight (7.1%) for a C9/C10-only, no-covariance total-BR proxy: the omitted C7 (~10%) + nonlocal charm + form-factor uncertainties dominate. Inflate the budget with an explicit, documented proxy/theory uncertainty (so the HARD verdict isn't spuriously tight), tied to the NEEDS-HUMAN-PHYSICS proxy limitation. Document the inflation source/rationale.
3. Scope: B016 = CHARGED B⁺→K⁺μμ. The same machinery predicts the NEUTRAL mode at 6.39σ tension — do NOT let B016 implicitly constrain the neutral mode. Restrict B016 to the charged mode explicitly in docstring/anchor selection; note the neutral mode is out of scope (separate entry / future).
Keep RS C9/C10 proxy NEEDS-HUMAN-PHYSICS. Isolated to B016.py, test, rare_b_dilepton.py (append-only). Run tests/constraints/ -q green. OUTPUT <=12 lines: changes, new budget value + rationale, pytest counts.
