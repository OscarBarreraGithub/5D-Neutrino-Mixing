RETROACTIVE BATCH CODE REVIEW (codex). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Three retro items committed during the rebuild WITHOUT codex review; review all three now. Do NOT modify code. Verify correctness + that they're consistent with the now-rigorous RS-EW sector. Run pytest where relevant.
R2 — DeltaF=2 adapter running-wrappers (commit fd2f46a): physics_adapters/deltaf2.py `evaluate_*_with_running` wrappers. Verify the RG-running Wilson evolution (mu_had=2 GeV) is correct, the wrappers don't double-apply running, and they delegate to the core correctly.
R3 — complex-M12 phase helpers (commit e08977d): the B002/C002 adapter phase helpers (S_psiKs/sin2beta, charm CPV). Verify the complex M12 phase extraction (Im part) is correct and feeds the constraints right.
R5 — pre-rebuild cores never re-reviewed in a gate: quarkConstraints/deltaf2.py (added 5206fc8) and quarkConstraints/paper_0710_1869/scales.py (added c540830). These underpin the wave-1 DeltaF=2 constraints (K001/K002/B001/B003/C001, the 5 fully-rigorous anchors). Verify the DeltaF=2 core (M12 computation, RG running, bag factors) and scales.py (spin-2 graviton root 3.8317 etc.) are physically correct.
For EACH: numbered findings BLOCKER/SHOULD-FIX/NIT with file:line + evidence (recompute a key number). Note the DeltaF=2 cores are heavily used; a bug here affects the 5 rigorous anchors.
OUTPUT (<=20 lines): per-item (R2/R3/R5) findings + verdicts. pytest tests/constraints/ -q count. End with: RBATCH-OK or RBATCH-NEEDS-FIXES (list which items need fixes).
