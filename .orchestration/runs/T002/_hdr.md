# Implement T002 — BR(t→uZ) FCNC. family=top_higgs_ew. REUSE top_fcnc.
REUSE quarkConstraints/top_fcnc.py (built for T001) + adapter. t→uZ is the up-quark analog of t→cZ; reuse with the t-u FCNC coupling. SM negligible → pure-NP bound vs collider limit (from T002.yaml), HARD. RS top-Z FCNC proxy reused → NEEDS-HUMAN-PHYSICS. Append-only if needed (do not modify T001's c functions).
