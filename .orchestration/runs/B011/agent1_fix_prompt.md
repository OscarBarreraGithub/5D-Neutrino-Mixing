You are agent1 fixing B011 (B̄→X_sγ) and its shared module quarkConstraints/bsgamma.py. Reviews: .orchestration/runs/B011/agent2_out.md = PHYSICS-NEEDS-FIXES (2 BLOCKERS), agent3_out.md = CODE-NEEDS-FIXES. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX:
1. (BLOCKER) Chirality reversed: with O7=(s̄_L σ b_R) and O7'=L↔R, the LEFT-handed b→s overlap must feed C7 and the RIGHT-handed overlap must feed C7' — the code maps right→C7 and left→C7'. Swap to the correct assignment.
2. (BLOCKER) No RG running: C7_NP is built at M_KK and inserted directly into the C7_SM(μ_b)=-0.304 rate. Add b→sγ RG evolution from M_KK to μ_b≈4.8 GeV for C7 AND C8 with their mixing (LL: C7(μ_b) ≈ 0.50·C7(M_KK) + 0.121·C8(M_KK); use the standard LL coefficients). Apply the NP at the low scale consistently with C7_SM(μ_b). (This mirrors the mandatory-running lesson from K001.)
3. (SHOULD-FIX code) The test cross-check is manual-formula-only; add a test that imports quarkConstraints.bsgamma and recomputes via compute_bsgamma_wilsons + branching_fraction_from_c7 (or evaluate_inclusive_bsgamma) — independent of the formula in the test.
Validate SM BR(B→X_sγ)≈3.3e-4 still reproduced after the running fix. Keep NEEDS-HUMAN-PHYSICS proxy flag. Isolated to bsgamma.py + B011.py + test. Run tests/constraints/ -q green. OUTPUT <=12 lines: changes, SM BR after running, the running coefficients used, pytest counts.
