# W2 PHASE 3 — PLAN ONLY (codex, gpt-5.x xhigh). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing. Produce a concrete IMPLEMENTATION PLAN (NO code yet) for Phase 3 of the approved design `.orchestration/rs_ew_sector_design_CONSENSUS.md` (§5 item 3; plus §3 point_builder, §4 schemas, §6 normalization pin). A codex reviewer and an Opus reviewer will review this PLAN (both must APPROVE) before any implementation. WRITE the plan to `.orchestration/runs/W2-P3/plan.md`.

PHASE 3 SCOPE (quark neutral currents): build quark mass-basis Z matrices + heavy neutral contacts from the Phase-2 kernel (`quarkConstraints/rs_ew_spectrum.py`, dual-approved) + the Yukawa-fit rotations; extend `point_builder`/`ParameterPoint`; and REWIRE Z-pole (T010–T013), FCNC-Z (T014), and same-flavor rare B/K/charm VECTOR Wilsons from proxy → rigorous, via the vector-diagonalized path, bypassing the old proxy helpers.

THE PLAN MUST SPECIFY:
1. **point_builder/ParameterPoint extension** — exactly which new extras to populate (`rs_ew_spectrum`, `rs_ew_couplings`, `rs_semileptonic_wilsons` quark sector, …), added to `KNOWN_EXTRA_KEYS` (fail-loud); how each is computed from sweep inputs (c-values, Λ_IR/M_KK, quark rotations) using RSEWSpectrum; the storage convention (dimensionless additive `z_delta_g` in the zpole convention, NO extra g_Z; contacts GeV^-2 already carrying 1/m_Z²·1/M_V²; rare Wilsons do NOT reuse `_wilson_prefactor`).
2. **Which constraints/adapters get rewired and HOW** — list each (T010,T011,T012,T013 Z-pole; T014 FCNC-Z; same-flavor rare B/K/charm vector Wilson consumers) with: the current proxy call → the new rigorous input/formula; confirm the SM side is UNCHANGED (only NP proxy→rigorous); confirm the per-constraint NEEDS_HUMAN flag is removed only where now rigorous (kept where still partial).
3. **A TRACTABLE SUB-STEP BREAKDOWN** — split Phase 3 into independently-testable, independently-reviewable sub-steps (e.g. 3a builder+quark-Z-matrices+contacts with unit tests but NO rewiring; 3b Z-pole rewire; 3c FCNC-Z; 3d rare B/K/charm vector) so each can pass the gate without a giant diff. Recommend the order + what each touches.
4. **TEST/VALIDATION strategy** — the mandatory checks: universal-c ⇒ all δg=0 (constraints recover SM); IR b_R ⇒ δg_R^b<0 ~1e-3 @3TeV reproduced at the COUPLING level (the design's pin); each rewired constraint's SM-limit output unchanged vs its committed value; full suite stays green; new numbers cross-checked independently of the adapter.
5. **RISKS** — normalization/sign clashes with the validated zpole/rare_* cores; double-counting; which rotations (CKM/up/down) feed the quark Z matrices; what stays PARTIAL/NEEDS-HUMAN after Phase 3.

OUTPUT (<=20 lines): the sub-step breakdown (3a/3b/…), the point_builder extension summary, the per-constraint rewire list (proxy→rigorous), the validation checks, and the top risks. Confirm `plan.md` written. This is PLAN ONLY — no production code.
