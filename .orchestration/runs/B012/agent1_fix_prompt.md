You are agent1 fixing B012 (B→K*γ). Code review .orchestration/runs/B012/agent3_out.md = CODE-NEEDS-FIXES (1 BLOCKER); physics .orchestration/runs/B012/agent2_out.md = PHYSICS-OK. Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
FIX (BLOCKER): _load_b012_anchor accepts an Anchor with a wrong block_key (probe returned NO_RAISE). Validate the neutral/charged block_key against the expected candidate tuples and raise AnchorError loudly on mismatch (like the missing-anchor case already does). Add a monkeypatch test near test_B012.py:147 that forces a mismatched block_key and asserts AnchorError.
Keep everything else (reused bsgamma C7 machinery, exclusive normalization, NEEDS-HUMAN-PHYSICS flag). Isolated to B012.py + test_B012.py. Run `python -m pytest tests/constraints/primary/beauty/test_B012.py -q` and `tests/constraints/ -q` green. OUTPUT <=8 lines: change, pytest counts.
