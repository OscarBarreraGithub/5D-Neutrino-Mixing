You are agent1 fixing T011 (Z→bb̄ asymmetries A_FB^{0,b}, A_b). Reviews .orchestration/runs/T011/{agent2_out.md,agent3_out.md} = NEEDS-FIXES (2 BLOCKERS). Repo: /n/holylabs/randall_lab/Lab/obarrera/5D-Neutrino-Mixing.
CONTEXT: T011.yaml is currently a CLOSED-AS-MERGED stub pointing to T010, and T011.py falls back to T010 anchors. BUT A_FB^{0,b} (b-quark forward-backward asymmetry, the famous LEP A_FB^b anomaly) is a genuine, distinct observable from T010's R_b. The honest fix is to PROMOTE T011 to a real standalone constraint, not alias T010.
FIX (BLOCKERS):
1. Add STANDALONE T011 anchors to flavor_catalog/processes/top_higgs_ew/T011.yaml with provenance: A_FB^{0,b} = 0.0992 ± 0.0016 and A_b = 0.923 ± 0.020 (LEP/SLD electroweak working group, PDG). Include source/year/url. Replace the CLOSED-AS-MERGED stub status with a real entry (keep a note that it was previously cross-referenced to T010).
2. T011.py must LOAD these standalone anchors via load_anchor and FAIL LOUDLY if missing — NO fallback to T010 anchors. Reuse quarkConstraints/zpole.py for the asymmetry observables (A_FB^{0,b} = (3/4) A_e A_b; A_b from the b couplings). Update the test to assert standalone anchors are used and to REJECT any T010 fallback.
Reuse zpole (T010/T012 funcs untouched, append-only). RS Zbb-shift proxy NEEDS-HUMAN-PHYSICS. Validate SM A_b≈0.935, A_FB^{0,b}≈0.103. Run tests/constraints/ -q green. OUTPUT <=12 lines: anchors added, SM values, pytest counts.
