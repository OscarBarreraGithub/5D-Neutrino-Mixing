# Notebooks

Executed analysis notebooks that were previously stored in the repository root
now live here.

These notebooks use repo-relative paths such as `../scan_outputs/` and
`../results/figures/` so they can be run from this directory without moving
artifacts back into the repo root.
