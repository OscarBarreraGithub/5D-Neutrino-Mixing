# PKA Worklog: C006

Process: `D0 -> e mu` lepton-flavor-violating neutral-charm decay.

I checked out `flavor-catalog/2026q2` first and read the plan, orchestrator
decisions, Opus signoff, process template, and catalog README in the required
order.  The assigned Section C row identifies C006 as a charm LFV mode with
initial coverage `NOT-SEEN` and a likely LHCb/PDG upper limit.

Source search focused on PDG Live/API and primary arXiv experiment records.
Queries included `PDG Live D0 e mu branching fraction S032`, `LHCb D0 e mu
1.3e-8 arXiv 1512.00322`, `BaBar D0 e mu 3.3e-7`, and `Belle D0 e mu
2.6e-7`.  The canonical current value is PDG Live/API `S032.40`, accessed on
2026-05-16: `BR(D0 -> mu+- e-+) < 1.3e-8` at 90% CL, using the LHCb 2016
measurement.  The LHCb arXiv abstract confirms the same limit with `3.0 fb^-1`
of 7 and 8 TeV pp data and notes that it improved the previous limit by an
order of magnitude.  Belle 2010 and BaBar 2012 are retained as previous
post-2008 B-factory limits.  I did not use HFLAV because this is a direct
forbidden-decay upper limit rather than a charm-mixing average.

All snapshots are minimal text files under `flavor_catalog/references/C006`;
no publisher PDFs were stored.  The RS baseline arXiv:0804.1954 is included
only as `paper_era_reference` context, not as a measured observable, following
the L001 arbitration policy.

Code coverage was rerun across `quarkConstraints/`, `qcd/`,
`flavorConstraints/`, `neutrinos/`, `yukawa/`, `warpConfig/`, `solvers/`,
`scanParams/`, and `tests/`.  Focused searches for `D0.*e.?mu`,
`c.?->.?u.*e.*mu`, and related spellings found no C006 implementation; nearby
hits are D0 mixing and the separate `mu -> e gamma` checker.  I marked coverage
`NO` and implementation difficulty `MEDIUM`, since a future first-pass live
constraint needs a new Delta C = 1 charged-LFV operator and rate normalization,
but not a global fit or the existing neutral-meson RG machinery.
