# PKA Worklog: T006

Process: `t -> u g`, assigned from plan v1 Section C as `T006` in the
`top_higgs_ew` family.  The assigned row is "Top FCNC gluon decay /
production; ATLAS/CMS/PDG", with initial coverage `NOT-SEEN`.

I first checked out `flavor-catalog/2026q2` and read the required planning
documents, process template, and catalog README in order.  For the canonical
experimental value I used PDG/pdgLive node `Q007TUG`, accessed on 2026-05-16.
That page records the current ATLAS Run-2 single-top FCNC result with
`139 fb^-1` at `sqrt(s)=13 TeV`: for the up-gluon mode,
`sigma(ug -> t) B(t -> bW) B(W -> l nu) < 3.0 pb`,
`|C_uG^{ut}|/Lambda^2 < 0.057 TeV^-2`, and
`B(t -> u g) < 6.1e-5` at 95% CL.  I also captured the CMS 7+8 TeV comparison
result from arXiv:1610.03545: `|kappa_tug|/Lambda < 4.1e-3 TeV^-1` and
`B(t -> u g) < 2.0e-5`.

For theory context I used Aguilar-Saavedra arXiv:hep-ph/0409342.  The source
quotes `B(t -> c g)=4.6e-12` and states that the up-mode rates are suppressed
relative to charm modes by `|V_ub/V_cb|^2 ~ 0.0079`; I therefore recorded the
derived SM estimate `B(t -> u g) ~ 3.6e-14` in `theory_context`, not
`pdg_or_equivalent`.  I included arXiv:0804.1954 only as the requested
2008-era RS flavor baseline.  Its `21 TeV` and `33 TeV` KK-gluon context
numbers are kept in `paper_era_references`, consistent with the Opus L001
arbitration policy.

Source snapshots are short text excerpts only.  I did not download or track
publisher PDFs.  ArXiv abstract and TeX-source excerpts were used for the
ATLAS, CMS, SM-theory, and CFW sources.

Code coverage was checked with the three plan greps plus a targeted search for
`t -> u g`, `tug`, `utg`, `tqg`, `C_uG`, and `ug -> t` across
`quarkConstraints/`, `qcd/`, `flavorConstraints/`, `neutrinos/`, `yukawa/`,
`warpConfig/`, `solvers/`, `scanParams/`, and `tests/`.  No implementation was
found.  The only implementation-directory hits were generic FCNC prose or
comments, not a top-gluon FCNC observable.

Open issues for WA/CA: decide whether to mention the numerically stronger
older CMS limit prominently, and normalize the final prose around the ATLAS
SMEFT `C_uG` convention versus the older CMS `kappa_tug` convention.
