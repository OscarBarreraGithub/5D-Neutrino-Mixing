# K008 PKA Worklog

Process: `K_L -> pi0 e+ e-` (`K008`, kaon).  I first checked out
`flavor-catalog/2026q2` and read the required plan, orchestrator decisions,
Opus signoff, process template, and catalog README in order.  Existing dirty
worktree edits were only in unrelated charm files and were left untouched.

Searches and source choices: I used PDG pdgLive REST/API queries for
`S013.20` and `S013R34`, confirming the canonical measured limit
`BR(K0L -> pi0 e+ e-) < 2.8E-10` at 90% CL, with the used input being the KTeV
combined result.  I used the KTeV arXiv preprint `hep-ex/0309072` as the primary
experimental paper and extracted only the abstract/introduction value text, not
the PDF.  Because the indirect-CP piece depends on the short-lived mode, I also
queried PDG `S012.10` for `K0S -> pi0 e+ e-` and recorded the NA48 value and
full-region extrapolation.  For CP separation I used the arXiv text extraction
from Isidori, Smith, and Unterdorfer (`hep-ph/0404127`), which gives the
`C_mix`, `C_int`, `C_dir`, and `C_CPC` coefficients for the electron mode.
For post-2008 context I deposited arXiv abstract snapshots for the 2016
RBC/UKQCD long-distance `K -> pi l+l-` exploratory calculation and the 2022
Snowmass rare-kaon white paper.  I also deposited the requested 2008 CFW RS
flavor baseline snapshot.

Rejected or not used: I did not track PDG or publisher PDFs.  I did not use
conference-slide formulae found in search results because the same CPV
decomposition is available from the arXiv paper.  I did not use newer generic
rare-kaon reviews as canonical value sources because they do not supersede the
PDG/KTeV experimental limit.

Code coverage: targeted greps over `quarkConstraints/`, `qcd/`,
`flavorConstraints/`, `neutrinos/`, `yukawa/`, `warpConfig/`, `solvers/`,
`scanParams/`, and `tests/` found no K008 implementation.  The nearest kaon
coverage is neutral-meson Delta F=2 machinery, especially
`quarkConstraints/deltaf2.py:209` and the modern policy list at
`quarkConstraints/modern/phenomenology.py:23`.

Open issues for WA/CA: verify at checker time that no final 2026 PDG listing
change has appeared after this snapshot, and decide whether K010 should carry
the `K_S -> pi0 e+e-` value as its own primary value while K008 keeps it only as
supporting input.
