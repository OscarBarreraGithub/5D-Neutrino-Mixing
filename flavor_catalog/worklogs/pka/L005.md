# L005 PKA Worklog

I followed the required reading order, then checked that no L005 process files
or reference directory existed before creating this deposit.  The canonical
value search focused on PDG live muon listing `NODE=S004`, because neighboring
L003/L004 drafts use that stream for the same process family.  I accessed
`https://pdg.lbl.gov/encoder_listings/s004.pdf` only as an input stream,
converted it to text with `pdftotext`, and saved a minimal extract rather than
tracking the PDF.  The relevant PDG row gives
`sigma(mu- Ti -> e- Ti) / sigma(mu- Ti -> capture) < 4.3 x 10^-12` at `90%`
C.L., document `DOHMEN 93`, technique `SPEC`, comment `SINDRUM II`, with a
ground-state assumption note.

For the primary experimental paper identity, I used Crossref DOI metadata for
`10.1016/0370-2693(93)91383-X` and also checked the RWTH publication record
for the title, journal, and collaboration metadata.  I rejected publisher PDF
downloads and ScienceDirect full-text access under the catalog licensing
policy.  Post-2008 context used arXiv snapshots for Mu2e TDR `1501.05241`,
COMET Phase-I TDR `1812.09018`, and the requested RS-flavor baseline
`0804.1954`.  Mu2e and COMET are recorded as aluminum-program context, not as
titanium measurements.

Coverage greps were run over `quarkConstraints`, `qcd`, `flavorConstraints`,
`neutrinos`, `yukawa`, `warpConfig`, `solvers`, `scanParams`, and `tests`.
The targeted conversion grep returned no hits.  Generic `conversion` hits were
unit-conversion or bag-parameter-conversion internals.  The only adjacent LFV
implementation is `flavorConstraints/muToEGamma.py:75`, called from
`scanParams/scan.py:524`.  I therefore marked coverage `NO` and implementation
difficulty `HIGH`, because a real constraint needs lepton-quark operators,
titanium nuclear inputs, capture normalization, and target-dependent amplitude
handling.  Open checker items are limited to PDG-live snapshot preference and
the future operator/nuclear convention choice.
