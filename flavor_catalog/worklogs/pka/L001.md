# PKA Worklog: L001 mu -> e gamma

I first checked out `flavor-catalog/2026q2` and read the required plan,
orchestrator decisions, Opus signoff, process template, and catalog README in
order. The relevant assignment row is L001, charged-lepton LFV, with initial
coverage `YES-LFV-MUEG`.

Source search focused on primary or PDG-equivalent materials. Queries included
`MEG II 2025 mu -> e gamma 1.5e-13 90% CL`, `New limit on the mu+ -> e+ gamma
decay with the MEG II experiment arXiv`, `PDG 2025 muon listing mu -> e gamma`,
and direct arXiv lookups for `2504.15711`, `2310.12614`, `1605.05081`, and
`0805.4652`. I rejected Wikipedia/HandWiki and conference-slide pages as
noncanonical, although they were useful for locating the 2025 paper. I did not
store publisher PDFs. For PDG I downloaded the listing PDF only to a temporary
directory, ran `pdftotext`, and saved a minimal text excerpt under
`flavor_catalog/references/L001/`.

The canonical current limit used in the draft is the MEG II 2025 primary result,
`B(mu+ -> e+ gamma) < 1.5e-13` at 90% C.L. The PDG 2025 muon listing snapshot
still records `<3.1e-13`; I retained that as audit context and marked the newer
MEG II paper as the PDG-equivalent current value because it matches the in-code
scan default. I also captured MEG II 2024, MEG 2016, and Perez-Randall 2008
snapshots for post-2008 history and code-normalization context.

Coverage greps were rerun over `quarkConstraints`, `qcd`, `flavorConstraints`,
`neutrinos`, `yukawa`, `warpConfig`, `solvers`, `scanParams`, and `tests`, plus
the plan-required `docs` and `scripts` auxiliary paths. The YES evidence is
`flavorConstraints/muToEGamma.py:1`, `:31`, `:61`, `:75`, and
`scanParams/scan.py:32`, `:33`, `:264`, with tests in
`tests/test_mu_to_e_gamma.py` and `tests/test_scan.py`.

Open issues for WA/CA: confirm the display policy for the PDG lag and audit the
paper-era Perez-Randall mass-scaling convention before turning this into final
paper prose.
