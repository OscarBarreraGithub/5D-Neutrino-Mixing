# PKA Worklog: K017

Process assignment: `K -> l nu / R_K`, kaon family, process id `K017`.  I
matched this to plan v1 Section C row `K017`, which names the charged-kaon
leptonic LFU ratio `K+ -> e nu` over `K+ -> mu nu` and marks initial coverage
as `NOT-SEEN`.

Required readings completed in order: `flavor_catalog_plan_v1.md`,
`flavor_catalog_orchestrator_decisions.md`, `flavor_catalog_plan_v1_opus_signoff.md`,
`flavor_catalog/latex/process_template.tex`, and `flavor_catalog/README.md`.
I followed the orchestrator policy of minimal text snapshots only, with no
publisher PDFs.  I used the PDG API because it provides an official text
interface, edition metadata, citation, and CC BY 4.0 license statement.

Primary value extraction: PDG API node `S010R28` gives
`G(K+ -> e+ nu_e)/G(K+ -> mu+ nu_mu) = (2.488 +/- 0.009) x 10^-5`, accessed
2026-05-16.  The PDG listing under `S010.11` records the dominant NA62 2013
input, `(2.488 +/- 0.007_stat +/- 0.007_syst) x 10^-5`, and the KLOE 2009
input, `(2.493 +/- 0.025_stat +/- 0.019_syst) x 10^-5`.  I also kept the
Cirigliano--Rosell SM prediction, `(2.477 +/- 0.001) x 10^-5`, because it is
the standard theory comparator for LFU tests in this channel.  FlaviaNet
`1005.2323` is included as the requested kaon working-group/global-fit context,
KLOE `0907.3594` is snapshotted separately as the second modern experimental
input, and CFW `0804.1954` is kept as the 2008 RS-flavor baseline for the
post-2008 subsection.

Searches run: PDG API queries for `S010R28`, `S010.11`, and `S010`; arXiv
lookups for `1212.4012`, `0707.4464`, `1005.2323`, and `0804.1954`; and the
required repository coverage greps across `quarkConstraints/`, `qcd/`,
`flavorConstraints/`, `neutrinos/`, `yukawa/`, `warpConfig/`, `solvers/`,
`scanParams/`, and `tests/`.  A specific `K+ e nu / K+ mu nu / K_l2` search
returned no matches.  Broader greps found only Delta F = 2 kaon/B/D mixing,
the policy-only modern phenomenology fence, and unrelated `mu->e gamma`.

Conclusion: code coverage is `NO`.  Implementation difficulty is `MEDIUM`
because a live constraint would need a new charged-current leptonic-decay
observable and BSM parameterization, even though the ratio avoids new lattice
matrix elements.  Open issue for WA/CA: if a 2026 PDG API edition appears
before signoff, update the source key and access metadata without changing the
current numerical extraction unless the PDG value itself changes.
