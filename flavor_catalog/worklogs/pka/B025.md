# PKA Worklog: B025

Process chosen: `B025`, `R_D`, from the beauty-sector row "B->D tau nu over
light leptons; HFLAV."  I first checked out `flavor-catalog/2026q2`, then read
the required plan v1, orchestrator decisions, Opus signoff, process template,
and catalog README in order.  Existing untracked B017 files were present before
this work; I did not touch them.

For the canonical value I used the HFLAV semileptonic CKM 2025 page for the
joint `R(D)` and `R(D*)` average, because it is newer than Spring 2025 and is
dated 2025-09-28.  The headline value recorded here is `R(D)=0.358 +/- 0.024`,
with joint-fit context `R(D*)=0.281 +/- 0.011`, correlation `-0.374`,
`chi2/dof=16.683/14`, and `CL=0.27`.  The same HFLAV page quotes the arithmetic
SM reference `R(D)=0.296 +/- 0.004`, `R(D*)=0.254 +/- 0.005`, and a combined
about `3.8 sigma` discrepancy.  I saved both the HFLAV HTML snapshot and the
plain-text CoCo logfile; no HFLAV or publisher PDFs were saved.

Primary-source snapshots are arXiv API text records for BaBar 2012
(`1205.5442`), BaBar 2013 (`1303.0571`), Belle 2015 (`1507.03233`), Belle 2020
(`1910.05864`), LHCb 2023 (`2302.02886`), LHCb 2025 (`2406.03387`), Belle II
2025 (`2504.11220`), FLAG 2024 (`2411.04268`), and the requested 2008 baseline
arXiv:`0804.1954`.  The Belle II CKM-2025 hadronic-tag input appears on the
HFLAV page as a conference talk PDF; I left it external-only under the no-PDF
policy.

Coverage result: `NO`.  The required broad greps found the repo's neutral
Delta-F=2 and `mu->e gamma` surfaces, not this process.  Targeted fixed-string
searches over `quarkConstraints/`, `qcd/`, `flavorConstraints/`, `neutrinos/`,
`yukawa/`, `warpConfig/`, `solvers/`, `scanParams/`, and `tests/` for `R(D)`,
`B -> D`, `B->D`, `D tau`, `b -> c`, `charged Higgs`, and `leptoquark`
returned no implementation hits.  I classify implementation difficulty as
`HIGH`: a useful live constraint needs charged-current semileptonic operators,
form-factor/rate integration for `B -> D`, and correlated handling with `R(D*)`.
