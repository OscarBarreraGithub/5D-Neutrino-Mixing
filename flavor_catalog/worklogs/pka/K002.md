# K002 PKA Worklog

Assigned process: Delta m_K, the K_L-K_S mass difference.  Plan v1 maps this
to `K002` in the kaon family with initial coverage `PARTIAL-D2-KM`; I used the
literal process id and path requested by the prompt.

I read the required governance files in order: plan v1, orchestrator decisions,
Opus sign-off, the process template, and the catalog README.  I followed the
licensing decision to store only minimal text snapshots plus URL, access date,
and sha256.  No publisher PDFs were downloaded or committed.

For the canonical value I used the 2026 PDG pdgLive DataBlock `S013D`, which
lists `m(K_L^0)-m(K_S^0)` in units of `10^10 hbar s^-1`.  The sidecar uses
the PDG `OUR FIT` assuming CPT, `0.5293 +/- 0.0009`, and retains the companion
no-CPT fit, `0.5289 +/- 0.0010`, for checker review.  I also captured the KTeV
arXiv page `1011.0127`, because PDG lists Abouzaid 2011 as the current
high-precision post-2008 experimental input.

For post-2008 theory I searched for `Delta m_K lattice`, `K_L-K_S mass
difference lattice QCD`, and `BSM kaon mixing physical masses`.  I kept
arXiv snapshots for Christ et al. `1212.5931`, Bai et al. `1406.0916`, Wang
`2001.06374` and `2301.01387`, Boyle et al. `2404.02297`, and FLAG Review
2024 `2411.04268`.  I rejected publisher landing pages and PDFs when an arXiv
abstract snapshot or minimal PDG text snapshot carried the needed metadata.
ResearchGate, conference-slide PDFs, and third-party summaries were not used.

Code coverage was rechecked with the plan v1 grep commands and a focused K002
grep over `quarkConstraints/`, `qcd/`, `flavorConstraints/`, `neutrinos/`,
`yukawa/`, `warpConfig/`, `solvers/`, `scanParams/`, and `tests/`.  The legacy
helper is in `quarkConstraints/deltaf2.py:755`; the modern lane also has an
explicit `K` input at `quarkConstraints/modern/inputs.py:955` and bridge
evaluator at `quarkConstraints/modern/phenomenology.py:493`.  I still recorded
coverage as `PARTIAL` to match the assignment row and because a standalone
long-distance policy is not audited here.

Open issues: CA should confirm whether final display should keep both PDG CPT
conventions and whether modern-lane evidence upgrades the initial `PARTIAL`
label during writer/checker normalization.

