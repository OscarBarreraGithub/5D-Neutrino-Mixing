# PKA Worklog: K013

Process assigned: \(K_L \to \pi^0\gamma\gamma\), the PI's ambiguous `K1 -> pi gamma` seed.  I read the required plan, orchestrator decisions, Opus signoff, template, and README before drafting.  Section C and the orchestrator decisions identify this as `K013` in the `kaon` family, so I used `flavor_catalog/processes/kaon/K013.{tex,yaml}`.  I did not update the catalog index.

Primary-value extraction: I used the PDG 2025 `K_L0` listing as the canonical source.  The relevant PDG block gives `Gamma(pi0 2 gamma)/Gamma_total = 1.273 +/- 0.033` in units of `10^-6`, averaged from `ABOUZAID 08` (KTeV) and `LAI 02B` (NA48).  I saved a minimal normalized text snapshot under `references/K013/pdg2025_kl_pi0gammagamma.txt`.  I also captured arXiv/metadata excerpts for KTeV 2008 and NA48 2002.  A small ambiguity remains in the user prompt: it says "NA48/2 + KTeV BR measurement," but the neutral-mode PDG average is NA48, not NA48/2.  I included NA48/2 only as post-2008 charged-mode context.

Theory/context checked: I added Gabbiani-Valencia 2001 for the relation to the CP-conserving component of \(K_L\to\pi^0 e^+e^-\), and Cappiello-Cata-D'Ambrosio 2018 for the post-2008 weak-chiral-coupling context.  I did not find a post-2008 neutral-mode branching-ratio replacement in PDG; KTeV 2008 remains the final-sample input.

Code coverage: I reran the plan-required greps plus a K013-specific search across `quarkConstraints`, `qcd`, `flavorConstraints`, `neutrinos`, `yukawa`, `warpConfig`, `solvers`, `scanParams`, and `tests`.  The exact K013 search returned no implementation hits.  Broader required greps found only the existing \(\Delta F=2\) kaon-mixing lane and the unrelated lepton dipole code.  I therefore marked coverage `NO` and implementation difficulty `HIGH`.

Open questions for WA/CA/Opus: confirm the PI is satisfied with K013 as the interpretation of `K1 -> pi gamma`; decide whether the NA48/2 charged-mode paragraph should stay in K013 or move entirely to a separate radiative-kaon context note.
