# PKA Worklog: T007

Process: `t -> c h`, assigned as `T007` in the `top_higgs_ew` family.  I first
checked out `flavor-catalog/2026q2`, then read the plan, orchestrator decisions,
Opus signoff, process template, and catalog README in the requested order.  I
confirmed that no `T007` files or reference directory already existed.

For the canonical value I used the current PDG `pdgLive` 2026 top-quark entry
for `t -> H c`, specifically the `Q007R01` datablock behind the branching-ratio
page.  It gives the headline 95% CL upper limit
`B(t -> H c) < 3.4e-4`, sourced to the ATLAS 2024 five-Higgs-mode combination.
I saved a minimal text snapshot rather than any PDG PDF.  I cross-checked the
source against the ATLAS arXiv abstract `2404.02123`, which reports the
140 fb^-1, sqrt(s)=13 TeV multilepton analysis and the same combined
`B(t -> Hc) < 3.4e-4` observed limit, expected `2.3e-4`.

I also captured the newer CMS arXiv abstract `2407.15172`, published as
PRD 112, 032008 in 2025.  CMS uses 138 fb^-1 at 13 TeV and reports a combined
limit `B(t -> Hc) < 0.037%`, close to but slightly weaker than the PDG/ATLAS
headline.  I kept PDG as canonical and noted that WA/CA may decide how prominent
the CMS value should be.  For theory context I stored short arXiv snapshots for
Aguilar-Saavedra `hep-ph/0409342` and the requested CFW baseline
`0804.1954`.

Rejected sources: publisher PDF pages from Springer, APS, and CDS were not
tracked because the orchestrator policy requires minimal text snapshots only.
I did not use secondary news or summary pages.

Code coverage was checked with the three plan greps and a targeted search for
`t -> Hc`, `tHc`, `tcH`, `Htc`, and `tqH` across the requested code
directories.  No implementation exists; only generic FCNC prose/comment hits
appeared in `quarkConstraints/PAPER_0710_1869.md` and
`tests/test_paper_couplings.py`.
