# PKA Worklog: T005

Process: `t -> c g`, assigned from plan v1 Section C as `T005` in the
`top_higgs_ew` family.  The assigned row is "Top FCNC gluon decay /
production; ATLAS/CMS/PDG", with initial coverage `NOT-SEEN`.

I first checked out `flavor-catalog/2026q2` and read the required planning
documents, process template, and catalog README in order.  For the canonical
experimental value I used PDG/pdgLive node `Q007TUG`, accessed on 2026-05-16.
That page records the current ATLAS Run-2 single-top FCNC result with
`139 fb^-1` at `sqrt(s)=13 TeV`: for the charm-gluon mode,
`sigma(cg -> t) B(t -> bW) B(W -> l nu) < 4.7 pb`,
`|C_uG^{ct}|/Lambda^2 < 0.14 TeV^-2`, and
`B(t -> c g) < 3.7e-4` at 95% CL.  I also captured the CMS 7+8 TeV
comparison result from arXiv:1610.03545: `|kappa_tcg|/Lambda < 1.8e-2
TeV^-1` and `B(t -> c g) < 4.1e-4`.

For theory context I used Aguilar-Saavedra arXiv:hep-ph/0409342, which gives
the Standard Model benchmark `B(t -> c g)=4.6e-12`.  I included
arXiv:0804.1954 only as the requested 2008-era RS flavor baseline: its
`21 TeV` and `33 TeV` KK-gluon scales are model-context numbers, so they are
kept in `paper_era_references` rather than `pdg_or_equivalent`.

Source snapshots are short text excerpts only.  I did not download or track
publisher PDFs.  ArXiv abstract and TeX-source excerpts were used for the
ATLAS, CMS, SM-theory, and CFW sources.

Code coverage was checked with the three plan greps plus a targeted search
for `t -> c g`, `tcg`, `tqg`, `C_uG`, and `cg -> t` across
`quarkConstraints/`, `qcd/`, `flavorConstraints/`, `neutrinos/`, `yukawa/`,
`warpConfig/`, `solvers/`, `scanParams/`, and `tests/`.  No implementation
was found.  The only implementation-directory hits were generic FCNC prose or
comments, not a top-gluon FCNC observable.

Open issues for WA/CA: decide whether to mention the older ATLAS 8 TeV PDG
row prominently, and normalize the final prose around the ATLAS SMEFT
`C_uG` convention versus the older CMS `kappa_tcg` convention.
