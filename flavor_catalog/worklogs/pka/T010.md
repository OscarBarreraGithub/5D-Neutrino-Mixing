# PKA worklog: T010

Assigned process: `Z -> b bbar` pole observables, explicitly including
`R_b`, `A_FB^{0,b}`, and `A_b`.

I read the required plan, orchestrator decisions, Opus signoff, process template,
and catalog README before creating files.  Plan v1 splits this material across
T010 (`Z -> b bbar`, `R_b`) and T011 (`A_FB^b`, `A_b`).  I chose T010 as the
single process id because the user assignment described one combined
Z-bottom pole-observable package; I recorded T011 as a related process in the
sidecar so the WA/orchestrator can either mark it covered or request a later
split.

For canonical values I used the PDG 2025 Z-boson listing, accessed on
2026-05-16, and extracted only a small normalized text snapshot.  The current
PDG values captured are `R_b^0 = 0.21629 +/- 0.00066`,
`A_FB^{0,b} = 0.0992 +/- 0.0016`, and `A_b = 0.923 +/- 0.020`.  I also
consulted the LEP/SLC final Z-resonance combination, `hep-ex/0509008`, because
the PDG listing points to LEP-SLC 06 and because it records the original
combined values and the `A_FB^{0,b}` pull.  No publisher PDFs were added; PDF
downloads were temporary extraction inputs only.

For RS relevance and post-2008 context I consulted CFW `0804.1954`,
Casagrande-Goertz-Haisch-Neubert-Pfoh `0807.4937`, Freitas `1401.2447`, and a
2025 FCC-ee projection `2502.17281`.  The conclusion is that this process is a
precision electroweak coupling constraint rather than a flavor-changing decay,
but it is directly tied to the same third-generation localization and custodial
protection choices that control RS flavor.

Code coverage search across the required directories found no `R_b`, `A_b`,
`A_FB^b`, or Zbb observable implementation.  Only generic `M_Z` support appeared
in QCD helper files.  I therefore marked code coverage `NO` and implementation
difficulty `HIGH`, since a live constraint needs new electroweak-pole observable
handling and RS matching for bottom Z-coupling shifts.
