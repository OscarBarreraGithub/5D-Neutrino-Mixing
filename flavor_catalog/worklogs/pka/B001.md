# PKA Worklog: B001

Process: `Delta m_d` / `B_d` mixing; family `beauty`; agent `PKA-B001`.

I followed the required catalog planning documents, the orchestrator decisions,
the Opus signoff notes, the process template, and the top-level catalog README
before creating files.  I also checked that the requested B001 files did not
already exist.  Source searches used queries such as `HFLAV PDG 2025 Delta m_d
B0 mixing`, `PDG 2025 B0 B0bar mixing Delta m_d`, `Belle II Delta m_d 2023
arXiv`, and `FLAG Review 2024 B mixing`.  The canonical value is from the
HFLAV/PDG 2025 oscillation page: `Delta m_d = 0.5069 +/- 0.0019 ps^-1`.  I kept
`x_d` and `chi_d` only as companion values.  Belle II arXiv:2302.12791 is used
as a concrete post-2008 experimental update, FLAG Review 2024 as lattice/theory
context, and arXiv:0804.1954 as the 2008 RS-flavor baseline.

I rejected publisher/PDF caching and stored only small text extracts with URLs,
access dates, and SHA-256 hashes.  Search results pointing to slides, mirrors,
old PDG PDFs, and generic publisher pages were not used once the HFLAV, arXiv,
and FLAG sources were available.

For code coverage I ran the required catalog greps across `quarkConstraints`,
`flavorConstraints`, `scanParams`, `neutrinos`, `yukawa`, `qcd`, `warpConfig`,
`solvers`, and `tests`, excluding notebooks.  The result is `YES-D2-BD`:
`quarkConstraints/deltaf2.py` defines the `b_d` input, stores the B_d hadronic
and experimental constants, evaluates `B_d` mixing, and provides the QCD-running
wrapper.  The modern policy/metadata layer also lists `B_d`, but the production
backend remains the legacy Delta F=2 evaluator.  Open issue for WA/CA: decide
whether the prose should keep `x_d` and `chi_d` or leave them only in YAML.
