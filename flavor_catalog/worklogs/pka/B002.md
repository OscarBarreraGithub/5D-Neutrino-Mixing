# PKA Worklog: B002

Assigned process: `S_psiK_S / sin(2 beta)`, process id `B002`, in the
`beauty` family.  I read the required plan, orchestrator decisions, Opus
signoff, template, and top-level catalog README before creating any files.
No pre-existing B002 process, sidecar, reference directory, or PKA worklog was
present.

For the canonical value I used the current HFLAV CPV/Unitarity Triangle
Summer 2025 page, accessed on 2026-05-16.  This is newer than the PDG 2024
triangle page and includes the 2024 LHCb and Belle II inputs.  The primary
headline value recorded in the YAML sidecar is the HFLAV all-charmonium
average `sin(2 beta) = 0.710 +/- 0.011`, with stat-only uncertainty `0.010`
and CL `0.34`.  Because the assigned process is specifically the
`B_d -> J/psi K_S` gold-plated channel, I also recorded the HFLAV
mode-specific `J/psi K_S` average `S = 0.712 +/- 0.011`.

The post-2008 update is driven by precision inputs rather than a conceptual
change.  I stored minimal text snapshots for the latest LHCb result
(`S = 0.717 +/- 0.013(stat) +/- 0.008(syst)` using `6 fb^-1`) and the Belle II
result (`S = 0.724 +/- 0.035(stat) +/- 0.009(syst)` using `362 fb^-1`).
Theory-systematics snapshots include Faller--Fleischer--Jung--Mannel for the
golden-mode precision strategy and Frings--Nierste--Wiebusch for the
penguin-phase bound `|Delta phi_d| <= 0.68 degrees`.  I also added a short
arXiv:0804.1954 snapshot as the required 2008 RS-flavor baseline anchor.  No
publisher PDFs were stored; all local reference files are small text extracts
with sha256 hashes in `source_manifest.yaml`.

For code coverage I ran the required implementation/test-directory greps.
There is no `S_psiK_S`, `sin2beta`, or equivalent CP-phase observable.  I
marked coverage `PARTIAL`, not `NO`, because the repo already has the B_d
Delta F=2 input, B_d hadronic parameters, QCD running for the VLL/VRR/LR
basis, and an `evaluate_bd_mixing` function.  The current evaluator takes
`abs(m12)`, so it supports the magnitude constraint but not the phase
observable.  I marked implementation difficulty `LOW`: no new operator or RG
basis is needed, but the complex amplitude, CKM convention, penguin budget,
and observable policy must be added.
