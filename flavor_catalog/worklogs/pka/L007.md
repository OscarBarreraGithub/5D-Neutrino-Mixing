# L007 PKA Worklog

I followed the required reading order on `flavor-catalog/2026q2`: plan v1,
orchestrator decisions, Opus signoff, the process template, and the catalog
README.  The process-row assignment is L007, charged-lepton family, `tau ->
mu gamma`, initially marked `NOT-SEEN` for code coverage.

Source searches used: `PDGLive tau -> mu gamma branching fraction upper
limit`, `PDG tau mu gamma 4.2e-8`, `Belle tau l gamma 988 fb arXiv`,
`BaBar tau e gamma tau mu gamma 4.4e-8 arXiv`, `Belle II tau mu gamma
50 ab-1 sensitivity`, `arXiv:0804.1954`, and `arXiv:0805.4652`.  I used
PDGLive `S035.31` as the canonical value source, with the current limit
`B(tau- -> mu- gamma) < 4.2 x 10^-8` at 90% CL.  The primary experimental
snapshot is Belle arXiv:2103.12994 / JHEP 10 (2021) 019, which reports the
same limit using 988 fb^-1.  I also stored BaBar arXiv:0908.2381 as the
post-2008 predecessor and the Belle II Physics Book arXiv:1808.10567 as a
prospect source for 50 ab^-1 sensitivity.  I did not store publisher PDFs;
all deposits are minimal text snapshots under `flavor_catalog/references/L007`.

For RS context I snapshotted arXiv:0804.1954 as the requested baseline and
arXiv:0805.4652 for the warped lepton-sector dipole/LMFV assumptions.  I
rejected secondary-only phenomenology papers and broad review snippets when
the same numerical claims were available from PDG, Belle, BaBar, or the Belle
II Physics Book.  I also rejected Belle II `tau -> 3mu` results as a different
process, useful only as evidence of the active Belle II tau-LFV program.

Coverage grep was run over the required code paths.  A targeted tau/mu/gamma
grep returned no matches.  The only adjacent implementation is
`flavorConstraints/muToEGamma.py`, called by `scanParams/scan.py`, and it is
hard-coded around the `(1,2)` mu-to-e dipole.  I therefore marked L007 code
coverage as `NO` and implementation difficulty as `MEDIUM`.
