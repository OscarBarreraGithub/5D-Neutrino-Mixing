# K003 PKA Worklog

Assigned process: epsilon'/epsilon direct CP violation in K -> pi pi.
Plan v1 Section C maps this unambiguously to `K003` in the kaon family.  I
used `kaon` as the natural family path.

I first checked the required governance files in order: plan v1, orchestrator
decisions, Opus sign-off, the process template, and the catalog README.  The
licensing rule I followed was minimal text snapshots with URL, access date,
and sha256; no publisher PDFs were downloaded or committed.

For the canonical value I used the PDG pdgLive K0 listing row S013EPS, which
shows `Re(epsilon'/epsilon) = 0.00166 +/- 0.00023 (S = 1.6)`.  I also saved
the PDG S013EPS DataBlock because it exposes the measurement table: `OUR FIT`
is `1.67 +/- 0.23` and `OUR AVERAGE` is `1.68 +/- 0.20` in units of `10^-3`.
The DataBlock also lists the KTeV `ABOUZAID 2011` input and the NA48
`BATLEY 2002` input.  I treated the K0 listing as the canonical sidecar value
and noted the small listing-vs-DataBlock display ambiguity for the checker.

For primary experimental inputs I consulted arXiv pages for KTeV
`1011.0127`, reporting `(19.2 +/- 2.1) x 10^-4`, and NA48
`hep-ex/0208009`, reporting `(14.7 +/- 2.2) x 10^-4`.  For post-2008 theory
I used RBC/UKQCD `2004.09440`, which reports the lattice result
`21.7(2.6)(6.2)(5.0) x 10^-4`, and Aebischer-Bobeth-Buras `2005.05978`,
which records ChPT/isospin-breaking updates and SM values
`(17.4 +/- 6.1) x 10^-4` and `(13.9 +/- 5.2) x 10^-4`.  I also consulted
BSM master-formula/anatomy papers `1807.02520` and `1808.00466`, plus the
CFW reference `0804.1954` as the requested 2008 baseline.

Code coverage is `NO`.  Focused grep over `quarkConstraints/`, `qcd/`,
`flavorConstraints/`, `neutrinos/`, `yukawa/`, `warpConfig/`, `solvers/`,
`scanParams/`, and `tests/` found no direct epsilon-prime, K->pipi,
Delta-S=1, Q6, Q8, or O8g implementation.  The nearby kaon implementation is
Delta-F=2 only: `quarkConstraints/deltaf2.py:1`, `:209`, and `:729`.

Open question for WA/CA: decide whether to display only the PDG K0 listing
value or also call out the DataBlock `OUR AVERAGE` in final prose.  I kept both
snapshots so the checker can verify the convention.

