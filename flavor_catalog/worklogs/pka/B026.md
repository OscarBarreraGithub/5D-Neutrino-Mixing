# PKA Worklog: B026 \(R_{D^*}\)

Date: 2026-05-16.  Agent: PKA-B026.

I first checked out `flavor-catalog/2026q2` and read the required planning
documents in order: plan v1, orchestrator decisions, Opus signoff, the process
template, and the catalog README.  The process-table row assigns B026 to the
beauty family as the charged-current LFU ratio \(R_{D^*}\), with HFLAV as the
likely canonical source and initial code coverage `NOT-SEEN`.

Source search focused on the HFLAV CKM 2025 semileptonic page for the joint
\(R_D\)-\(R_{D^*}\) average.  I snapshotted the HFLAV HTML page and CoCo logfile
as text under `flavor_catalog/references/B026/`.  The current HFLAV page is
updated 2025-09-28 and quotes \(R_{D^*}=0.281\pm0.011\), \(R_D=0.358\pm0.024\),
correlation \(-0.374\), experimental-combination \(\chi^2/\mathrm{dof}=16.683/14\),
and a \(3.8\sigma\) combined discrepancy against its displayed arithmetic SM
reference.  This is higher than the prompt's approximate \(3.3\sigma\), so I
listed that as a WA/CA item rather than downgrading the current source.

Primary-reference deposits are minimal arXiv abstract-page extracts for BaBar,
Belle, LHCb, Belle II, FLAG 2024, and the 2008 CFW baseline.  I initially hit
arXiv API rate limits and then used arXiv abstract pages, trimming page chrome
and long author lists so every source snapshot remains below 100 KB.  I did not
download publisher PDFs.  I also did not snapshot the CKM-2025 Belle II hadronic
tag talk PDF from Indico; the HFLAV table entry is retained as the source for
that input under the no-PDF policy.

For code coverage I reran the required greps over `quarkConstraints/`, `qcd/`,
`flavorConstraints/`, `neutrinos/`, `yukawa/`, `warpConfig/`, `solvers/`,
`scanParams/`, and `tests/`.  The searches found the existing Delta F=2 and
mu-to-e-gamma surfaces, but targeted patterns for \(R(D^*)\), \(D^*\tau\),
\(B\to D^*\), \(b\to c\), charged-Higgs, and leptoquark observable code had no
implementation hits.  I therefore marked coverage `NO` and implementation
difficulty `HIGH`.
