# K009 PKA Worklog

Process: `K_L -> pi0 mu+ mu-` (`K009`, kaon).  I first checked out
`flavor-catalog/2026q2` and read the required plan, orchestrator decisions,
Opus signoff, process template, and catalog README in order.  Existing local
changes from other processes were left untouched.

Searches and source choices: I used PDG pdgLive API enumeration to locate the
correct long-lived listing `S013.16` / data block `S013R30`; an initial query
of nearby `S013.21` was rejected because it is the different
`K_L -> mu+mu- e+e-` mode.  The canonical measured value is the PDG/KTeV
90% CL upper limit `BR(K_L -> pi0 mu+ mu-) < 3.8e-10`.  I deposited a minimal
PDG text snapshot and a KTeV arXiv abstract snapshot (`hep-ex/0001006`) with
the event and background counts.  For the indirect-CP input I found PDG
`S012.15` and the NA48 arXiv abstract (`hep-ex/0409011`) for
`K_S -> pi0 mu+ mu-`; this is supporting material, not the K009 canonical
measured observable.  For the CP decomposition I extracted the muon-mode
coefficients from the arXiv source of Isidori, Smith, and Unterdorfer
(`hep-ph/0404127`).  I also deposited the requested CFW 2008 RS-flavor baseline
and post-2008 context snapshots for the 2016 RBC/UKQCD exploratory
long-distance calculation and the 2022 Snowmass rare-kaon white paper.

Rejected or not used: I did not track PDG or publisher PDFs.  I did not use
conference slides on `K_L -> pi0 l+l-` because the needed decomposition is
available from the arXiv source.  I did not treat theoretical normalization
coefficients as `pdg_or_equivalent`; they are recorded under
`theory_decomposition`, following the arbitration policy.

Code coverage: the prescribed broad greps and a targeted fixed-string grep over
`quarkConstraints/`, `qcd/`, `flavorConstraints/`, `neutrinos/`, `yukawa/`,
`warpConfig/`, `solvers/`, `scanParams/`, and `tests/` found no K009
implementation.  The closest kaon coverage is `Delta F=2`, specifically
`quarkConstraints/deltaf2.py:209` and
`quarkConstraints/modern/phenomenology.py:23`.

Open issues for WA/CA: re-query PDG `S013.16` before freeze and keep the
`K_S -> pi0 mu+ mu-` value synchronized with the separate K011 process.
