# C002 PKA Worklog

I checked out `flavor-catalog/2026q2` before reading the required planning
documents.  The governing points for this deposit are: use minimal text
snapshots only, keep all writes under `flavor_catalog/`, treat C002 as the
HFLAV `|q/p|` and `phi_D` neutral-charm CPV process, and rerun the coverage
grep rather than trusting the initial NOT-SEEN row.

Primary source search focused on current HFLAV charm mixing pages.  The
canonical value comes from the HFLAV CKM25 page
`https://hflav-eos.web.cern.ch/hflav-eos/charm/CKM25/results_mix_cpv.html`
and its linked `table_results.pdf`; I piped the PDF through `pdftotext` and
stored only a short text extract, not the PDF.  I cross-checked the same
numbers against a minimal PDG 2025 D0-D0bar mixing review excerpt.  For
post-2008 context, I saved arXiv abstract snapshots for LHCb
`2208.06512`, Belle/Belle II `2410.22961`, and the RS baseline
`0804.1954`.  I did not store publisher PDFs.  I rejected older HFLAV pages
such as CKM21, CKM23, FPCP08, and EPS09 as non-canonical for the current
value, though they confirmed the historical evolution.

Coverage grep used the required implementation directories:
`quarkConstraints`, `qcd`, `flavorConstraints`, `neutrinos`, `yukawa`,
`warpConfig`, `solvers`, `scanParams`, and `tests`.  The result is PARTIAL.
The legacy and modern D0 lanes evaluate a conservative D-mixing magnitude, and
the paper-mode Delta-F=2 code can serialize complex `M12_D0_NP_GeV` including
its imaginary component.  However, no exported scan or modern-phenomenology
path evaluates the HFLAV observables `|q/p|` or `phi_D`.

Unresolved issue for WA/CA: decide whether C002 should remain a qualitative
phase diagnostic or graduate to a production likelihood using the HFLAV
`|q/p|`-`phi_D` contour/covariance.  My implementation-difficulty call is LOW
because no new operator basis or RG machinery is needed, but the exact
likelihood policy is a physics-choice question.
