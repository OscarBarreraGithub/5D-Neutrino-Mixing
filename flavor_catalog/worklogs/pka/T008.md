# PKA Worklog: T008

Process: `t -> H u` top-Higgs FCNC decay.  I followed the required plan files
and local template before source work.  The existing T007 charm-channel draft
was used only for local style and for identifying the relevant companion
ATLAS/CMS sources; T008 has separate snapshots and a separate sidecar.

Searches used: PDG `pdgLive` top-quark page and the `Q007` decay clump for
Delta T = 1 weak neutral-current modes; targeted PDG datablock
`Q007R00`; arXiv queries for `2111.02219`, `2404.02123`, `2407.15172`, and
`0804.1954`; web search for `B(t -> H u) PDG`, `TUMASYAN 2022A t Hu`, and
`CMS TOP-22-002 t Hu`.  The canonical value I found is the PDG 2026 headline
limit `B(t -> H u) < 1.9e-4` at 95% CL, sourced to CMS 2022 diphoton data.
This differs from the prompt's approximate `90% CL` wording; all primary
sources checked here state 95% CL.  The CMS 2025 combination has the same
observed value but a different expected limit, so I kept PDG as canonical and
recorded CMS 2025 as a companion source.

Sources deposited are minimal text snapshots only.  I did not save publisher
PDFs.  The PDG snapshot is a short manual excerpt of the top-quark datablock
with URL, access date, and values.  The arXiv snapshots are abstract-page
summaries for CMS 2022 diphoton, ATLAS 2024 multilepton/combination, CMS 2025
same-sign/combination, and the requested paper-era RS/composite-Higgs context
paper arXiv:0804.1954.

Code coverage: reran the required grep set across `quarkConstraints/`, `qcd/`,
`flavorConstraints/`, `neutrinos/`, `yukawa/`, `warpConfig/`, `solvers/`,
`scanParams/`, and `tests/`, plus a targeted top-Higgs FCNC grep.  No T008
observable or matching code is present; only generic FCNC comments appeared in
`quarkConstraints/PAPER_0710_1869.md:35` and
`tests/test_paper_couplings.py:258`.  Main open issue for WA/CA is whether to
prefer the PDG headline row or the CMS 2025 combination in prose when the
observed limit is numerically identical.
