# PKA Worklog: B034

Process: `B_s -> phi phi` penguin CP phase, process ID `B034`, family
`beauty`.  I first read the required plan, orchestrator decisions, Opus
signoff, template, and README, then worked only under `flavor_catalog/`.

Searches used: `LHCb B_s -> phi phi CP violation phi_s ssbar s measurement`,
`HFLAV B_s phi phi branching fraction`, `PDG B_s phi phi beta_s b -> s sbar s`,
`site:cds.cern.ch B_s phi phi CP violation 2304.06198`, and older LHCb
queries for the 2014 and 2019 inputs.  Primary sources kept as minimal local
text snapshots were the PDG `B_s^0` listing, LHCb 2023 CDS/arXiv metadata,
LHCb 2019 CDS metadata, LHCb 2014 arXiv metadata, the HFLAV May 2024
branching-fraction table, and arXiv:0804.1954 for the paper-era RS-flavor
context.  I did not store publisher PDFs.  I rejected ResearchGate, university
repository mirrors, indico slides, and PDF-only publisher pages when a CDS,
arXiv, PDG, or HFLAV source was available.

Canonical measured values recorded in the YAML are the LHCb/PDG combined
`phi_s^{s sbar s} = -0.074 +/- 0.069 rad` and `|lambda| = 1.009 +/- 0.030`,
with the PDG beta_s convention also stored.  HFLAV's `B_s^0 -> phi phi`
branching-fraction average is included as contextual measured channel
information, not as the headline CP observable.

Coverage greps over `quarkConstraints/`, `qcd/`, `flavorConstraints/`,
`neutrinos/`, `yukawa/`, `warpConfig/`, `solvers/`, `scanParams/`, and
`tests/` found no direct B034 implementation.  The only adjacent support is
neutral `B_s` mixing magnitude machinery in `quarkConstraints/deltaf2.py` and
`quarkConstraints/modern/phenomenology.py`, so the sidecar marks coverage `NO`
and implementation difficulty `HIGH`.

Open issue for WA/CA: decide whether the prose should lead with PDG's
`beta_s` convention or LHCb's direct `phi_s^{s sbar s}` convention.  I used
the latter because it matches the experimental time-dependent CP papers.
